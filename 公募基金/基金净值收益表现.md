# 基金净值收益表现（MCP 工具 `ft_get_fund_net_value_performance`）

> **MCP 工具**：`ft_get_fund_net_value_performance`（category: `公募基金`）。返回统一 MCP 输出：`structuredContent.metadata` + `structuredContent.data`；`content[0].text` 是同值的序列化 JSON，不额外返回 Markdown。输入参数 / 输出参数 / 数据样例见下文。
> 文中 `Response`、`items`、`records`、`code`、`message` 等名称仅为字段说明；MCP 对外固定为上述 `metadata/data`。

- 描述：按 `fund_code` 查净值口径收益表现。`stat_date` 单日或 `start_date`/`end_date` 区间（互斥）。每个统计日期一条记录，含日收益率及本周/近一周、本月/近一月、近三月/六月、今年、近一/二/三/五/十年、成立以来收益率与对应年化收益率等 22 个字段。提示：`fund_code` 必填；`stat_date` 与 `start_date`+`end_da。实时参数约束以 `tools/list` 返回的 `inputSchema` 为准。
- 数据范围：以服务端返回为准
- 单次限量：分页返回，默认 `page=1`、`page_size=50`（部分工具上限 200）

## 输入参数

| 名称 | 类型 | 必选 | 描述 |
|------|------|------|------|
| end_date | integer | N | 统计结束日期 |
| fund_code | string | Y | 基金代码 |
| page | integer | N | 页码，默认 1 |
| page_size | integer | N | 每页条数，默认 50，上限 200 |
| start_date | integer | N | 统计开始日期（需与 end_date 同传） |
| stat_date | integer | N | 统计日期 YYYYMMDD（与 start/end 互斥） |

未在 `inputSchema` 中声明的字段会被拒绝。

## 输出参数

> MCP 固定输出信封为 `structuredContent.metadata` + `structuredContent.data`；`content[0].text` 是与其同值的序列化 JSON，不是 Markdown。分页与截断信息统一归入 `metadata`。

| MCP 字段 | 类型 | 必填 | 描述 |
|----------|------|------|------|
| metadata | object | Y | 契约版本、数据来源、工具名、业务口径、总量、分页、返回条数、截断状态及 warnings |
| data | array | Y | 归一化后的业务数据项；元素字段见下方 |

### data 业务字段

| 名称 | 类型 | 默认显示 | 描述 |
|------|------|---------|------|
| fund_code | string | Y | 基金代码 |
| fund_name | string | Y | 基金名称 |
| trade_code | string | Y | 交易代码 |
| stat_date | int | Y | 统计日期 |
| daily_return | string | Y | 日收益率 |
| this_week_return | string | Y | 本周以来收益率 |
| one_week_return | string | Y | 近一周收益率 |
| this_month_return | string | Y | 本月以来收益率 |
| one_month_return | string | Y | 近一月收益率 |
| three_month_return | string | Y | 近三月收益率 |
| six_month_return | string | Y | 近六月收益率 |
| this_year_return | string | Y | 今年以来收益率 |
| one_year_return | string | Y | 近一年收益率 |
| two_year_return | string | Y | 近两年收益率 |
| annualized_two_year_return | string | Y | 近两年年化收益率 |
| three_year_return | string | Y | 近三年收益率 |
| annualized_three_year_return | string | Y | 近三年年化收益率 |
| five_year_return | string | Y | 近五年收益率 |
| annualized_five_year_return | string | Y | 近五年年化收益率 |
| ten_year_return | string | Y | 近十年收益率 |
| annualized_ten_year_return | string | Y | 近十年年化收益率 |
| since_inception_return | string | Y | 成立以来收益率 |
| annualized_since_inception_return | string | Y | 成立以来年化收益率 |

## 调用方法（MCP）

> MCP 工具名 `ft_get_fund_net_value_performance`。MCP Streamable HTTP 要求**先 initialize 拿 `Mcp-Session-Id`，发送 `notifications/initialized`，再 `tools/call`**，后续请求同时带该 Session ID 和协商后的 `MCP-Protocol-Version`。返回统一 `metadata/data` 结构化输出；`content[0].text` 为同值 JSON 文本。

**curl**：

```bash
SID=$(curl -sS -m 10 -D - -o /dev/null -X POST <MCP_BASE_URL> \
  -H "Accept: application/json, text/event-stream" -H "Content-Type: application/json" \
  -d '{"jsonrpc":"2.0","id":1,"method":"initialize","params":{"protocolVersion":"2025-11-25","capabilities":{},"clientInfo":{"name":"t","version":"1"}}}' \
  | grep -i 'mcp-session-id:' | awk '{print $2}' | tr -d '\r')

curl -fsS -m 10 -o /dev/null -X POST <MCP_BASE_URL> \
  -H "Accept: application/json, text/event-stream" -H "Content-Type: application/json" \
  -H "Mcp-Session-Id: $SID" -H "MCP-Protocol-Version: 2025-11-25" \
  -d '{"jsonrpc":"2.0","method":"notifications/initialized"}'

curl -sS -m 10 -X POST <MCP_BASE_URL> \
  -H "Accept: application/json, text/event-stream" -H "Content-Type: application/json" \
  -H "Mcp-Session-Id: $SID" -H "MCP-Protocol-Version: 2025-11-25" \
  -d '{"jsonrpc":"2.0","id":2,"method":"tools/call","params":{"name":"ft_get_fund_net_value_performance","arguments":{"fund_code": "000001", "stat_date": 20260717, "page": 1, "page_size": 2}}}'
```

**Python（`mcp` SDK，自动握手管理 session）**：

```python
import asyncio
from mcp import ClientSession
from mcp.client.streamable_http import streamablehttp_client

async def main():
    async with streamablehttp_client('<MCP_BASE_URL>') as (r, w, _):
        async with ClientSession(r, w) as s:
            await s.initialize()
            res = await s.call_tool('ft_get_fund_net_value_performance', {"fund_code": "000001", "stat_date": 20260717, "page": 1, "page_size": 2})
            print(res.structuredContent)   # 推荐：统一 metadata/data 结构化输出
            print(res.content[0].text)   # 兼容：与 structuredContent 同值的 JSON 文本
asyncio.run(main())
```
