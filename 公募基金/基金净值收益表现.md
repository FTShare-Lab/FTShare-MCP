# 基金净值收益表现（MCP 工具 `ft_get_fund_net_value_performance`）

> **MCP 工具**：`ft_get_fund_net_value_performance`（category: `公募基金`）。返回统一 MCP 输出：`structuredContent.metadata` + `structuredContent.data`；`content[0].text` 是同值的序列化 JSON，不额外返回 Markdown。输入参数 / 输出参数 / 数据样例见下文。
> 文中 `Response`、`items`、`records`、`code`、`message` 等名称仅为字段说明；MCP 对外固定为上述 `metadata/data`。

- 描述：按必填的 `fund_code` 查询净值口径收益表现；`stat_date` 单日条件与 `start_date`/`end_date` 区间条件互斥。数据包括日、本周、近一周、本月、近一月、近三月、近六月、今年、近一/二/三/五/十年及成立以来收益率与对应年化收益率。
- 数据范围：按统计日查询
- 单次限量：默认 `page=1`/`page_size=50`；上限 200
- 提示：
  - `fund_code` 必填。
  - `stat_date` 与 `start_date`+`end_date` 互斥。
  - 每个统计日期返回一条多区间收益率记录。

## 输入参数

| 名称 | 类型 | 必选 | 描述 |
|------|------|------|------|
| fund_code | string | Y | 基金代码 |
| stat_date | int | N | 统计日期 YYYYMMDD（与 start/end 互斥） |
| start_date | int | N | 统计开始日期（需与 end_date 同传） |
| end_date | int | N | 统计结束日期 |
| page | int | N | 页码，默认 1 |
| page_size | int | N | 每页条数，默认 50，上限 200 |

## 输出参数

> MCP 固定输出信封为 `structuredContent.metadata` + `structuredContent.data`；`content[0].text` 是与其同值的序列化 JSON，不是 Markdown。
>
> `items` / `records` / `code` / `message` 等传输字段不会直接出现在 MCP 结果中；分页与截断信息统一归入 `metadata`。

| MCP 字段 | 类型 | 必填 | 描述 |
|----------|------|------|------|
| metadata | object | Y | 契约版本、数据来源、工具名、业务口径、总量、分页、返回条数、截断状态及 warnings |
| data | array | Y | 归一化后的业务数据项 |

### data 业务字段

| 名称 | 类型 | 默认显示 | 描述 |
|------|------|---------|------|
| annualized_five_year_return | string | Y | 近五年年化收益率 |
| annualized_since_inception_return | string | Y | 成立以来年化收益率 |
| annualized_ten_year_return | string | Y | 近十年年化收益率 |
| annualized_three_year_return | string | Y | 近三年年化收益率 |
| annualized_two_year_return | string | Y | 近两年年化收益率 |
| daily_return | string | Y | 日收益率 |
| five_year_return | string | Y | 近五年收益率 |
| fund_code | string | Y | 基金代码 |
| fund_name | string | Y | 基金名称 |
| one_month_return | string | Y | 近一月收益率 |
| one_week_return | string | Y | 近一周收益率 |
| one_year_return | string | Y | 近一年收益率 |
| since_inception_return | string | Y | 成立以来收益率 |
| six_month_return | string | Y | 近六月收益率 |
| stat_date | int | Y | 统计日期 |
| ten_year_return | string | Y | 近十年收益率 |
| this_month_return | string | Y | 本月以来收益率 |
| this_week_return | string | Y | 本周以来收益率 |
| this_year_return | string | Y | 今年以来收益率 |
| three_month_return | string | Y | 近三月收益率 |
| three_year_return | string | Y | 近三年收益率 |
| trade_code | string | Y | 交易代码 |
| two_year_return | string | Y | 近两年收益率 |

## 调用方法（MCP）

> MCP Streamable HTTP 要求**先 initialize 拿 `Mcp-Session-Id`，发送 `notifications/initialized`，再 `tools/call`**，后续请求同时带该 Session ID 和协商后的 `MCP-Protocol-Version`。返回统一 `metadata/data` 结构化输出；`content[0].text` 为同值 JSON 文本，不额外返回 Markdown。

**curl**：

```bash
set -euo pipefail

MCP_BASE_URL="<MCP_BASE_URL>"

check_mcp_response() {
  local response=$1
  printf '%s\n' "$response"
  if printf '%s\n' "$response" | grep -Eq '"isError"[[:space:]]*:[[:space:]]*true|"error"[[:space:]]*:[[:space:]]*\{'; then
    return 1
  fi
}

SID=$(curl -fsS -m 60 -D - -o /dev/null -X POST "$MCP_BASE_URL" \
  -H "Accept: application/json, text/event-stream" \
  -H "Content-Type: application/json" \
  -d '{"jsonrpc":"2.0","id":1,"method":"initialize","params":{"protocolVersion":"2025-11-25","capabilities":{},"clientInfo":{"name":"ftshare-doc-example","version":"1.0.0"}}}' \
  | awk 'tolower($1)=="mcp-session-id:" {print $2}' \
  | tr -d '\r')

if [ -z "$SID" ]; then
  printf '%s\n' 'initialize 未返回 Mcp-Session-Id' >&2
  exit 1
fi

curl -fsS -m 60 -o /dev/null -X POST "$MCP_BASE_URL" \
  -H "Accept: application/json, text/event-stream" \
  -H "Content-Type: application/json" \
  -H "Mcp-Session-Id: $SID" \
  -H "MCP-Protocol-Version: 2025-11-25" \
  -d '{"jsonrpc":"2.0","method":"notifications/initialized"}'

CALL_RESPONSE=$(curl -fsS -m 60 -X POST "$MCP_BASE_URL" \
  -H "Accept: application/json, text/event-stream" \
  -H "Content-Type: application/json" \
  -H "Mcp-Session-Id: $SID" \
  -H "MCP-Protocol-Version: 2025-11-25" \
  -d '{"jsonrpc":"2.0","id":2,"method":"tools/call","params":{"name":"ft_get_fund_net_value_performance","arguments":{"fund_code":"000001","start_date":20260710,"end_date":20260717,"page":1,"page_size":2}}}')

check_mcp_response "$CALL_RESPONSE"
```

**Python（`mcp` SDK，自动握手管理 session）**：

```python
import asyncio

from mcp import ClientSession
from mcp.client.streamable_http import streamable_http_client


MCP_BASE_URL = "<MCP_BASE_URL>"


async def main():
    async with streamable_http_client(MCP_BASE_URL) as (read_stream, write_stream):
        async with ClientSession(read_stream, write_stream) as session:
            await session.initialize()
            result = await session.call_tool(
                'ft_get_fund_net_value_performance',
                {'fund_code': '000001', 'start_date': 20260710, 'end_date': 20260717, 'page': 1, 'page_size': 2},
            )
            if result.is_error:
                raise RuntimeError(result.content[0].text)
            print(result.structured_content)
            print(result.content[0].text)


asyncio.run(main())
```

## 数据样例

| stat_date | daily_return | this_week_return | one_week_return | this_month_return | one_month_return | three_month_return | six_month_return | this_year_return | one_year_return | two_year_return |
|------|------|------|------|------|------|------|------|------|------|------|
| 20260717 | -5.987172927 | -13.6214878997 | -13.6214878997 | -19.4257827884 | -6.2544491511 | 17.662789838 | 10.3765641385 | 21.7913202096 | 48.5578805562 | 74.6822464676 |
| 20260713 | -2.357563277 | -2.357563277 | 1.2220009015 | -8.9187494171 | 15.9409116632 | 37.0404387475 | 28.3132576354 | 37.6731433296 | 72.9487399457 | 96.1739226134 |
| ... | ... | ... | ... | ... | ... | ... | ... | ... | ... | ... |
