# 基金持有人结构（MCP 工具 `ft_get_fund_holder_structure`）

> **MCP 工具**：`ft_get_fund_holder_structure`（category: `公募基金`）。返回统一 MCP 输出：`structuredContent.metadata` + `structuredContent.data`；`content[0].text` 是同值的序列化 JSON，不额外返回 Markdown。输入参数 / 输出参数 / 数据样例见下文。
> 文中 `Response`、`items`、`records`、`code`、`message` 等名称仅为字段说明；MCP 对外固定为上述 `metadata/data`。

- 描述：查询基金持有人结构（按报告期）。`fund_code` 必填，`report_type` 精确匹配报告类型，`start_date`/`end_date` 报告期范围。直接返回数组（非分页），含持有人户数、户均持有份额、机构/个人/未分类/专业机构/ETF 联接/前十大持有人的份额与比例等 18 个字段。提示：`fund_code` 必填（trade_code 形式，纯数字按 `.OF` 主体解析。实时参数约束以 `tools/list` 返回的 `inputSchema` 为准。
- 数据范围：以服务端返回为准
- 单次限量：分页返回，默认 `page=1`、`page_size=50`（部分工具上限 200）

## 输入参数

| 名称 | 类型 | 必选 | 描述 |
|------|------|------|------|
| end_date | integer | N | 报告期截止日期 YYYYMMDD |
| fund_code | string | Y | 基金代码 |
| report_type | string | N | 报告类型：年度报告/中期报告/上市公告书/基金合同生效公告，缺省全部 |
| start_date | integer | N | 报告期起始日期 YYYYMMDD |

未在 `inputSchema` 中声明的字段会被拒绝。

## 输出参数

> MCP 固定输出信封为 `structuredContent.metadata` + `structuredContent.data`；`content[0].text` 是与其同值的序列化 JSON，不是 Markdown。分页与截断信息统一归入 `metadata`。

| MCP 字段 | 类型 | 必填 | 描述 |
|----------|------|------|------|
| metadata | object | Y | 契约版本、数据来源、工具名、业务口径、总量、分页、返回条数、截断状态及 warnings |
| data | array | Y | 归一化后的业务数据项；元素字段见下方 |

### data 业务字段

| 名称 | 类型 | 默认显示 | 描述 |
|------|------|---------|------|
| fund_code | string | Y | 基金代码 |
| report_type | string | Y | 报告类型 |
| publish_date | int | Y | 公告日期 YYYYMMDD |
| report_date | int | Y | 报告期 YYYYMMDD |
| holder_accounts | int | Y | 持有人户数 |
| avg_hold_shares | string | Y | 户均持有份额（份） |
| insti_hold_shares | string | Y | 机构持有份额（份） |
| insti_hold_rt | string | Y | 机构持有比例（%） |
| indiv_hold_shares | string | Y | 个人持有份额（份） |
| indiv_hold_rt | string | Y | 个人持有比例（%） |
| undef_hold_shares | string | Y | 未分类持有份额 |
| undef_hold_rt | string | Y | 未分类持有比例（%） |
| prof_hold_shares | string | Y | 专业机构持有份额 |
| prof_hold_rt | string | Y | 专业机构持有比例（%） |
| etf_connect_shares | string | Y | ETF 联接基金持有份额 |
| etf_connect_rt | string | Y | ETF 联接基金持有比例（%） |
| top10_hold_shares | string | Y | 前十大持有人持有份额 |
| top10_hold_rt | string | Y | 前十大持有人持有比例（%） |

## 调用方法（MCP）

> MCP 工具名 `ft_get_fund_holder_structure`。MCP Streamable HTTP 要求**先 initialize 拿 `Mcp-Session-Id`，发送 `notifications/initialized`，再 `tools/call`**，后续请求同时带该 Session ID 和协商后的 `MCP-Protocol-Version`。返回统一 `metadata/data` 结构化输出；`content[0].text` 为同值 JSON 文本。

**curl**：

```bash
SID=$(curl -sS -m 10 -D - -o /dev/null -X POST <MCP_BASE_URL> \
  -H "Accept: application/json, text/event-stream" -H "Content-Type: application/json" \
  -d '{"jsonrpc":"2.0","id":1,"method":"initialize","params":{"protocolVersion":"2025-11-25","capabilities":{},"clientInfo":{"name":"t","version":"1"}}}' \
  | grep -i 'mcp-session-id:' | awk '{print $2}' | tr -d '\r')

curl -fsS -m 10 -o /dev/null -X POST <MCP_BASE_URL> \
  -H "Accept: application/json, text/event-stream" -H "Content-Type: application/json" \
  -H "Mcp-Session-Id: $SID" -H "MCP-Protocol-Version: 2025-11-25" \
  -d '{"jsonrpc":"2.0","method":"notifications/initialized"}'

curl -sS -m 10 -X POST <MCP_BASE_URL> \
  -H "Accept: application/json, text/event-stream" -H "Content-Type: application/json" \
  -H "Mcp-Session-Id: $SID" -H "MCP-Protocol-Version: 2025-11-25" \
  -d '{"jsonrpc":"2.0","id":2,"method":"tools/call","params":{"name":"ft_get_fund_holder_structure","arguments":{"fund_code": "000001"}}}'
```

**Python（`mcp` SDK，自动握手管理 session）**：

```python
import asyncio
from mcp import ClientSession
from mcp.client.streamable_http import streamablehttp_client

async def main():
    async with streamablehttp_client('<MCP_BASE_URL>') as (r, w, _):
        async with ClientSession(r, w) as s:
            await s.initialize()
            res = await s.call_tool('ft_get_fund_holder_structure', {"fund_code": "000001"})
            print(res.structuredContent)   # 推荐：统一 metadata/data 结构化输出
            print(res.content[0].text)   # 兼容：与 structuredContent 同值的 JSON 文本
asyncio.run(main())
```
