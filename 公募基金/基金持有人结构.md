# 基金持有人结构（MCP 工具 `ft_get_fund_holder_structure`）

> **MCP 工具**：`ft_get_fund_holder_structure`（category: `公募基金`）。返回统一 MCP 输出：`structuredContent.metadata` + `structuredContent.data`；`content[0].text` 是同值的序列化 JSON，不额外返回 Markdown。输入参数 / 输出参数 / 数据样例见下文。
> 文中 `Response`、`items`、`records`、`code`、`message` 等名称仅为字段说明；MCP 对外固定为上述 `metadata/data`。

- 描述：按必填的 `fund_code` 查询基金持有人结构，可通过 `report_type` 精确筛选报告类型，并以 `start_date`/`end_date` 限定报告期。数据包括持有人户数、户均持有份额，以及机构、个人、未分类、专业机构、ETF 联接和前十大持有人的份额与比例。
- 数据范围：按报告期查询
- 单次限量：非分页，返回该基金全部报告期持有人结构
- 提示：
  - `fund_code` 必填（trade_code 形式，纯数字按 `.OF` 主体解析，如 `000001` 或 `000001.OF`）。
  - `report_type` 精确匹配：`年度报告`/`中期报告`/`上市公告书`/`基金合同生效公告`，缺省返回全部。
  - 非分页，直接返回数组。

## 输入参数

| 名称 | 类型 | 必选 | 描述 |
|------|------|------|------|
| fund_code | string | Y | 基金代码 |
| report_type | string | N | 报告类型：年度报告/中期报告/上市公告书/基金合同生效公告，缺省全部 |
| start_date | int | N | 报告期起始日期 YYYYMMDD |
| end_date | int | N | 报告期截止日期 YYYYMMDD |

## 输出参数

> MCP 固定输出信封为 `structuredContent.metadata` + `structuredContent.data`；`content[0].text` 是与其同值的序列化 JSON，不是 Markdown。
>
> `items` / `records` / `code` / `message` 等传输字段不会直接出现在 MCP 结果中；分页与截断信息统一归入 `metadata`。

| MCP 字段 | 类型 | 必填 | 描述 |
|----------|------|------|------|
| metadata | object | Y | 契约版本、数据来源、工具名、业务口径、总量、分页、返回条数、截断状态及 warnings |
| data | array | Y | 归一化后的业务数据项 |

### data 业务字段

| 名称 | 类型 | 默认显示 | 描述 |
|------|------|---------|------|
| avg_hold_shares | string | N | 户均持有份额（份） |
| etf_connect_rt | string | N | ETF 联接基金持有比例（%） |
| etf_connect_shares | string | N | ETF 联接基金持有份额 |
| fund_code | string | Y | 基金代码 |
| holder_accounts | int | N | 持有人户数 |
| indiv_hold_rt | string | N | 个人持有比例（%） |
| indiv_hold_shares | string | N | 个人持有份额（份） |
| insti_hold_rt | string | N | 机构持有比例（%） |
| insti_hold_shares | string | N | 机构持有份额（份） |
| prof_hold_rt | string | N | 专业机构持有比例（%） |
| prof_hold_shares | string | N | 专业机构持有份额 |
| publish_date | int | Y | 公告日期 YYYYMMDD |
| report_date | int | Y | 报告期 YYYYMMDD |
| report_type | string | Y | 报告类型 |
| top10_hold_rt | string | N | 前十大持有人持有比例（%） |
| top10_hold_shares | string | N | 前十大持有人持有份额 |
| undef_hold_rt | string | N | 未分类持有比例（%） |
| undef_hold_shares | string | N | 未分类持有份额 |

## 调用方法（MCP）

> MCP Streamable HTTP 要求**先 initialize 拿 `Mcp-Session-Id`，发送 `notifications/initialized`，再 `tools/call`**，后续请求同时带该 Session ID 和协商后的 `MCP-Protocol-Version`。返回统一 `metadata/data` 结构化输出；`content[0].text` 为同值 JSON 文本，不额外返回 Markdown。

**curl**：

```bash
set -euo pipefail

MCP_BASE_URL="<MCP_BASE_URL>"

check_mcp_response() {
  local response=$1
  printf '%s\n' "$response"
  if printf '%s\n' "$response" | grep -Eq '"isError"[[:space:]]*:[[:space:]]*true|"error"[[:space:]]*:[[:space:]]*\{'; then
    return 1
  fi
}

SID=$(curl -fsS -m 60 -D - -o /dev/null -X POST "$MCP_BASE_URL" \
  -H "Accept: application/json, text/event-stream" \
  -H "Content-Type: application/json" \
  -d '{"jsonrpc":"2.0","id":1,"method":"initialize","params":{"protocolVersion":"2025-11-25","capabilities":{},"clientInfo":{"name":"ftshare-doc-example","version":"1.0.0"}}}' \
  | awk 'tolower($1)=="mcp-session-id:" {print $2}' \
  | tr -d '\r')

if [ -z "$SID" ]; then
  printf '%s\n' 'initialize 未返回 Mcp-Session-Id' >&2
  exit 1
fi

curl -fsS -m 60 -o /dev/null -X POST "$MCP_BASE_URL" \
  -H "Accept: application/json, text/event-stream" \
  -H "Content-Type: application/json" \
  -H "Mcp-Session-Id: $SID" \
  -H "MCP-Protocol-Version: 2025-11-25" \
  -d '{"jsonrpc":"2.0","method":"notifications/initialized"}'

CALL_RESPONSE=$(curl -fsS -m 60 -X POST "$MCP_BASE_URL" \
  -H "Accept: application/json, text/event-stream" \
  -H "Content-Type: application/json" \
  -H "Mcp-Session-Id: $SID" \
  -H "MCP-Protocol-Version: 2025-11-25" \
  -d '{"jsonrpc":"2.0","id":2,"method":"tools/call","params":{"name":"ft_get_fund_holder_structure","arguments":{"fund_code":"000001"}}}')

check_mcp_response "$CALL_RESPONSE"
```

**Python（`mcp` SDK，自动握手管理 session）**：

```python
import asyncio

from mcp import ClientSession
from mcp.client.streamable_http import streamable_http_client


MCP_BASE_URL = "<MCP_BASE_URL>"


async def main():
    async with streamable_http_client(MCP_BASE_URL) as (read_stream, write_stream):
        async with ClientSession(read_stream, write_stream) as session:
            await session.initialize()
            result = await session.call_tool(
                'ft_get_fund_holder_structure',
                {'fund_code': '000001'},
            )
            if result.is_error:
                raise RuntimeError(result.content[0].text)
            print(result.structured_content)
            print(result.content[0].text)


asyncio.run(main())
```

## 数据样例

| report_type | publish_date | report_date | holder_accounts | avg_hold_shares | insti_hold_rt | indiv_hold_rt | prof_hold_rt |
|------|------|------|------|------|------|------|------|
| 年度报告 | 20260331 | 20251231 | 153718 | 17641.7500 | 0.500000 | 99.500000 | 0.000000 |
| 中期报告 | 20210830 | 20210630 | 180556 | 16923.0700 | 0.590000 | 99.410000 | 0.000000 |
| 年度报告 | 20210330 | 20201231 | 187692 | 18864.7700 | 5.310000 | 94.690000 | 0.000000 |
| ... | ... | ... | ... | ... | ... | ... | ... |
