# 基金新发（MCP 工具 `ft_get_fund_new_found`）

> **MCP 工具**：`ft_get_fund_new_found`（category: `公募基金`）。返回统一 MCP 输出：`structuredContent.metadata` + `structuredContent.data`；`content[0].text` 是同值的序列化 JSON，不额外返回 Markdown。输入参数 / 输出参数 / 数据样例见下文。
> 文中 `Response`、`items`、`records`、`code`、`message` 等名称仅为字段说明；MCP 对外固定为上述 `metadata/data`。

- 描述：查询新发基金，可通过 `start_date`/`end_date` 限定成立日范围，未传时默认查询以 `end_date` 为锚点的近一年数据，并可通过 `fund_type` 筛选类型。数据包括基金代码、名称、管理人、托管人、运作方式、类型、成立日期与规模、发行起止日期、业绩基准及最低申购/认购金额，按成立日倒序排列。
- 数据范围：按成立日查询
- 单次限量：默认 `page=1`/`page_size=50`；上限 200
- 提示：
  - `start_date`/`end_date` 不传默认近 1 年（以 `end_date` 为锚点）。
  - `fund_type` 精确匹配：`混合型`/`债券型`/`股票型`/`货币型`/`其他型`/`保本型`/`REITs`。
  - `established_size`/`min_purchase_amount`/`min_subscription_amount` 可能为空。

## 输入参数

| 名称 | 类型 | 必选 | 描述 |
|------|------|------|------|
| start_date | int | N | 成立日起始日期 YYYYMMDD（不传默认近 1 年） |
| end_date | int | N | 成立日截止日期 YYYYMMDD（不传默认今天） |
| fund_type | string | N | 基金类型过滤：混合型/债券型/股票型/货币型/其他型/保本型/REITs |
| page | int | N | 页码，默认 1 |
| page_size | int | N | 每页条数，默认 50，上限 200 |

## 输出参数

> MCP 固定输出信封为 `structuredContent.metadata` + `structuredContent.data`；`content[0].text` 是与其同值的序列化 JSON，不是 Markdown。
>
> `items` / `records` / `code` / `message` 等传输字段不会直接出现在 MCP 结果中；分页与截断信息统一归入 `metadata`。

| MCP 字段 | 类型 | 必填 | 描述 |
|----------|------|------|------|
| metadata | object | Y | 契约版本、数据来源、工具名、业务口径、总量、分页、返回条数、截断状态及 warnings |
| data | array | Y | 归一化后的业务数据项 |

### data 业务字段

| 名称 | 类型 | 默认显示 | 描述 |
|------|------|---------|------|
| benchmark | string | Y | 业绩比较基准 |
| custodian | string | Y | 托管人 |
| end_date | int | Y | 到期/封闭期结束日期 |
| established_date | int | Y | 成立日期 YYYYMMDD |
| established_size | string | N | 成立规模（元，可能空） |
| fund_code | string | Y | 基金代码（带后缀） |
| fund_company | string | Y | 基金管理人 |
| fund_name | string | Y | 基金名称 |
| fund_type | string | Y | 基金类型 |
| min_purchase_amount | string | N | 最低申购金额 |
| min_subscription_amount | string | N | 最低认购金额 |
| operation_mode | string | Y | 运作方式 |
| start_date | int | Y | 发行开始日期 |

## 调用方法（MCP）

> MCP Streamable HTTP 要求**先 initialize 拿 `Mcp-Session-Id`，发送 `notifications/initialized`，再 `tools/call`**，后续请求同时带该 Session ID 和协商后的 `MCP-Protocol-Version`。返回统一 `metadata/data` 结构化输出；`content[0].text` 为同值 JSON 文本，不额外返回 Markdown。

**curl**：

```bash
set -euo pipefail

MCP_BASE_URL="<MCP_BASE_URL>"

check_mcp_response() {
  local response=$1
  printf '%s\n' "$response"
  if printf '%s\n' "$response" | grep -Eq '"isError"[[:space:]]*:[[:space:]]*true|"error"[[:space:]]*:[[:space:]]*\{'; then
    return 1
  fi
}

SID=$(curl -fsS -m 60 -D - -o /dev/null -X POST "$MCP_BASE_URL" \
  -H "Accept: application/json, text/event-stream" \
  -H "Content-Type: application/json" \
  -d '{"jsonrpc":"2.0","id":1,"method":"initialize","params":{"protocolVersion":"2025-11-25","capabilities":{},"clientInfo":{"name":"ftshare-doc-example","version":"1.0.0"}}}' \
  | awk 'tolower($1)=="mcp-session-id:" {print $2}' \
  | tr -d '\r')

if [ -z "$SID" ]; then
  printf '%s\n' 'initialize 未返回 Mcp-Session-Id' >&2
  exit 1
fi

curl -fsS -m 60 -o /dev/null -X POST "$MCP_BASE_URL" \
  -H "Accept: application/json, text/event-stream" \
  -H "Content-Type: application/json" \
  -H "Mcp-Session-Id: $SID" \
  -H "MCP-Protocol-Version: 2025-11-25" \
  -d '{"jsonrpc":"2.0","method":"notifications/initialized"}'

CALL_RESPONSE=$(curl -fsS -m 60 -X POST "$MCP_BASE_URL" \
  -H "Accept: application/json, text/event-stream" \
  -H "Content-Type: application/json" \
  -H "Mcp-Session-Id: $SID" \
  -H "MCP-Protocol-Version: 2025-11-25" \
  -d '{"jsonrpc":"2.0","id":2,"method":"tools/call","params":{"name":"ft_get_fund_new_found","arguments":{"page":1,"page_size":2}}}')

check_mcp_response "$CALL_RESPONSE"
```

**Python（`mcp` SDK，自动握手管理 session）**：

```python
import asyncio

from mcp import ClientSession
from mcp.client.streamable_http import streamable_http_client


MCP_BASE_URL = "<MCP_BASE_URL>"


async def main():
    async with streamable_http_client(MCP_BASE_URL) as (read_stream, write_stream):
        async with ClientSession(read_stream, write_stream) as session:
            await session.initialize()
            result = await session.call_tool(
                'ft_get_fund_new_found',
                {'page': 1, 'page_size': 2},
            )
            if result.is_error:
                raise RuntimeError(result.content[0].text)
            print(result.structured_content)
            print(result.content[0].text)


asyncio.run(main())
```

## 数据样例

| fund_code | fund_name | fund_company | fund_type | established_date | operation_mode | benchmark |
|------|------|------|------|------|------|------|
| 028637.OF | 银华科创主题灵活配置混合(LOF)C | 银华基金 | 混合型 | 20260807 | 开放式 | 中国战略新兴产业成份指数收益率×40%+恒生指数收益率（使用估值汇率调整）×10%+中债综合财富指数收益率×50% |
| 028405.OF | 富国中证金融科技主题ETF发起式联接C | 富国基金 | 股票型 | 20260806 | ETF联接基金 | 中证金融科技主题指数收益率*95%+活期存款基准利率*5% |
| ... | ... | ... | ... | ... | ... | ... |
