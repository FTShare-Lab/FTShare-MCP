# 研报列表（MCP 工具 `ft_stock_reports`）

> **MCP 工具**：`ft_stock_reports`（category: `大模型语料`）。返回统一 MCP 输出：`structuredContent.metadata` + `structuredContent.data`；`content[0].text` 是同值的序列化 JSON，不额外返回 Markdown。输入参数 / 输出参数 / 数据样例见下文。
> 文中 `Response`、`items`、`records`、`code`、`message` 等名称仅为字段说明；MCP 对外固定为上述 `metadata/data`。

- 描述：查询 A 股研报，必须在证券代码与开始日期中选择一种查询方式；按证券代码查询单只股票的全部研报，按开始日期查询全市场研报时可指定结束日期且范围不超过 3 天，研报信息包含本次及上次评级、评级变动、研究员和机构。
- 数据范围：按报告期查询
- 单次限量：分页返回，`page` / `page_size` 必填；按日期范围查询时区间跨度不能超过 3 天
- 提示：
  - 二选一：必须提供 `stock_code`（按标的查）或 `start_date`（按日期范围查），否则报错。
  - `type` 当前只支持 `"stock"`。
  - 按日期范围查询时，`start_date` / `end_date` 格式 `YYYYMMDD`，区间不能超过 3 天；`end_date` 不填默认当前时间。
  - `report_institution` 字段序列化名为 `report_Institution`（大写 I），消费方注意大小写。
  - `rating_change` 为中文：维持 / 上调 / 下调 / 首次覆盖。
  - 研报正文文件下载请用 `/api/v1/market/data/report/stock-reports/{url_hash}`（另见独立接口）。

## 输入参数

| 名称 | 类型 | 必选 | 描述 |
|------|------|------|------|
| stock_code | string | N | 证券代码（按标的查询时必填） |
| start_date | string | N | 开始日期 YYYYMMDD（按日期范围查询时必填） |
| type | string | Y | 查询类型，当前只支持 `stock` |
| page | int | Y | 页码 |
| page_size | int | Y | 每页条数 |

## 输出参数

> MCP 固定输出信封为 `structuredContent.metadata` + `structuredContent.data`；`content[0].text` 是与其同值的序列化 JSON，不是 Markdown。
>
> `items` / `records` / `code` / `message` 等传输字段不会直接出现在 MCP 结果中；分页与截断信息统一归入 `metadata`。

| MCP 字段 | 类型 | 必填 | 描述 |
|----------|------|------|------|
| metadata | object | Y | 契约版本、数据来源、工具名、业务口径、总量、分页、返回条数、截断状态及 warnings |
| data | array | Y | 归一化后的业务数据项 |

### data 业务字段

| 名称 | 类型 | 默认显示 | 描述 |
|------|------|---------|------|
| last_report_rating_name | string | Y | 上次评级名称 |
| last_report_rating_value | string | Y | 上次评级等级 |
| rating_change | string | Y | 评级变动中文：维持 / 上调 / 下调 / 首次覆盖 |
| rating_researcher | string | Y | 研究员 |
| report_Institution | string | Y | 研究机构（序列化名 `report_Institution`） |
| report_id | string | Y | 研报 id |
| report_rating_name | string | Y | 本次评级名称 |
| report_rating_value | string | Y | 本次评级等级 |
| report_time | string | Y | 发布时间 YYYYMMDD |
| report_title | string | Y | 研报标题 |
| stock_code | string | Y | 股票代码 |
| stock_name | string | Y | 股票名称 |
| url_hash | string | Y | 研报文件 URL 哈希，用于下载正文文件 |

## 调用方法（MCP）

> MCP Streamable HTTP 要求**先 initialize 拿 `Mcp-Session-Id`，发送 `notifications/initialized`，再 `tools/call`**，后续请求同时带该 Session ID 和协商后的 `MCP-Protocol-Version`。返回统一 `metadata/data` 结构化输出；`content[0].text` 为同值 JSON 文本，不额外返回 Markdown。

**curl**：

```bash
set -euo pipefail

MCP_BASE_URL="<MCP_BASE_URL>"

check_mcp_response() {
  local response=$1
  printf '%s\n' "$response"
  if printf '%s\n' "$response" | grep -Eq '"isError"[[:space:]]*:[[:space:]]*true|"error"[[:space:]]*:[[:space:]]*\{'; then
    return 1
  fi
}

SID=$(curl -fsS -m 60 -D - -o /dev/null -X POST "$MCP_BASE_URL" \
  -H "Accept: application/json, text/event-stream" \
  -H "Content-Type: application/json" \
  -d '{"jsonrpc":"2.0","id":1,"method":"initialize","params":{"protocolVersion":"2025-11-25","capabilities":{},"clientInfo":{"name":"ftshare-doc-example","version":"1.0.0"}}}' \
  | awk 'tolower($1)=="mcp-session-id:" {print $2}' \
  | tr -d '\r')

if [ -z "$SID" ]; then
  printf '%s\n' 'initialize 未返回 Mcp-Session-Id' >&2
  exit 1
fi

curl -fsS -m 60 -o /dev/null -X POST "$MCP_BASE_URL" \
  -H "Accept: application/json, text/event-stream" \
  -H "Content-Type: application/json" \
  -H "Mcp-Session-Id: $SID" \
  -H "MCP-Protocol-Version: 2025-11-25" \
  -d '{"jsonrpc":"2.0","method":"notifications/initialized"}'

CALL_RESPONSE=$(curl -fsS -m 60 -X POST "$MCP_BASE_URL" \
  -H "Accept: application/json, text/event-stream" \
  -H "Content-Type: application/json" \
  -H "Mcp-Session-Id: $SID" \
  -H "MCP-Protocol-Version: 2025-11-25" \
  -d '{"jsonrpc":"2.0","id":2,"method":"tools/call","params":{"name":"ft_stock_reports","arguments":{"stock_code":"600519.SH","type":"stock","page":1,"page_size":2}}}')

check_mcp_response "$CALL_RESPONSE"
```

**Python（`mcp` SDK，自动握手管理 session）**：

```python
import asyncio

from mcp import ClientSession
from mcp.client.streamable_http import streamable_http_client


MCP_BASE_URL = "<MCP_BASE_URL>"


async def main():
    async with streamable_http_client(MCP_BASE_URL) as (read_stream, write_stream):
        async with ClientSession(read_stream, write_stream) as session:
            await session.initialize()
            result = await session.call_tool(
                'ft_stock_reports',
                {'stock_code': '600519.SH', 'type': 'stock', 'page': 1, 'page_size': 2},
            )
            if result.is_error:
                raise RuntimeError(result.content[0].text)
            print(result.structured_content)
            print(result.content[0].text)


asyncio.run(main())
```

## 数据样例

| stock_code | stock_name | report_id | report_title | report_time | report_institution | report_rating_name | report_rating_value | last_report_rating_name | last_report_rating_value | rating_change | rating_researcher | url_hash |
|------|------|------|------|------|------|------|------|------|------|------|------|------|
| 600519.SH | 贵州茅台 | AP202607231827290069 | 需求根基稳固，市场化定价持续兑现 | 20260723 | null | 买入 | 3 | 买入 | 3 | 首次覆盖 | 蔡雪昱,张子健 | 0dddf48e2ad56a92235c40ddd11e69cb5e0f053e8daee8071fedd2ec6919… |
| 600519.SH | 贵州茅台 | AP202607201827134022 | 飞天茅台年内二次提价，进一步落实市场化机制 | 20260720 | null | 持有 | 1 | 持有 | 1 | 首次覆盖 | 顾向君 | b5081474ba8c603aa88dd583898f9802900211b2e4e64760093b5088f68b… |
| ... | ... | ... | ... | ... | ... | ... | ... | ... | ... | ... | ... | ... |
