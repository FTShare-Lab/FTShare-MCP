# 研报列表（MCP 工具 `ft_stock_reports`）

> **MCP 工具**：`ft_stock_reports`（category: `大模型语料`）。返回统一 MCP 输出：`structuredContent.metadata` + `structuredContent.data`；`content[0].text` 是同值的序列化 JSON，不额外返回 Markdown。输入参数 / 输出参数 / 数据样例见下文。
> 文中 `Response`、`items`、`records`、`code`、`message` 等名称仅为字段说明；MCP 对外固定为上述 `metadata/data`。

- 描述：查询 A 股研报列表。支持按 `stock_code` 查询单票历史研报，或按 `start_date` 查询指定单日的全市场研报；每条包含评级、研究员和机构等信息。提示：必须提供 `stock_code` 或 `start_date` 之一，否则报错；`type` 当前只支持 `stock`；`start_date` 格式 YYYYMMDD，仅取该日研报。
- 数据范围：以服务端返回为准
- 单次限量：分页返回，`page` / `page_size` 必填；日期模式仅支持单日
- 提示：
  - 二选一：必须提供 `stock_code`（按标的查）或 `start_date`（按日期范围查），否则报错。
  - `type` 当前只支持 `"stock"`。
  - MCP 日期模式只公开 `start_date`，格式 `YYYYMMDD`，用于查询指定单日。
  - `report_institution` 字段序列化名为 `report_Institution`（大写 I），消费方注意大小写。
  - `rating_change` 为中文：维持 / 上调 / 下调 / 首次覆盖。
  - 研报正文文件下载请用 `/api/v1/market/data/report/stock-reports/{url_hash}`（另见独立接口）。

## 输入参数

| 名称 | 类型 | 必选 | 描述 |
|------|------|------|------|
| stock_code | string | N | 证券代码（按标的查询时必填） |
| start_date | string | N | 研报日期 YYYYMMDD（按日期查询时必填） |
| type | string | Y | 查询类型，当前只支持 `stock` |
| page | int | Y | 页码 |
| page_size | int | Y | 每页条数 |

## 输出参数

> MCP 固定输出信封为 `structuredContent.metadata` + `structuredContent.data`；`content[0].text` 是与其同值的序列化 JSON，不是 Markdown。
>
> `items` / `records` / `code` / `message` 等传输字段不会直接出现在 MCP 结果中；分页与截断信息统一归入 `metadata`。

| MCP 字段 | 类型 | 必填 | 描述 |
|----------|------|------|------|
| metadata | object | Y | 契约版本、数据来源、工具名、业务口径、总量、分页、返回条数、截断状态及 warnings |
| data | array | Y | 归一化后的业务数据项；元素字段见下方 |

### data 业务字段

StockReportItem：

| 名称 | 类型 | 默认显示 | 描述 |
|------|------|---------|------|
| stock_code | string | Y | 股票代码 |
| stock_name | string | Y | 股票名称 |
| report_id | string | Y | 研报 id |
| report_title | string | Y | 研报标题 |
| report_time | string | Y | 发布时间 YYYYMMDD |
| report_institution | string | Y | 研究机构（序列化名 `report_Institution`） |
| report_rating_name | string | Y | 本次评级名称 |
| report_rating_value | string | Y | 本次评级等级 |
| last_report_rating_name | string | Y | 上次评级名称 |
| last_report_rating_value | string | Y | 上次评级等级 |
| rating_change | string | Y | 评级变动中文：维持 / 上调 / 下调 / 首次覆盖 |
| rating_researcher | string | Y | 研究员 |
| url_hash | string | Y | 研报文件 URL 哈希，用于下载正文文件 |

## 调用方法（MCP）

> MCP 工具名 `ft_stock_reports`。MCP Streamable HTTP 要求**先 initialize 拿 `Mcp-Session-Id`，发送 `notifications/initialized`，再 `tools/call`**，后续请求同时带该 Session ID 和协商后的 `MCP-Protocol-Version`。返回统一 `metadata/data` 结构化输出；`content[0].text` 为同值 JSON 文本，不额外返回 Markdown。

**curl**：

```bash
SID=$(curl -sS -m 10 -D - -o /dev/null -X POST <MCP_BASE_URL> \
  -H "Accept: application/json, text/event-stream" -H "Content-Type: application/json" \
  -d '{"jsonrpc":"2.0","id":1,"method":"initialize","params":{"protocolVersion":"2025-11-25","capabilities":{},"clientInfo":{"name":"t","version":"1"}}}' \
  | grep -i 'mcp-session-id:' | awk '{print $2}' | tr -d '\r')

curl -fsS -m 10 -o /dev/null -X POST <MCP_BASE_URL> \
  -H "Accept: application/json, text/event-stream" -H "Content-Type: application/json" \
  -H "Mcp-Session-Id: $SID" \
  -H "MCP-Protocol-Version: 2025-11-25" \
  -d '{"jsonrpc":"2.0","method":"notifications/initialized"}'

curl -sS -m 10 -X POST <MCP_BASE_URL> \
  -H "Accept: application/json, text/event-stream" -H "Content-Type: application/json" \
  -H "Mcp-Session-Id: $SID" \
  -H "MCP-Protocol-Version: 2025-11-25" \
  -d '{"jsonrpc":"2.0","id":2,"method":"tools/call","params":{"name":"ft_stock_reports","arguments":{"type": "stock", "stock_code": "600519.SH", "page": 1, "page_size": 3}}}'
```

**Python（`mcp` SDK，自动握手管理 session）**：

```python
import asyncio
from mcp import ClientSession
from mcp.client.streamable_http import streamablehttp_client

async def main():
    async with streamablehttp_client('<MCP_BASE_URL>') as (r, w, _):
        async with ClientSession(r, w) as s:
            await s.initialize()
            res = await s.call_tool('ft_stock_reports', {"type": "stock", "stock_code": "600519.SH", "page": 1, "page_size": 3})
            print(res.structuredContent)   # 推荐：统一 metadata/data 结构化输出
            print(res.content[0].text)   # 兼容：与 structuredContent 同值的 JSON 文本
asyncio.run(main())
```

## 数据样例

| stock_code | stock_name | report_id | report_title | report_time | report_institution | report_rating_name | report_rating_value | last_report_rating_name | last_report_rating_value | rating_change | rating_researcher | url_hash |
|------------|------------|-----------|--------------|-------------|--------------------|--------------------|---------------------|-------------------------|--------------------------|---------------|-------------------|----------|
| ... | ... | ... | ... | ... | ... | ... | ... | ... | ... | ... | ... | ... |

> 示例调用已验证通过，真实样例可从 `structuredContent.data` 查看。
