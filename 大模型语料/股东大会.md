# 股东大会（MCP 工具 `ft_shareholders_meeting`）

> **MCP 工具**：`ft_shareholders_meeting`（category: `大模型语料`）。返回统一 MCP 输出：`structuredContent.metadata` + `structuredContent.data`；`content[0].text` 是同值的序列化 JSON，不额外返回 Markdown。输入参数 / 输出参数 / 数据样例见下文。
> 文中 `Response`、`items`、`records`、`code`、`message` 等名称仅为字段说明；MCP 对外固定为上述 `metadata/data`。

- 描述：查询 A 股上市公司股东大会通知信息，返回每场股东大会的会议名称、股权登记日、现场/网络投票起止日、决议公告日、通知日期及审议议案等。数据来源于东方财富（eastmoney）股东大会公告。提示：无请求过滤参数，全量分页返回所有股东大会通知；多数字段可为空，缺失字段返回 null；`start_adjust_date` 为开始调整日期，`equity_record_date` 为股权登记日。
- 数据范围：以服务端返回为准
- 单次限量：分页返回，默认 `page=1`，`page_size` 由后端定
- 提示：
  - 无请求过滤参数，全量分页返回所有股东大会通知。
  - 多数字段为 `Option<String>`，缺失字段返回 null。
  - `start_adjust_date` 为开始调整日期，`equity_record_date` 为股权登记日。

## 输入参数

| 名称 | 类型 | 必选 | 描述 |
|------|------|------|------|
| page | int | N | 页码，默认 1 |
| page_size | int | N | 每页条数 |

## 输出参数

> MCP 固定输出信封为 `structuredContent.metadata` + `structuredContent.data`；`content[0].text` 是与其同值的序列化 JSON，不是 Markdown。
>
> `items` / `records` / `code` / `message` 等传输字段不会直接出现在 MCP 结果中；分页与截断信息统一归入 `metadata`。

| MCP 字段 | 类型 | 必填 | 描述 |
|----------|------|------|------|
| metadata | object | Y | 契约版本、数据来源、工具名、业务口径、总量、分页、返回条数、截断状态及 warnings |
| data | array | Y | 归一化后的业务数据项；元素字段见下方 |

### data 业务字段

ShareholdersMeetingItem：

| 名称 | 类型 | 默认显示 | 描述 |
|------|------|---------|------|
| security_code | string | Y | 证券代码 |
| security_name_abbr | string | Y | 证券简称 |
| meeting_title | string | Y | 会议名称（如「2026年第2次临时股东大会」） |
| start_adjust_date | string | Y | 开始调整日期 |
| equity_record_date | string | Y | 股权登记日 |
| onsite_record_date | string | Y | 现场登记日 |
| web_start_date | string | Y | 网络投票开始日 |
| web_end_date | string | Y | 网络投票结束日 |
| decision_notice_date | string | Y | 决议公告日 |
| notice_date | string | Y | 通知日期 |
| serial_num | string | Y | 序号 |
| proposal | string | Y | 审议议案 |

## 调用方法（MCP）

> MCP 工具名 `ft_shareholders_meeting`。MCP Streamable HTTP 要求**先 initialize 拿 `Mcp-Session-Id`，发送 `notifications/initialized`，再 `tools/call`**，后续请求同时带该 Session ID 和协商后的 `MCP-Protocol-Version`。返回统一 `metadata/data` 结构化输出；`content[0].text` 为同值 JSON 文本，不额外返回 Markdown。

**curl**：

```bash
SID=$(curl -sS -m 10 -D - -o /dev/null -X POST <MCP_BASE_URL> \
  -H "Accept: application/json, text/event-stream" -H "Content-Type: application/json" \
  -d '{"jsonrpc":"2.0","id":1,"method":"initialize","params":{"protocolVersion":"2025-11-25","capabilities":{},"clientInfo":{"name":"t","version":"1"}}}' \
  | grep -i 'mcp-session-id:' | awk '{print $2}' | tr -d '\r')

curl -fsS -m 10 -o /dev/null -X POST <MCP_BASE_URL> \
  -H "Accept: application/json, text/event-stream" -H "Content-Type: application/json" \
  -H "Mcp-Session-Id: $SID" \
  -H "MCP-Protocol-Version: 2025-11-25" \
  -d '{"jsonrpc":"2.0","method":"notifications/initialized"}'

curl -sS -m 10 -X POST <MCP_BASE_URL> \
  -H "Accept: application/json, text/event-stream" -H "Content-Type: application/json" \
  -H "Mcp-Session-Id: $SID" \
  -H "MCP-Protocol-Version: 2025-11-25" \
  -d '{"jsonrpc":"2.0","id":2,"method":"tools/call","params":{"name":"ft_shareholders_meeting","arguments":{}}}'
```

**Python（`mcp` SDK，自动握手管理 session）**：

```python
import asyncio
from mcp import ClientSession
from mcp.client.streamable_http import streamablehttp_client

async def main():
    async with streamablehttp_client('<MCP_BASE_URL>') as (r, w, _):
        async with ClientSession(r, w) as s:
            await s.initialize()
            res = await s.call_tool('ft_shareholders_meeting', {})
            print(res.structuredContent)   # 推荐：统一 metadata/data 结构化输出
            print(res.content[0].text)   # 兼容：与 structuredContent 同值的 JSON 文本
asyncio.run(main())
```

## 数据样例

股东大会通知共 1269 条，节选：

| security_code | security_name_abbr | meeting_title |
|---------------|-------------------|---------------|
| 301217 | 铜冠铜箔 | 2026年第2次临时股东大会 |
| ... | ... | ... |
