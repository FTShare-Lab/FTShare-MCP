# 华尔街见闻财经日历（MCP 工具 `ft_wallstreetcn_financial_calendar`）

> **MCP 工具**：`ft_wallstreetcn_financial_calendar`（category: `宏观经济/国内宏观`）。返回统一 MCP 输出：`structuredContent.metadata` + `structuredContent.data`；`content[0].text` 是同值的序列化 JSON，不额外返回 Markdown。输入参数 / 输出参数 / 数据样例见下文。
> 文中 `Response`、`items`、`records`、`code`、`message` 等名称仅为字段说明；MCP 对外固定为上述 `metadata/data`。

- 描述：查询华尔街见闻财经日历，按日期范围返回经济事件、重要性、实际值、预测值、前值和原文链接。提示：`start_date` 和 `end_date` 必填。
- 数据范围：按日期范围查询
- 单次限量：分页返回，`page_size` 默认 50、最大 200；`page` 默认 1
- 提示：
  - `start_date` / `end_date` 必填，结束日期不能早于开始日期。
  - 数据来源：华尔街见闻（路径中保留 `wallstreetcn` 来源标识）。
  - 响应为 code/message/data 信封，data 含 records 数组与分页字段。

## 输入参数

| 名称 | 类型 | 必选 | 描述 |
|------|------|------|------|
| start_date | string | Y | 起始日期 |
| end_date | string | Y | 结束日期 |
| page | int | N | 页码，从 1 开始，默认 1 |
| page_size | int | N | 每页条数，默认 50，最大 200 |

## 输出参数

> MCP 固定输出信封为 `structuredContent.metadata` + `structuredContent.data`；`content[0].text` 是与其同值的序列化 JSON，不是 Markdown。
>
> `items` / `records` / `code` / `message` 等传输字段不会直接出现在 MCP 结果中；分页与截断信息统一归入 `metadata`。

| MCP 字段 | 类型 | 必填 | 描述 |
|----------|------|------|------|
| metadata | object | Y | 契约版本、数据来源、工具名、业务口径、总量、分页、返回条数、截断状态及 warnings |
| data | array | Y | 归一化后的业务数据项；元素字段见下方 |

### data 业务字段

WallstreetcnCalendarItem：

| 名称 | 类型 | 默认显示 | 描述 |
|------|------|---------|------|
| stat_date | string | Y | 事件日期 |
| tab_type | string | Y | 事件分类标签 |
| event_time | string | Y | 事件时间 |
| region | string | Y | 地区 |
| event | string | Y | 事件名称 |
| importance | string | Y | 重要性 |
| actual_value | string | N | 实际值 |
| forecast_value | string | N | 预测值 |
| previous_value | string | N | 前值 |
| link | string | N | 原文链接 |

## 调用方法（MCP）

> MCP 工具名 `ft_wallstreetcn_financial_calendar`。MCP Streamable HTTP 要求**先 initialize 拿 `Mcp-Session-Id`，发送 `notifications/initialized`，再 `tools/call`**，后续请求同时带该 Session ID 和协商后的 `MCP-Protocol-Version`。返回统一 `metadata/data` 结构化输出；`content[0].text` 为同值 JSON 文本，不额外返回 Markdown。

**curl**：

```bash
SID=$(curl -sS -m 10 -D - -o /dev/null -X POST <MCP_BASE_URL> \
  -H "Accept: application/json, text/event-stream" -H "Content-Type: application/json" \
  -d '{"jsonrpc":"2.0","id":1,"method":"initialize","params":{"protocolVersion":"2025-11-25","capabilities":{},"clientInfo":{"name":"t","version":"1"}}}' \
  | grep -i 'mcp-session-id:' | awk '{print $2}' | tr -d '\r')

curl -fsS -m 10 -o /dev/null -X POST <MCP_BASE_URL> \
  -H "Accept: application/json, text/event-stream" -H "Content-Type: application/json" \
  -H "Mcp-Session-Id: $SID" \
  -H "MCP-Protocol-Version: 2025-11-25" \
  -d '{"jsonrpc":"2.0","method":"notifications/initialized"}'

curl -sS -m 10 -X POST <MCP_BASE_URL> \
  -H "Accept: application/json, text/event-stream" -H "Content-Type: application/json" \
  -H "Mcp-Session-Id: $SID" \
  -H "MCP-Protocol-Version: 2025-11-25" \
  -d '{"jsonrpc":"2.0","id":2,"method":"tools/call","params":{"name":"ft_wallstreetcn_financial_calendar","arguments":{"start_date": "2026-06-20", "end_date": "2026-06-23"}}}'
```

**Python（`mcp` SDK，自动握手管理 session）**：

```python
import asyncio
from mcp import ClientSession
from mcp.client.streamable_http import streamablehttp_client

async def main():
    async with streamablehttp_client('<MCP_BASE_URL>') as (r, w, _):
        async with ClientSession(r, w) as s:
            await s.initialize()
            res = await s.call_tool('ft_wallstreetcn_financial_calendar', {"start_date": "2026-06-20", "end_date": "2026-06-23"})
            print(res.structuredContent)   # 推荐：统一 metadata/data 结构化输出
            print(res.content[0].text)   # 兼容：与 structuredContent 同值的 JSON 文本
asyncio.run(main())
```

## 数据样例

响应为 code/message/data 信封，data.records 为事件列表（start_date=2026-06-17&end_date=2026-06-20 共 106 条）。节选核心字段：

| stat_date | region | event | importance | tab_type |
|-----------|--------|-------|------------|----------|
| 2026-06-17 | 中国 | 2026陆家嘴论坛将于6月17日至18日召开 | 3 | general |
| ... | ... | ... | ... | ... |

> 注：start_date / end_date 使用 YYYY-MM-DD 格式，结束日期不能早于开始日期。
