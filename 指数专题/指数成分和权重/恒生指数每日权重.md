# 恒生指数每日权重（MCP 工具 `ft_hsi_daily_weight`）

> **MCP 工具**：`ft_hsi_daily_weight`（category: `指数专题/指数成分和权重`）。返回统一 MCP 输出：`structuredContent.metadata` + `structuredContent.data`；`content[0].text` 是同值的序列化 JSON，不额外返回 Markdown。输入参数 / 输出参数 / 数据样例见下文。
> 文中 `Response`、`items`、`records`、`code`、`message` 等名称仅为字段说明；MCP 对外固定为上述 `metadata/data`。

- 描述：查询恒生系列指数的每日成分股权重，支持按交易日或日期范围，并与指数代码、股票代码组合筛选；至少需要一个筛选条件，精确交易日与日期范围不能同时使用。
- 数据范围：已采集的恒生指数权重文件
- 单次限量：默认 `page=1`、`page_size=50`，`page_size` 最大 200
- 提示：
  - 至少需要提供一个业务过滤条件，不能无条件查询全表。
  - `trade_date` 与 `start_date`/`end_date` 互斥。
  - `start_date` 和 `end_date` 必须同时提供，且起始日期不大于结束日期。
  - 日期条件可与 `index_slug`、`stock_code` 组合使用。

## 输入参数

| 名称 | 类型 | 必选 | 描述 |
|------|------|------|------|
| trade_date | int | N | 精确交易日，`YYYYMMDD` |
| start_date | int | N | 起始交易日，`YYYYMMDD` |
| end_date | int | N | 结束交易日，`YYYYMMDD` |
| index_slug | string | N | 指数代码，如 `HSI`、`HSCEI` |
| stock_code | string | N | 成分股代码 |
| page | int | N | 页码，默认 1 |
| page_size | int | N | 每页条数，默认 50，最大 200 |

## 输出参数

> MCP 固定输出信封为 `structuredContent.metadata` + `structuredContent.data`；`content[0].text` 是与其同值的序列化 JSON，不是 Markdown。
>
> `items` / `records` / `code` / `message` 等传输字段不会直接出现在 MCP 结果中；分页与截断信息统一归入 `metadata`。

| MCP 字段 | 类型 | 必填 | 描述 |
|----------|------|------|------|
| metadata | object | Y | 契约版本、数据来源、工具名、业务口径、总量、分页、返回条数、截断状态及 warnings |
| data | array | Y | 归一化后的业务数据项 |

### data 业务字段

| 名称 | 类型 | 默认显示 | 描述 |
|------|------|---------|------|
| trade_date | string/null | Y | 交易日，`YYYY-MM-DD` |
| index_slug | string | Y | 指数代码，如 `HSI`、`HSCEI` |
| index_name | string/null | N | 指数名称 |
| stock_code | string | Y | 成分股代码 |
| stock_name | string/null | N | 成分股名称 |
| weight_pct | string/null | Y | 权重百分比 |
| source_file | string/null | N | 来源文件 |
| url_hash | string/null | N | 来源文件哈希 |
| processed_at | string/null | N | 处理时间 |

## 调用方法（MCP）

> MCP Streamable HTTP 要求**先 initialize 拿 `Mcp-Session-Id`，发送 `notifications/initialized`，再 `tools/call`**，后续请求同时带该 Session ID 和协商后的 `MCP-Protocol-Version`。返回统一 `metadata/data` 结构化输出；`content[0].text` 为同值 JSON 文本，不额外返回 Markdown。

**curl**：

```bash
set -euo pipefail

MCP_BASE_URL="<MCP_BASE_URL>"

check_mcp_response() {
  local response=$1
  printf '%s\n' "$response"
  if printf '%s\n' "$response" | grep -Eq '"isError"[[:space:]]*:[[:space:]]*true|"error"[[:space:]]*:[[:space:]]*\{'; then
    return 1
  fi
}

SID=$(curl -fsS -m 60 -D - -o /dev/null -X POST "$MCP_BASE_URL" \
  -H "Accept: application/json, text/event-stream" \
  -H "Content-Type: application/json" \
  -d '{"jsonrpc":"2.0","id":1,"method":"initialize","params":{"protocolVersion":"2025-11-25","capabilities":{},"clientInfo":{"name":"ftshare-doc-example","version":"1.0.0"}}}' \
  | awk 'tolower($1)=="mcp-session-id:" {print $2}' \
  | tr -d '\r')

if [ -z "$SID" ]; then
  printf '%s\n' 'initialize 未返回 Mcp-Session-Id' >&2
  exit 1
fi

curl -fsS -m 60 -o /dev/null -X POST "$MCP_BASE_URL" \
  -H "Accept: application/json, text/event-stream" \
  -H "Content-Type: application/json" \
  -H "Mcp-Session-Id: $SID" \
  -H "MCP-Protocol-Version: 2025-11-25" \
  -d '{"jsonrpc":"2.0","method":"notifications/initialized"}'

CALL_RESPONSE=$(curl -fsS -m 60 -X POST "$MCP_BASE_URL" \
  -H "Accept: application/json, text/event-stream" \
  -H "Content-Type: application/json" \
  -H "Mcp-Session-Id: $SID" \
  -H "MCP-Protocol-Version: 2025-11-25" \
  -d '{"jsonrpc":"2.0","id":2,"method":"tools/call","params":{"name":"ft_hsi_daily_weight","arguments":{"index_slug":"HSI","start_date":20260701,"end_date":20260731,"page":1,"page_size":5}}}')

check_mcp_response "$CALL_RESPONSE"
```

**Python（`mcp` SDK，自动握手管理 session）**：

```python
import asyncio

from mcp import ClientSession
from mcp.client.streamable_http import streamable_http_client


MCP_BASE_URL = "<MCP_BASE_URL>"


async def main():
    async with streamable_http_client(MCP_BASE_URL) as (read_stream, write_stream):
        async with ClientSession(read_stream, write_stream) as session:
            await session.initialize()
            result = await session.call_tool(
                'ft_hsi_daily_weight',
                {'index_slug': 'HSI', 'start_date': 20260701, 'end_date': 20260731, 'page': 1, 'page_size': 5},
            )
            if result.is_error:
                raise RuntimeError(result.content[0].text)
            print(result.structured_content)
            print(result.content[0].text)


asyncio.run(main())
```

## 数据样例

`index_slug=HSI, start_date=20260701, end_date=20260731, page=1, page_size=5` 返回 5 条（共 93 条），节选：

| trade_date | index_slug | stock_code | stock_name | weight_pct | source_file | url_hash | processed_at |
|------|------|------|------|------|------|------|------|
| 2026-07-31 | hsi | 0001 | CKH | 1.1700 | con_31Jul26.pdf | d897b807861a3cb6f2ad4763091175e366eb9cc0520cf388573ca261ab76862d | 2026-08-01 20:00:03 |
| ... | ... | ... | ... | ... | ... | ... | ... |
