# 申万行业总览（MCP 工具 `ft_sw_industry_overview`）

> **MCP 工具**：`ft_sw_industry_overview`（category: `指数专题/申万行业`）。返回统一 MCP 输出：`structuredContent.metadata` + `structuredContent.data`；`content[0].text` 是同值的序列化 JSON，不额外返回 Markdown。输入参数 / 输出参数 / 数据样例见下文。
> 文中 `Response`、`items`、`records`、`code`、`message` 等名称仅为字段说明；MCP 对外固定为上述 `metadata/data`。

- 描述：按交易日查询申万一级、二级和三级行业的估值概况快照，包含成份股数、静态与滚动市盈率及其历史分位、市净率及其分位、股息率及其分位；交易日必填，可按行业层级筛选。
- 数据范围：2026-05-28 至今
- 单次限量：由 page/page_size 分页；默认返回全部命中
- 提示：
  - `date` 必填，格式 **YYYYMMDD**（带 `-` 会报 invalid date format）。
  - `level` 可选，传 1/2/3 过滤行业层级；不传返回全部层级。
  - 返回为 `{code,message,data}` 包裹的分页结构，`records` 为行数组，非顶层 items/total_items。
  - 估值字段均为字符串。

## 输入参数

| 名称 | 类型 | 必选 | 描述 |
|------|------|------|------|
| date | string | Y | 交易日，格式 YYYYMMDD |
| level | int | N | 行业层级：1/2/3，不传返回全部 |
| page | int | N | 页码，从 1 开始 |
| page_size | int | N | 每页数量 |

## 输出参数

> MCP 固定输出信封为 `structuredContent.metadata` + `structuredContent.data`；`content[0].text` 是与其同值的序列化 JSON，不是 Markdown。
>
> `items` / `records` / `code` / `message` 等传输字段不会直接出现在 MCP 结果中；分页与截断信息统一归入 `metadata`。

| MCP 字段 | 类型 | 必填 | 描述 |
|----------|------|------|------|
| metadata | object | Y | 契约版本、数据来源、工具名、业务口径、总量、分页、返回条数、截断状态及 warnings |
| data | array | Y | 归一化后的业务数据项 |

### data 业务字段

| 名称 | 类型 | 默认显示 | 描述 |
|------|------|---------|------|
| constituentCount | string | N | 成份股数量 |
| dvRatio | string | N | 股息率（%） |
| dvRatioPercentile | string | N | 股息率历史分位（%） |
| industryCode | string | Y | 申万行业代码，如 801010.SI |
| industryName | string | Y | 行业名称（中文） |
| level | int | Y | 行业层级 1/2/3 |
| lyrPe | string | N | 静态 PE（LYR） |
| lyrPePercentile | string | N | LYR PE 历史分位（%） |
| parentIndustryName | string | N | 父级行业名称 |
| pb | string | N | PB |
| pbPercentile | string | N | PB 历史分位（%） |
| tradeDate | string | N | 交易日期 |
| ttmPe | string | N | TTM PE |
| ttmPePercentile | string | N | TTM PE 历史分位（%） |

## 调用方法（MCP）

> MCP Streamable HTTP 要求**先 initialize 拿 `Mcp-Session-Id`，发送 `notifications/initialized`，再 `tools/call`**，后续请求同时带该 Session ID 和协商后的 `MCP-Protocol-Version`。返回统一 `metadata/data` 结构化输出；`content[0].text` 为同值 JSON 文本，不额外返回 Markdown。

**curl**：

```bash
set -euo pipefail

MCP_BASE_URL="<MCP_BASE_URL>"

check_mcp_response() {
  local response=$1
  printf '%s\n' "$response"
  if printf '%s\n' "$response" | grep -Eq '"isError"[[:space:]]*:[[:space:]]*true|"error"[[:space:]]*:[[:space:]]*\{'; then
    return 1
  fi
}

SID=$(curl -fsS -m 60 -D - -o /dev/null -X POST "$MCP_BASE_URL" \
  -H "Accept: application/json, text/event-stream" \
  -H "Content-Type: application/json" \
  -d '{"jsonrpc":"2.0","id":1,"method":"initialize","params":{"protocolVersion":"2025-11-25","capabilities":{},"clientInfo":{"name":"ftshare-doc-example","version":"1.0.0"}}}' \
  | awk 'tolower($1)=="mcp-session-id:" {print $2}' \
  | tr -d '\r')

if [ -z "$SID" ]; then
  printf '%s\n' 'initialize 未返回 Mcp-Session-Id' >&2
  exit 1
fi

curl -fsS -m 60 -o /dev/null -X POST "$MCP_BASE_URL" \
  -H "Accept: application/json, text/event-stream" \
  -H "Content-Type: application/json" \
  -H "Mcp-Session-Id: $SID" \
  -H "MCP-Protocol-Version: 2025-11-25" \
  -d '{"jsonrpc":"2.0","method":"notifications/initialized"}'

CALL_RESPONSE=$(curl -fsS -m 60 -X POST "$MCP_BASE_URL" \
  -H "Accept: application/json, text/event-stream" \
  -H "Content-Type: application/json" \
  -H "Mcp-Session-Id: $SID" \
  -H "MCP-Protocol-Version: 2025-11-25" \
  -d '{"jsonrpc":"2.0","id":2,"method":"tools/call","params":{"name":"ft_sw_industry_overview","arguments":{"date":"20260624","page":1,"page_size":2}}}')

check_mcp_response "$CALL_RESPONSE"
```

**Python（`mcp` SDK，自动握手管理 session）**：

```python
import asyncio

from mcp import ClientSession
from mcp.client.streamable_http import streamable_http_client


MCP_BASE_URL = "<MCP_BASE_URL>"


async def main():
    async with streamable_http_client(MCP_BASE_URL) as (read_stream, write_stream):
        async with ClientSession(read_stream, write_stream) as session:
            await session.initialize()
            result = await session.call_tool(
                'ft_sw_industry_overview',
                {'date': '20260624', 'page': 1, 'page_size': 2},
            )
            if result.is_error:
                raise RuntimeError(result.content[0].text)
            print(result.structured_content)
            print(result.content[0].text)


asyncio.run(main())
```

## 数据样例

| industryCode | industryName | level | constituentCount | tradeDate | lyrPe | lyrPePercentile | ttmPe | ttmPePercentile | pb | pbPercentile | dvRatio | dvRatioPercentile |
|------|------|------|------|------|------|------|------|------|------|------|------|------|
| 801010.SI | 农林牧渔 | 1 | 104 | 2026-06-24 | 20.240000 | 2.570000 | 25.540000 | 33.200000 | 1.890000 | 7.500000 | 2.580000 | 95.780000 |
| 801030.SI | 基础化工 | 1 | 410 | 2026-06-24 | 30.110000 | 61.190000 | 29.160000 | 69.480000 | 2.670000 | 56.320000 | 1.370000 | 50.660000 |
| ... | ... | ... | ... | ... | ... | ... | ... | ... | ... | ... | ... | ... |
