# 申万行业日度指标（MCP 工具 `ft_sw_industry_daily_metrics`）

> **MCP 工具**：`ft_sw_industry_daily_metrics`（category: `指数专题/申万行业`）。返回统一 MCP 输出：`structuredContent.metadata` + `structuredContent.data`；`content[0].text` 是同值的序列化 JSON，不额外返回 Markdown。输入参数 / 输出参数 / 数据样例见下文。
> 文中 `Response`、`items`、`records`、`code`、`message` 等名称仅为字段说明；MCP 对外固定为上述 `metadata/data`。

- 描述：查询指定申万行业的日频估值、资金和量价指标时间序列，包含静态与滚动市盈率及其分位和中位数、市净率、股息率、换手率、总市值、收盘价、破净比例、指数收盘、成交额及拥挤度；起止日期和行业代码必填。
- 数据范围：2021-05-31 至今
- 单次限量：无 3 天跨度限制，由 page/page_size 分页
- 提示：
  - `start_date`/`end_date`/`industry_code` 必填；`level` 可选，不传按 `industry_code` 自动匹配层级。
  - 日期格式 **YYYYMMDD**；`industry_code` 必须带 `.SI` 后缀（如 801010.SI）。
  - 字段 camelCase、均为字符串，输出参数表列出全部 42 项。

## 输入参数

| 名称 | 类型 | 必选 | 描述 |
|------|------|------|------|
| level | int | N | 行业层级：1/2/3；不传按 industry_code 自动匹配层级 |
| start_date | string | Y | 起始日期，YYYYMMDD |
| end_date | string | Y | 截止日期，YYYYMMDD |
| industry_code | string | Y | 行业代码，带 .SI 后缀，如 801010.SI |
| page | int | N | 页码，从 1 开始，默认 1 |
| page_size | int | N | 每页数量，默认 50，最大 200 |

## 输出参数

> MCP 固定输出信封为 `structuredContent.metadata` + `structuredContent.data`；`content[0].text` 是与其同值的序列化 JSON，不是 Markdown。
>
> `items` / `records` / `code` / `message` 等传输字段不会直接出现在 MCP 结果中；分页与截断信息统一归入 `metadata`。

| MCP 字段 | 类型 | 必填 | 描述 |
|----------|------|------|------|
| metadata | object | Y | 契约版本、数据来源、工具名、业务口径、总量、分页、返回条数、截断状态及 warnings |
| data | array | Y | 归一化后的业务数据项 |

### data 业务字段

| 名称 | 类型 | 默认显示 | 描述 |
|------|------|---------|------|
| addClose | string | Y | 加权收盘价 |
| addDvRatio | string | Y | 加权股息率 |
| addDvTtm | string | Y | 加权 TTM 股息率 |
| addLyrPe | string | Y |  |
| addLyrPeQuantile | string | Y |  |
| addPb | string | Y |  |
| addPbQuantile | string | Y |  |
| addTtmPe | string | Y |  |
| addTtmPeQuantile | string | Y |  |
| addTurnoverRate | string | Y | 加权换手率 |
| addTurnoverRateF | string | Y | 加权自由流通换手率 |
| amount | string | Y | 成交额 |
| amountCongestion | string | Y |  |
| amountCongestionQuantile | string | Y |  |
| belowNetAssetCount | string | Y | 破净成份股数量 |
| belowNetAssetPercent | string | Y | 破净成份股占比 |
| closePrice | string | Y | 行业收盘价 |
| dvRatio | string | Y |  |
| dvRatioQuantile | string | Y |  |
| dvTtm | string | Y |  |
| dvTtmQuantile | string | Y |  |
| indexClose | string | Y | 行业指数收盘点位 |
| industryCode | string | Y | 行业代码（带 .SI 后缀） |
| industryName | string | Y | 行业名称 |
| lyrPe | string | Y |  |
| lyrPeQuantile | string | Y |  |
| middleLyrPe | string | Y |  |
| middleLyrPeQuantile | string | Y |  |
| middlePb | string | Y |  |
| middlePbQuantile | string | Y |  |
| middleTtmPe | string | Y |  |
| middleTtmPeQuantile | string | Y |  |
| pb | string | Y |  |
| pbQuantile | string | Y |  |
| totalCount | string | Y | 成份股数量 |
| totalMv | string | Y | 总市值 |
| tradeDate | string | Y | 交易日 |
| ttmPe | string | Y |  |
| ttmPeQuantile | string | Y |  |
| turnoverRate | string | Y | 换手率（%） |
| turnoverRateF | string | Y |  |
| turnoverRateFQuantile | string | Y |  |

## 调用方法（MCP）

> MCP Streamable HTTP 要求**先 initialize 拿 `Mcp-Session-Id`，发送 `notifications/initialized`，再 `tools/call`**，后续请求同时带该 Session ID 和协商后的 `MCP-Protocol-Version`。返回统一 `metadata/data` 结构化输出；`content[0].text` 为同值 JSON 文本，不额外返回 Markdown。

**curl**：

```bash
set -euo pipefail

MCP_BASE_URL="<MCP_BASE_URL>"

check_mcp_response() {
  local response=$1
  printf '%s\n' "$response"
  if printf '%s\n' "$response" | grep -Eq '"isError"[[:space:]]*:[[:space:]]*true|"error"[[:space:]]*:[[:space:]]*\{'; then
    return 1
  fi
}

SID=$(curl -fsS -m 60 -D - -o /dev/null -X POST "$MCP_BASE_URL" \
  -H "Accept: application/json, text/event-stream" \
  -H "Content-Type: application/json" \
  -d '{"jsonrpc":"2.0","id":1,"method":"initialize","params":{"protocolVersion":"2025-11-25","capabilities":{},"clientInfo":{"name":"ftshare-doc-example","version":"1.0.0"}}}' \
  | awk 'tolower($1)=="mcp-session-id:" {print $2}' \
  | tr -d '\r')

if [ -z "$SID" ]; then
  printf '%s\n' 'initialize 未返回 Mcp-Session-Id' >&2
  exit 1
fi

curl -fsS -m 60 -o /dev/null -X POST "$MCP_BASE_URL" \
  -H "Accept: application/json, text/event-stream" \
  -H "Content-Type: application/json" \
  -H "Mcp-Session-Id: $SID" \
  -H "MCP-Protocol-Version: 2025-11-25" \
  -d '{"jsonrpc":"2.0","method":"notifications/initialized"}'

CALL_RESPONSE=$(curl -fsS -m 60 -X POST "$MCP_BASE_URL" \
  -H "Accept: application/json, text/event-stream" \
  -H "Content-Type: application/json" \
  -H "Mcp-Session-Id: $SID" \
  -H "MCP-Protocol-Version: 2025-11-25" \
  -d '{"jsonrpc":"2.0","id":2,"method":"tools/call","params":{"name":"ft_sw_industry_daily_metrics","arguments":{"level":1,"start_date":"20260520","end_date":"20260616","industry_code":"801010.SI","page":1,"page_size":2}}}')

check_mcp_response "$CALL_RESPONSE"
```

**Python（`mcp` SDK，自动握手管理 session）**：

```python
import asyncio

from mcp import ClientSession
from mcp.client.streamable_http import streamable_http_client


MCP_BASE_URL = "<MCP_BASE_URL>"


async def main():
    async with streamable_http_client(MCP_BASE_URL) as (read_stream, write_stream):
        async with ClientSession(read_stream, write_stream) as session:
            await session.initialize()
            result = await session.call_tool(
                'ft_sw_industry_daily_metrics',
                {'level': 1, 'start_date': '20260520', 'end_date': '20260616', 'industry_code': '801010.SI', 'page': 1, 'page_size': 2},
            )
            if result.is_error:
                raise RuntimeError(result.content[0].text)
            print(result.structured_content)
            print(result.content[0].text)


asyncio.run(main())
```

## 数据样例

| industryCode | industryName | tradeDate | lyrPe | ttmPe | pb | dvRatio | turnoverRate | totalMv | closePrice | indexClose | amount |
|------|------|------|------|------|------|------|------|------|------|------|------|
| 801010.SI | 农林牧渔 | 2026-05-20 | 68.830000 | 60.280000 | 5.650000 | 1.710000 | 1.950000 | 128418153.960 | 12.210000 | 2636.340000 | 15440669.780 |
| 801010.SI | 农林牧渔 | 2026-05-21 | 66.790000 | 58.560000 | 5.520000 | 1.750000 | 2.300000 | 125707631.790 | 11.850000 | 2582.170000 | 17335969.406 |
| ... | ... | ... | ... | ... | ... | ... | ... | ... | ... | ... | ... |
