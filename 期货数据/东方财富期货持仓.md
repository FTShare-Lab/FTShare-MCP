# 东方财富期货持仓（MCP 工具 `ft_get_eastmoney_futures_position`）

> **MCP 工具**：`ft_get_eastmoney_futures_position`（category: `期货数据`）。返回统一 MCP 输出：`structuredContent.metadata` + `structuredContent.data`；`content[0].text` 是同值的序列化 JSON，不额外返回 Markdown。输入参数 / 输出参数 / 数据样例见下文。
> 文中 `Response`、`items`、`records`、`code`、`message` 等名称仅为字段说明；MCP 对外固定为上述 `metadata/data`。

- 描述：获取东方财富期货每日成交持仓排行榜数据，按合约/会员维度给出成交量、多头、空头、净多头、净空头的排名与持仓量及变化，含多头增/减仓、空头增/减仓排名，以及结算价、多头/空头平均价。数据来源：东方财富。支持按交易所、品种、合约、会员、交易日或日期区间过滤。提示：每个合约每个交易日下会有多会员多行，总量较大，建议按合约+日期精准过滤；`trade_date` 与 `start_date`/`end_date` 互斥。
- 数据范围：持仓类，最新一期或当日快照（历史序列下限以服务端返回为准）
- 单次限量：分页返回，默认 `page=1`、`page_size=50`、最大 `page_size=200`
- 提示：
  - `exchange` 取值：shfe（上期所）/ dce（大商所）/ czce（郑商所）/ cffex（中金所）/ ine（能源中心）/ gfe（广期所）。
  - 每个合约每个交易日下会有多会员多行，总量较大，建议按合约+日期精准过滤。
  - `trade_date` 与 `start_date`/`end_date` 互斥。

## 输入参数

| 名称 | 类型 | 必选 | 描述 |
|------|------|------|------|
| exchange | string | N | 交易所代码：shfe / dce / czce / cffex / ine / gfe |
| variety_code | string | N | 品种代码，如 cu / au / al / IF |
| contract_code | string | N | 合约代码，如 CU2607 / AU2608 |
| trade_date | string | N | 交易日 YYYYMMDD；与 start_date/end_date 互斥 |
| start_date | string | N | 区间起始日 YYYYMMDD；需与 end_date 同时提供 |
| end_date | string | N | 区间结束日 YYYYMMDD；需与 start_date 同时提供 |
| member_name_abbr | string | N | 会员简称 |
| page | int | N | 页码，从 1 开始，默认 1 |
| page_size | int | N | 每页条数，默认 50，最大 200 |

## 输出参数

> MCP 固定输出信封为 `structuredContent.metadata` + `structuredContent.data`；`content[0].text` 是与其同值的序列化 JSON，不是 Markdown。
>
> `items` / `records` / `code` / `message` 等传输字段不会直接出现在 MCP 结果中；分页与截断信息统一归入 `metadata`。

| MCP 字段 | 类型 | 必填 | 描述 |
|----------|------|------|------|
| metadata | object | Y | 契约版本、数据来源、工具名、业务口径、总量、分页、返回条数、截断状态及 warnings |
| data | array | Y | 归一化后的业务数据项；元素字段见下方 |

### data 业务字段

EastmoneyFuturesPositionItem：

| 名称 | 类型 | 默认显示 | 描述 |
|------|------|---------|------|
| exchange | string | Y | 交易所代码 |
| variety_code | string | Y | 品种代码 |
| contract_code | string | Y | 合约代码 |
| trade_date | string | Y | 交易日期 YYYYMMDD |
| org_code | string | Y | 会员机构代码 |
| member_name_abbr | string | Y | 会员简称 |
| volume_rank | string | Y | 成交量排名 |
| volume | string | Y | 成交量（手） |
| volume_change | string | Y | 成交量变化 |
| lp_rank | string | Y | 多头持仓排名 |
| long_position | string | Y | 多头持仓量 |
| lp_change | string | Y | 多头持仓变化 |
| sp_rank | string | Y | 空头持仓排名 |
| short_position | string | Y | 空头持仓量 |
| sp_change | string | Y | 空头持仓变化 |
| nlp_rank | string | Y | 净多头排名 |
| net_long_position | string | Y | 净多头持仓量 |
| nlp_change | string | Y | 净多头变化 |
| nsp_rank | string | Y | 净空头排名 |
| net_short_position | string | Y | 净空头持仓量 |
| nsp_change | string | Y | 净空头变化 |
| lp_up_rank | string | Y | 多头增仓排名 |
| lp_down_rank | string | Y | 多头减仓排名 |
| sp_up_rank | string | Y | 空头增仓排名 |
| sp_down_rank | string | Y | 空头减仓排名 |
| settle_price | string | Y | 结算价 |
| lp_avg_price | string | Y | 多头平均价 |
| sp_avg_price | string | Y | 空头平均价 |

## 调用方法（MCP）

> MCP 工具名 `ft_get_eastmoney_futures_position`。MCP Streamable HTTP 要求**先 initialize 拿 `Mcp-Session-Id`，发送 `notifications/initialized`，再 `tools/call`**，后续请求同时带该 Session ID 和协商后的 `MCP-Protocol-Version`。返回统一 `metadata/data` 结构化输出；`content[0].text` 为同值 JSON 文本，不额外返回 Markdown。

**curl**：

```bash
SID=$(curl -sS -m 10 -D - -o /dev/null -X POST <MCP_BASE_URL> \
  -H "Accept: application/json, text/event-stream" -H "Content-Type: application/json" \
  -d '{"jsonrpc":"2.0","id":1,"method":"initialize","params":{"protocolVersion":"2025-11-25","capabilities":{},"clientInfo":{"name":"t","version":"1"}}}' \
  | grep -i 'mcp-session-id:' | awk '{print $2}' | tr -d '\r')

curl -fsS -m 10 -o /dev/null -X POST <MCP_BASE_URL> \
  -H "Accept: application/json, text/event-stream" -H "Content-Type: application/json" \
  -H "Mcp-Session-Id: $SID" \
  -H "MCP-Protocol-Version: 2025-11-25" \
  -d '{"jsonrpc":"2.0","method":"notifications/initialized"}'

curl -sS -m 10 -X POST <MCP_BASE_URL> \
  -H "Accept: application/json, text/event-stream" -H "Content-Type: application/json" \
  -H "Mcp-Session-Id: $SID" \
  -H "MCP-Protocol-Version: 2025-11-25" \
  -d '{"jsonrpc":"2.0","id":2,"method":"tools/call","params":{"name":"ft_get_eastmoney_futures_position","arguments":{}}}'
```

**Python（`mcp` SDK，自动握手管理 session）**：

```python
import asyncio
from mcp import ClientSession
from mcp.client.streamable_http import streamablehttp_client

async def main():
    async with streamablehttp_client('<MCP_BASE_URL>') as (r, w, _):
        async with ClientSession(r, w) as s:
            await s.initialize()
            res = await s.call_tool('ft_get_eastmoney_futures_position', {})
            print(res.structuredContent)   # 推荐：统一 metadata/data 结构化输出
            print(res.content[0].text)   # 兼容：与 structuredContent 同值的 JSON 文本
asyncio.run(main())
```

## 数据样例

期货每日成交持仓排行共 889441 条，节选：

| exchange | variety_code | contract_code | trade_date | member_name_abbr | volume |
|----------|--------------|---------------|------------|------------------|--------|
| cffex | ic | IC2606 | 2026-06-17 | 中信期货(代客) | ... |
| ... | ... | ... | ... | ... | ... |
