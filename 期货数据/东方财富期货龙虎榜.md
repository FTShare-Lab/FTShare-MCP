# 东方财富期货龙虎榜（MCP 工具 `ft_eastmoney_futures_strange`）

> **MCP 工具**：`ft_eastmoney_futures_strange`（category: `期货数据`）。返回统一 MCP 输出：`structuredContent.metadata` + `structuredContent.data`；`content[0].text` 是同值的序列化 JSON，不额外返回 Markdown。输入参数 / 输出参数 / 数据样例见下文。
> 文中 `Response`、`items`、`records`、`code`、`message` 等名称仅为字段说明；MCP 对外固定为上述 `metadata/data`。

- 描述：查询指定交易所、品种、合约和交易日的东方财富期货龙虎榜面板，包括占比切片、会员排名和汇总数据。
- 数据范围：按交易日查询已采集的期货龙虎榜快照
- 单次限量：返回该合约对应的完整面板数组，不分页
- 提示：
  - `exchange`、`variety`、`contract`、`trade_date` 均为必填。
  - `trade_date` 格式为 `YYYYMMDD`。

## 输入参数

| 名称 | 类型 | 必选 | 描述 |
|------|------|------|------|
| exchange | string | Y | 交易所代码：`shfe`/`dce`/`czce`/`cffex`/`ine`/`gfe` |
| variety | string | Y | 品种名称，如 `多晶硅` |
| contract | string | Y | 合约代码，如 `ps2609` |
| trade_date | string | Y | 交易日，`YYYYMMDD` |

## 输出参数

> MCP 固定输出信封为 `structuredContent.metadata` + `structuredContent.data`；`content[0].text` 是与其同值的序列化 JSON，不是 Markdown。
>
> `items` / `records` / `code` / `message` 等传输字段不会直接出现在 MCP 结果中；分页与截断信息统一归入 `metadata`。

| MCP 字段 | 类型 | 必填 | 描述 |
|----------|------|------|------|
| metadata | object | Y | 契约版本、数据来源、工具名、业务口径、总量、分页、返回条数、截断状态及 warnings |
| data | array | Y | 归一化后的业务数据项 |

### data 业务字段

| 名称 | 类型 | 默认显示 | 描述 |
|------|------|---------|------|
| key | string | Y | 面板标识，如 `volume_ratio`/`long_rank`/`short_rank` 等 |
| rows | array/null | N | 排名记录；每项含 `rank`/`member_id`/`member_name`/`value`/`change` |
| slices | array/null | N | 饼图切片；每项含 `name`/`value`/`percent` |
| summary | object/null | N | 汇总；含 `today_total`/`previous_total`/`total_change` |
| title | string | Y | 面板标题，如 `成交持仓比例`/`成交量龙虎榜` |

## 调用方法（MCP）

> MCP Streamable HTTP 要求**先 initialize 拿 `Mcp-Session-Id`，发送 `notifications/initialized`，再 `tools/call`**，后续请求同时带该 Session ID 和协商后的 `MCP-Protocol-Version`。返回统一 `metadata/data` 结构化输出；`content[0].text` 为同值 JSON 文本，不额外返回 Markdown。

**curl**：

```bash
set -euo pipefail

MCP_BASE_URL="<MCP_BASE_URL>"

check_mcp_response() {
  local response=$1
  printf '%s\n' "$response"
  if printf '%s\n' "$response" | grep -Eq '"isError"[[:space:]]*:[[:space:]]*true|"error"[[:space:]]*:[[:space:]]*\{'; then
    return 1
  fi
}

SID=$(curl -fsS -m 60 -D - -o /dev/null -X POST "$MCP_BASE_URL" \
  -H "Accept: application/json, text/event-stream" \
  -H "Content-Type: application/json" \
  -d '{"jsonrpc":"2.0","id":1,"method":"initialize","params":{"protocolVersion":"2025-11-25","capabilities":{},"clientInfo":{"name":"ftshare-doc-example","version":"1.0.0"}}}' \
  | awk 'tolower($1)=="mcp-session-id:" {print $2}' \
  | tr -d '\r')

if [ -z "$SID" ]; then
  printf '%s\n' 'initialize 未返回 Mcp-Session-Id' >&2
  exit 1
fi

curl -fsS -m 60 -o /dev/null -X POST "$MCP_BASE_URL" \
  -H "Accept: application/json, text/event-stream" \
  -H "Content-Type: application/json" \
  -H "Mcp-Session-Id: $SID" \
  -H "MCP-Protocol-Version: 2025-11-25" \
  -d '{"jsonrpc":"2.0","method":"notifications/initialized"}'

CALL_RESPONSE=$(curl -fsS -m 60 -X POST "$MCP_BASE_URL" \
  -H "Accept: application/json, text/event-stream" \
  -H "Content-Type: application/json" \
  -H "Mcp-Session-Id: $SID" \
  -H "MCP-Protocol-Version: 2025-11-25" \
  -d '{"jsonrpc":"2.0","id":2,"method":"tools/call","params":{"name":"ft_eastmoney_futures_strange","arguments":{"exchange":"shfe","variety":"螺纹钢","contract":"rb2610","trade_date":"20260624"}}}')

check_mcp_response "$CALL_RESPONSE"
```

**Python（`mcp` SDK，自动握手管理 session）**：

```python
import asyncio

from mcp import ClientSession
from mcp.client.streamable_http import streamable_http_client


MCP_BASE_URL = "<MCP_BASE_URL>"


async def main():
    async with streamable_http_client(MCP_BASE_URL) as (read_stream, write_stream):
        async with ClientSession(read_stream, write_stream) as session:
            await session.initialize()
            result = await session.call_tool(
                'ft_eastmoney_futures_strange',
                {'exchange': 'shfe', 'variety': '螺纹钢', 'contract': 'rb2610', 'trade_date': '20260624'},
            )
            if result.is_error:
                raise RuntimeError(result.content[0].text)
            print(result.structured_content)
            print(result.content[0].text)


asyncio.run(main())
```

## 数据样例

`exchange=shfe, variety=螺纹钢, contract=rb2610, trade_date=20260624` 返回 12 个面板（3 个占比切片面板 + 9 个排名面板）。以下展示 2 个代表面板，其余面板结构相同。

**volume_ratio（成交持仓比例，slices 字段）**

| name | percent | value |
|------|------|------|
| 东证期货 | 15.98 | 123254 |
| 中信期货 | 14.18 | 109333 |
| 国泰君安 | 12.81 | 98768 |
| ... | ... | ... |

**volume_rank（成交量龙虎榜，rows 字段）**

| rank | member_id | member_name | value | change |
|------|------|------|------|------|
| 1 | 0163 | 东证期货 | 123254 | 12285 |
| 2 | 0148 | 中信期货 | 109333 | 11002 |
| 3 | 0004 | 国泰君安 | 98768 | 11679 |
| ... | ... | ... | ... | ... |

排名类面板另含 `summary`（汇总字段），如 `volume_rank` 的 `summary` 为 `{"previous_total": 689660, "today_total": 771219, "total_change": 81559}`。
