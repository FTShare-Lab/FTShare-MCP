# 中国期货基础数据（MCP 工具 `ft_get_china_futures_base_data_handler`）

> **MCP 工具**：`ft_get_china_futures_base_data_handler`（category: `期货数据`）。返回 **markdown 表格文本**（非结构化 JSON）。输入参数 / 输出参数 / 数据样例对照 [ftshare-doc](../../ftshare-doc/api-doc) 原接口。

- 描述：查询指定交易日的期货合约要素（交易单位/最小变动价位/保证金/合约乘数/上市日/最后交易日/交割等）。不传 `trade_date` 默认前一交易日（CST）。可按 WIND 合约全码过滤，未命中返回 `items: []`（HTTP 200）。返回 `ChinaFuturesBaseDataResponse`，由 `get_china_futures_base_data` 读取。
- 数据范围：最新快照（按交易日的合约要素，无时间序列）
- 单次限量：单交易日全量或单合约要素，无分页
- 提示：
  - `trade_date` 不传则使用前一交易日（CST）。
  - `symbol` 为 WIND 合约全码（如 `A2605.DCE`），大小写不敏感；不传或空表示该日全部。
  - 未命中返回空 `items`，HTTP 仍 200。

## 输入参数

| 名称 | 类型 | 必选 | 描述 |
|------|------|------|------|
| trade_date | int | N | 交易日 YYYYMMDD；不传则使用前一交易日（CST） |
| symbol | string | N | WIND 合约全码如 A2605.DCE；大小写不敏感；不传或空表示该日全部 |

## 输出参数

| 名称 | 类型 | 默认显示 | 描述 |
|------|------|---------|------|
| trade_date | int | Y | 实际使用的交易日 |
| items | array[ChinaFuturesBaseDataItem] | Y | 合约要素列表 |

ChinaFuturesBaseDataItem：

| 名称 | 类型 | 默认显示 | 描述 |
|------|------|---------|------|
| trade_date | int | Y | 交易日 YYYYMMDD |
| exchange | string | Y | 交易所 |
| symbol | string | Y | 合约代码 |
| product | string | Y | 品种 |
| symbol_cn_name | string | Y | 合约中文名称 |
| trade_unit | string | Y | 交易单位 |
| price_unit | f64 | N | 价格单位 |
| put_price | string | Y | 报价 |
| min_put_price_info | string | Y | 最小变动价位说明 |
| margin_info | string | Y | 保证金说明 |
| trade_months_info | string | Y | 合约月份说明 |
| trade_hours_info | string | Y | 交易时间说明 |
| last_trade_date | string | Y | 最后交易日 |
| last_trade_date_hour | string | Y | 最后交易日时间 |
| delivery_date | string | Y | 交割日期 |
| multiplier | f64 | N | 合约乘数 |
| list_date | int | N | 上市日 YYYYMMDD |
| de_list_tdate | int | N | 摘牌日 YYYYMMDD |
| delivery_mean | string | Y | 交割方式 |
| delivery_site | string | Y | 交割地点 |
| value_info | string | Y | 价值说明 |
| price_fluctuation_info | string | Y | 涨跌停板说明 |
| position_limit_info | string | Y | 持仓限额说明 |
| mean_cal_value | f64 | N | 平均价计算值 |
| spot_symbol | string | N | 现货代码 |
| type_code | int | N | 类型代码 |
| sub_type_code | int | N | 子类型代码 |
| future_type | int | N | 合约类型：1 主力 / 2 真实 / 3 连续 |

## 调用方法（MCP）

> MCP 工具名 `ft_get_china_futures_base_data_handler`。MCP Streamable HTTP 要求**先 initialize 拿 `Mcp-Session-Id`，再 `tools/call`**，后续请求带该 header。返回 **markdown 表格文本**。

**curl**：

```bash
SID=$(curl -sS -m 10 -D - -o /dev/null -X POST <MCP_BASE_URL> \
  -H "Accept: application/json, text/event-stream" -H "Content-Type: application/json" \
  -d '{"jsonrpc":"2.0","id":1,"method":"initialize","params":{"protocolVersion":"2025-11-25","capabilities":{},"clientInfo":{"name":"t","version":"1"}}}' \
  | grep -i 'mcp-session-id:' | awk '{print $2}' | tr -d '')

curl -sS -m 10 -X POST <MCP_BASE_URL> \
  -H "Accept: application/json, text/event-stream" -H "Content-Type: application/json" \
  -H "Mcp-Session-Id: $SID" \
  -d '{"jsonrpc":"2.0","id":2,"method":"tools/call","params":{"name":"ft_get_china_futures_base_data_handler","arguments":{}}}'
```

**Python（`mcp` SDK，自动握手管理 session）**：

```python
import asyncio
from mcp import ClientSession
from mcp.client.streamable_http import streamablehttp_client

async def main():
    async with streamablehttp_client('<MCP_BASE_URL>') as (r, w, _):
        async with ClientSession(r, w) as s:
            await s.initialize()
            res = await s.call_tool('ft_get_china_futures_base_data_handler', {})
            print(res.content[0].text)   # markdown 表格文本

asyncio.run(main())
```

## 数据样例

公网数据量较大未完整取，结构含合约基础信息（交易单位/最小变动价位/保证金/合约乘数/上市日/最后交易日/交割等）：

| symbol | exchange | symbol_cn_name | trade_unit | multiplier | last_trade_date | ... |
|--------|----------|----------------|------------|------------|-----------------|-----|
| ... | ... | ... | ... | ... | ... | ... |
