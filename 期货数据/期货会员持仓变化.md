# 期货会员持仓变化（MCP 工具 `ft_member_build_process`）

> **MCP 工具**：`ft_member_build_process`（category: `期货数据`）。返回统一 MCP 输出：`structuredContent.metadata` + `structuredContent.data`；`content[0].text` 是同值的序列化 JSON，不额外返回 Markdown。输入参数 / 输出参数 / 数据样例见下文。
> 文中 `Response`、`items`、`records`、`code`、`message` 等名称仅为字段说明；MCP 对外固定为上述 `metadata/data`。

- 描述：查询单个期货会员在指定合约和日期区间内的持仓变化，并结合合约行情估算每日及累计盈亏。
- 数据范围：期货会员持仓和期货日终行情历史数据
- 单次限量：默认 `page=1`、`page_size=50`，`page_size` 最大 200
- 提示：
  - `exchange`、`member_name`、`instrument_id` 为必填。
  - `start_date`/`end_date` 支持 `YYYYMMDD` 或 `YYYY-MM-DD`，默认起始 `20260101`，默认结束当天。
  - `contract_multiplier` 不传时按品种默认表推导。

## 输入参数

| 名称 | 类型 | 必选 | 描述 |
|------|------|------|------|
| exchange | string | Y | 交易所代码，如 `SHFE`、`DCE`、`CZCE` |
| member_name | string | Y | 会员名称 |
| instrument_id | string | Y | 合约代码，如 `rb2601` |
| start_date | string | N | 起始日期，`YYYYMMDD` 或 `YYYY-MM-DD`，默认 `20260101` |
| end_date | string | N | 结束日期，`YYYYMMDD` 或 `YYYY-MM-DD`，默认当天 |
| contract_multiplier | float | N | 合约乘数；不传时按品种默认表推导 |
| page | int | N | 页码，默认 1 |
| page_size | int | N | 每页条数，默认 50，最大 200 |

## 输出参数

> MCP 固定输出信封为 `structuredContent.metadata` + `structuredContent.data`；`content[0].text` 是与其同值的序列化 JSON，不是 Markdown。
>
> `items` / `records` / `code` / `message` 等传输字段不会直接出现在 MCP 结果中；分页与截断信息统一归入 `metadata`。

| MCP 字段 | 类型 | 必填 | 描述 |
|----------|------|------|------|
| metadata | object | Y | 契约版本、数据来源、工具名、业务口径、总量、分页、返回条数、截断状态及 warnings |
| data | array | Y | 归一化后的业务数据项 |

### data 业务字段

> 当前参数组合暂无数据，字段以实际返回为准。

| 名称 | 类型 | 默认显示 | 描述 |
|------|------|---------|------|
| trade_date | string | Y | 交易日 |
| exchange | string | Y | 交易所 |
| instrument_id | string | Y | 合约代码 |
| product_name | string | Y | 品种名称 |
| member_name | string | Y | 会员名称 |
| volume_rank | int/null | N | 成交排名 |
| volume_value | int/null | N | 成交量 |
| volume_change | int/null | N | 成交量变化 |
| long_rank | int/null | N | 多头排名 |
| long_value | int/null | N | 多头持仓量 |
| long_change | int/null | N | 多头持仓变化 |
| short_rank | int/null | N | 空头排名 |
| short_value | int/null | N | 空头持仓量 |
| short_change | int/null | N | 空头持仓变化 |
| net_position | int/null | N | 净持仓 |
| observed_side | string | Y | 观察方向 |
| open_price | string/null | N | 开盘价 |
| high_price | string/null | N | 最高价 |
| low_price | string/null | N | 最低价 |
| close_price | string/null | N | 收盘价 |
| settlement_price | string/null | N | 结算价 |
| pre_settlement_price | string/null | N | 前结算价 |
| volume | string/null | N | 合约成交量 |
| open_interest | string/null | N | 合约持仓量 |
| open_interest_change | string/null | N | 合约持仓量变化 |
| contract_multiplier | float/null | N | 合约乘数 |
| estimated_net_change | int/null | N | 估算净持仓变化 |
| estimated_position_cost | float/null | N | 估算持仓成本 |
| estimated_daily_pnl | float/null | N | 估算当日盈亏 |
| estimated_daily_pnl_wan | float/null | N | 估算当日盈亏，万元 |
| estimated_cumulative_pnl | float/null | N | 估算累计盈亏 |
| estimated_cumulative_pnl_wan | float/null | N | 估算累计盈亏，万元 |

## 调用方法（MCP）

> MCP Streamable HTTP 要求**先 initialize 拿 `Mcp-Session-Id`，发送 `notifications/initialized`，再 `tools/call`**，后续请求同时带该 Session ID 和协商后的 `MCP-Protocol-Version`。返回统一 `metadata/data` 结构化输出；`content[0].text` 为同值 JSON 文本，不额外返回 Markdown。

**curl**：

```bash
set -euo pipefail

MCP_BASE_URL="<MCP_BASE_URL>"

check_mcp_response() {
  local response=$1
  printf '%s\n' "$response"
  if printf '%s\n' "$response" | grep -Eq '"isError"[[:space:]]*:[[:space:]]*true|"error"[[:space:]]*:[[:space:]]*\{'; then
    return 1
  fi
}

SID=$(curl -fsS -m 60 -D - -o /dev/null -X POST "$MCP_BASE_URL" \
  -H "Accept: application/json, text/event-stream" \
  -H "Content-Type: application/json" \
  -d '{"jsonrpc":"2.0","id":1,"method":"initialize","params":{"protocolVersion":"2025-11-25","capabilities":{},"clientInfo":{"name":"ftshare-doc-example","version":"1.0.0"}}}' \
  | awk 'tolower($1)=="mcp-session-id:" {print $2}' \
  | tr -d '\r')

if [ -z "$SID" ]; then
  printf '%s\n' 'initialize 未返回 Mcp-Session-Id' >&2
  exit 1
fi

curl -fsS -m 60 -o /dev/null -X POST "$MCP_BASE_URL" \
  -H "Accept: application/json, text/event-stream" \
  -H "Content-Type: application/json" \
  -H "Mcp-Session-Id: $SID" \
  -H "MCP-Protocol-Version: 2025-11-25" \
  -d '{"jsonrpc":"2.0","method":"notifications/initialized"}'

CALL_RESPONSE=$(curl -fsS -m 60 -X POST "$MCP_BASE_URL" \
  -H "Accept: application/json, text/event-stream" \
  -H "Content-Type: application/json" \
  -H "Mcp-Session-Id: $SID" \
  -H "MCP-Protocol-Version: 2025-11-25" \
  -d '{"jsonrpc":"2.0","id":2,"method":"tools/call","params":{"name":"ft_member_build_process","arguments":{"exchange":"SHFE","member_name":"永安期货","instrument_id":"rb2610","start_date":"20260622","end_date":"20260624","page":1,"page_size":2}}}')

check_mcp_response "$CALL_RESPONSE"
```

**Python（`mcp` SDK，自动握手管理 session）**：

```python
import asyncio

from mcp import ClientSession
from mcp.client.streamable_http import streamable_http_client


MCP_BASE_URL = "<MCP_BASE_URL>"


async def main():
    async with streamable_http_client(MCP_BASE_URL) as (read_stream, write_stream):
        async with ClientSession(read_stream, write_stream) as session:
            await session.initialize()
            result = await session.call_tool(
                'ft_member_build_process',
                {'exchange': 'SHFE', 'member_name': '永安期货', 'instrument_id': 'rb2610', 'start_date': '20260622', 'end_date': '20260624', 'page': 1, 'page_size': 2},
            )
            if result.is_error:
                raise RuntimeError(result.content[0].text)
            print(result.structured_content)
            print(result.content[0].text)


asyncio.run(main())
```

## 数据样例

> 当前参数组合暂无数据，样例字段以实际返回为准。

| trade_date | exchange | instrument_id | member_name | net_position | observed_side | close_price | estimated_daily_pnl | ... |
|------|------|------|------|------|------|------|------|------|
| ... | ... | ... | ... | ... | ... | ... | ... | ... |
