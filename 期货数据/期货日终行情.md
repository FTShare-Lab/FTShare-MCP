# 期货日终行情（MCP 工具 `ft_futures_eod_price`）

> **MCP 工具**：`ft_futures_eod_price`（category: `期货数据`）。返回统一 MCP 输出：`structuredContent.metadata` + `structuredContent.data`；`content[0].text` 是同值的序列化 JSON，不额外返回 Markdown。输入参数 / 输出参数 / 数据样例见下文。
> 文中 `Response`、`items`、`records`、`code`、`message` 等名称仅为字段说明；MCP 对外固定为上述 `metadata/data`。

- 描述：查询期货日终行情，包含开盘价、最高价、最低价、收盘价、结算价、成交量、成交额和持仓量；支持按交易所、合约、单一交易日或交易日区间筛选，单一交易日与日期区间不能同时使用。
- 数据范围：已入库的期货日终行情
- 单次限量：默认 `page=1`、`page_size=50`，`page_size` 最大 200
- 提示：
  - `trade_date` 不可与 `start_date`/`end_date` 同时使用；同时提供起止日期时需满足 `start_date <= end_date`。
  - `trade_date`/`start_date`/`end_date` 为 8 位整数 `YYYYMMDD`。

## 输入参数

| 名称 | 类型 | 必选 | 描述 |
|------|------|------|------|
| exchange | string | N | 交易所代码，精确匹配 |
| symbol | string | N | 合约代码，精确匹配 |
| trade_date | int | N | 精确交易日，8 位 `YYYYMMDD` |
| start_date | int | N | 起始交易日，8 位 `YYYYMMDD` |
| end_date | int | N | 结束交易日，8 位 `YYYYMMDD` |
| page | int | N | 页码，默认 1 |
| page_size | int | N | 每页条数，默认 50，最大 200 |

## 输出参数

> MCP 固定输出信封为 `structuredContent.metadata` + `structuredContent.data`；`content[0].text` 是与其同值的序列化 JSON，不是 Markdown。
>
> `items` / `records` / `code` / `message` 等传输字段不会直接出现在 MCP 结果中；分页与截断信息统一归入 `metadata`。

| MCP 字段 | 类型 | 必填 | 描述 |
|----------|------|------|------|
| metadata | object | Y | 契约版本、数据来源、工具名、业务口径、总量、分页、返回条数、截断状态及 warnings |
| data | array | Y | 归一化后的业务数据项 |

### data 业务字段

| 名称 | 类型 | 默认显示 | 描述 |
|------|------|---------|------|
| amount | string | Y | 成交额 |
| close_price | string | Y | 收盘价 |
| exchange | string | Y | 交易所 |
| highest_price | string | Y | 最高价 |
| lowest_price | string | Y | 最低价 |
| open_interest | string | Y | 持仓量 |
| open_interest_change | string | Y | 持仓量变化 |
| open_price | string | Y | 开盘价 |
| pre_settlement_price | string | Y | 前结算价 |
| settlement_price | string | Y | 结算价 |
| symbol | string | Y | 合约代码 |
| trade_date | int | Y | 交易日 |
| up_down_1 | string | Y | 涨跌额口径一 |
| up_down_2 | string | Y | 涨跌额口径二 |
| updated_at | string | Y | 更新时间 |
| volume | int64 | Y | 成交量 |

## 调用方法（MCP）

> MCP Streamable HTTP 要求**先 initialize 拿 `Mcp-Session-Id`，发送 `notifications/initialized`，再 `tools/call`**，后续请求同时带该 Session ID 和协商后的 `MCP-Protocol-Version`。返回统一 `metadata/data` 结构化输出；`content[0].text` 为同值 JSON 文本，不额外返回 Markdown。

**curl**：

```bash
set -euo pipefail

MCP_BASE_URL="<MCP_BASE_URL>"

check_mcp_response() {
  local response=$1
  printf '%s\n' "$response"
  if printf '%s\n' "$response" | grep -Eq '"isError"[[:space:]]*:[[:space:]]*true|"error"[[:space:]]*:[[:space:]]*\{'; then
    return 1
  fi
}

SID=$(curl -fsS -m 60 -D - -o /dev/null -X POST "$MCP_BASE_URL" \
  -H "Accept: application/json, text/event-stream" \
  -H "Content-Type: application/json" \
  -d '{"jsonrpc":"2.0","id":1,"method":"initialize","params":{"protocolVersion":"2025-11-25","capabilities":{},"clientInfo":{"name":"ftshare-doc-example","version":"1.0.0"}}}' \
  | awk 'tolower($1)=="mcp-session-id:" {print $2}' \
  | tr -d '\r')

if [ -z "$SID" ]; then
  printf '%s\n' 'initialize 未返回 Mcp-Session-Id' >&2
  exit 1
fi

curl -fsS -m 60 -o /dev/null -X POST "$MCP_BASE_URL" \
  -H "Accept: application/json, text/event-stream" \
  -H "Content-Type: application/json" \
  -H "Mcp-Session-Id: $SID" \
  -H "MCP-Protocol-Version: 2025-11-25" \
  -d '{"jsonrpc":"2.0","method":"notifications/initialized"}'

CALL_RESPONSE=$(curl -fsS -m 60 -X POST "$MCP_BASE_URL" \
  -H "Accept: application/json, text/event-stream" \
  -H "Content-Type: application/json" \
  -H "Mcp-Session-Id: $SID" \
  -H "MCP-Protocol-Version: 2025-11-25" \
  -d '{"jsonrpc":"2.0","id":2,"method":"tools/call","params":{"name":"ft_futures_eod_price","arguments":{"exchange":"DCE","symbol":"a2609","page":1,"page_size":5}}}')

check_mcp_response "$CALL_RESPONSE"
```

**Python（`mcp` SDK，自动握手管理 session）**：

```python
import asyncio

from mcp import ClientSession
from mcp.client.streamable_http import streamable_http_client


MCP_BASE_URL = "<MCP_BASE_URL>"


async def main():
    async with streamable_http_client(MCP_BASE_URL) as (read_stream, write_stream):
        async with ClientSession(read_stream, write_stream) as session:
            await session.initialize()
            result = await session.call_tool(
                'ft_futures_eod_price',
                {'exchange': 'DCE', 'symbol': 'a2609', 'page': 1, 'page_size': 5},
            )
            if result.is_error:
                raise RuntimeError(result.content[0].text)
            print(result.structured_content)
            print(result.content[0].text)


asyncio.run(main())
```

## 数据样例

| trade_date | symbol | open_price | highest_price | lowest_price | close_price | settlement_price | volume |
|------|------|------|------|------|------|------|------|
| 20260810 | a2609 | 4823.0000 | 4870.0000 | 4816.0000 | 4854.0000 | 4842.0000 | 176396 |
| 20260807 | a2609 | 4788.0000 | 4890.0000 | 4783.0000 | 4841.0000 | 4834.0000 | 325111 |
| 20260806 | a2609 | 4717.0000 | 4791.0000 | 4712.0000 | 4791.0000 | 4740.0000 | 236313 |
| ... | ... | ... | ... | ... | ... | ... | ... |
