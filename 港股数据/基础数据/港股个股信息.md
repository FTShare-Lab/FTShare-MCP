# 港股个股信息（MCP 工具 `ft_get_hk_basinfo`）

> **MCP 工具**：`ft_get_hk_basinfo`（category: `港股数据/基础数据`）。返回统一 MCP 输出：`structuredContent.metadata` + `structuredContent.data`；`content[0].text` 是同值的序列化 JSON，不额外返回 Markdown。输入参数 / 输出参数 / 数据样例见下文。
> 文中 `Response`、`items`、`records`、`code`、`message` 等名称仅为字段说明；MCP 对外固定为上述 `metadata/data`。

- 描述：查询单只港股的基础信息及最新市值、估值指标，包括上市状态、关联 A 股、股本、市值、PE、PS、PB、PCF 和股息率等。提示：`hk_code` 支持纯数字或 `.HK` 后缀，例如 `00700`、`00700.HK`。
- 数据范围：无时间维度，实时/最新快照（最新交易日视图）
- 单次限量：单条返回，无分页
- 提示：
  - 对外 v1 仅暴露 GET（v1 调 get_hk_stock_view；底层 v0 同时支持 GET+POST）。
  - `hk_code` 建议使用带 `.HK` 后缀的港股代码，如 `00700.HK`。
  - 数值字段为 `f64`（非 Decimal），与 F9 `market-cap-hk` 字段口径类似但类型不同。
  - 底层依赖日线静态文件，文件缺失返回 400。

## 输入参数

| 名称 | 类型 | 必选 | 描述 |
|------|------|------|------|
| hk_code | string | Y | 港股代码，如 `00700.HK` |

## 输出参数

> MCP 固定输出信封为 `structuredContent.metadata` + `structuredContent.data`；`content[0].text` 是与其同值的序列化 JSON，不是 Markdown。
>
> `items` / `records` / `code` / `message` 等传输字段不会直接出现在 MCP 结果中；分页与截断信息统一归入 `metadata`。

| MCP 字段 | 类型 | 必填 | 描述 |
|----------|------|------|------|
| metadata | object | Y | 契约版本、数据来源、工具名、业务口径、总量、分页、返回条数、截断状态及 warnings |
| data | array | Y | 归一化后的业务数据项 |

### data 业务字段

| 名称 | 类型 | 默认显示 | 描述 |
|------|------|---------|------|
| ashare_code | string | Y | 关联的 A 股代码 |
| ashare_num | f64 | Y | A 股股数 |
| board_type | string | Y | 板块类别 |
| bshare_num | f64 | Y | B 股股数 |
| de_listed_date | int32 | Y | 退市日期 |
| dividrt_lyr_rpt | f64 | Y | 静态股息率（报告期，%） |
| dividrt_ttm | f64 | Y | 滚动股息率（%） |
| forwd_pe | f64 | Y | 动态市盈率 |
| hshare_circ_mv | f64 | Y | 港股流通市值（元） |
| hshare_limit_num | f64 | Y | 港股限售股股数 |
| hshare_mv | f64 | Y | 港股市值（元） |
| hshare_num | f64 | Y | 港股股数 |
| listed_date | int32 | Y | 上市日期 |
| nonhshare_num | f64 | Y | 非港股股数 |
| pb | f64 | Y | 市净率 |
| pcf | f64 | Y | 市现率（经营现金流量净额） |
| pcf_ttm | f64 | Y | 滚动市现率（经营现金流量净额） |
| pe_lyr | f64 | Y | 静态市盈率 |
| pe_ttm | f64 | Y | 滚动市盈率 |
| ps | f64 | Y | 市销率 |
| ps_ttm | f64 | Y | 滚动市销率 |
| security_name | string | Y | 证券简称 |
| status | string | Y | 证券状态 |
| total_mv | f64 | Y | 总市值 |
| total_share_num | f64 | Y | 总股本 |
| trade_code | string | Y | 交易代码 |
| trade_date | int32 | Y | 交易日期 |

## 调用方法（MCP）

> MCP Streamable HTTP 要求**先 initialize 拿 `Mcp-Session-Id`，发送 `notifications/initialized`，再 `tools/call`**，后续请求同时带该 Session ID 和协商后的 `MCP-Protocol-Version`。返回统一 `metadata/data` 结构化输出；`content[0].text` 为同值 JSON 文本，不额外返回 Markdown。

**curl**：

```bash
set -euo pipefail

MCP_BASE_URL="<MCP_BASE_URL>"

check_mcp_response() {
  local response=$1
  printf '%s\n' "$response"
  if printf '%s\n' "$response" | grep -Eq '"isError"[[:space:]]*:[[:space:]]*true|"error"[[:space:]]*:[[:space:]]*\{'; then
    return 1
  fi
}

SID=$(curl -fsS -m 60 -D - -o /dev/null -X POST "$MCP_BASE_URL" \
  -H "Accept: application/json, text/event-stream" \
  -H "Content-Type: application/json" \
  -d '{"jsonrpc":"2.0","id":1,"method":"initialize","params":{"protocolVersion":"2025-11-25","capabilities":{},"clientInfo":{"name":"ftshare-doc-example","version":"1.0.0"}}}' \
  | awk 'tolower($1)=="mcp-session-id:" {print $2}' \
  | tr -d '\r')

if [ -z "$SID" ]; then
  printf '%s\n' 'initialize 未返回 Mcp-Session-Id' >&2
  exit 1
fi

curl -fsS -m 60 -o /dev/null -X POST "$MCP_BASE_URL" \
  -H "Accept: application/json, text/event-stream" \
  -H "Content-Type: application/json" \
  -H "Mcp-Session-Id: $SID" \
  -H "MCP-Protocol-Version: 2025-11-25" \
  -d '{"jsonrpc":"2.0","method":"notifications/initialized"}'

CALL_RESPONSE=$(curl -fsS -m 60 -X POST "$MCP_BASE_URL" \
  -H "Accept: application/json, text/event-stream" \
  -H "Content-Type: application/json" \
  -H "Mcp-Session-Id: $SID" \
  -H "MCP-Protocol-Version: 2025-11-25" \
  -d '{"jsonrpc":"2.0","id":2,"method":"tools/call","params":{"name":"ft_get_hk_basinfo","arguments":{"hk_code":"00700.HK"}}}')

check_mcp_response "$CALL_RESPONSE"
```

**Python（`mcp` SDK，自动握手管理 session）**：

```python
import asyncio

from mcp import ClientSession
from mcp.client.streamable_http import streamable_http_client


MCP_BASE_URL = "<MCP_BASE_URL>"


async def main():
    async with streamable_http_client(MCP_BASE_URL) as (read_stream, write_stream):
        async with ClientSession(read_stream, write_stream) as session:
            await session.initialize()
            result = await session.call_tool(
                'ft_get_hk_basinfo',
                {'hk_code': '00700.HK'},
            )
            if result.is_error:
                raise RuntimeError(result.content[0].text)
            print(result.structured_content)
            print(result.content[0].text)


asyncio.run(main())
```

## 数据样例

| trade_code | security_name | board_type | total_mv | pe_ttm | pb | ... |
|------|------|------|------|------|------|------|
| 00700.HK | 腾讯控股 | 1 | 4348807144693.2 | 16.3316 | 3.4051 | null |
| ... | ... | ... | ... | ... | ... | ... |
