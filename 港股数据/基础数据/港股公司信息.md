# 港股公司信息（MCP 工具 `ft_get_company_hk`）

> **MCP 工具**：`ft_get_company_hk`（category: `港股数据/基础数据`）。返回统一 MCP 输出：`structuredContent.metadata` + `structuredContent.data`；`content[0].text` 是同值的序列化 JSON，不额外返回 Markdown。输入参数 / 输出参数 / 数据样例见下文。
> 文中 `Response`、`items`、`records`、`code`、`message` 等名称仅为字段说明；MCP 对外固定为上述 `metadata/data`。

- 描述：按 `trade_code` 查询港股公司介绍，返回单条 （公司名/注册信息/法人/地址/简介/主营业务/员工数/联系方式等）。无分页，`trade_code` 必填，空值报 `trade_code is required`。提示：`trade_code` 必填，支持纯数字或带 `.HK` 后缀格式；注册资本单位为元。
- 数据范围：无时间维度，实时/最新快照（公司档案最新信息）
- 单次限量：单条返回，无分页
- 提示：
  - 对外 v1 仅暴露 GET（底层 v0 同时支持 GET+POST）。
  - `trade_code` 必填，支持纯数字或带 `.HK` 后缀格式。
  - 注册资本单位为元。

## 输入参数

| 名称 | 类型 | 必选 | 描述 |
|------|------|------|------|
| trade_code | string | Y | 港股交易代码 |

## 输出参数

> MCP 固定输出信封为 `structuredContent.metadata` + `structuredContent.data`；`content[0].text` 是与其同值的序列化 JSON，不是 Markdown。
>
> `items` / `records` / `code` / `message` 等传输字段不会直接出现在 MCP 结果中；分页与截断信息统一归入 `metadata`。

| MCP 字段 | 类型 | 必填 | 描述 |
|----------|------|------|------|
| metadata | object | Y | 契约版本、数据来源、工具名、业务口径、总量、分页、返回条数、截断状态及 warnings |
| data | array | Y | 归一化后的业务数据项；元素字段见下方 |

### data 业务字段

单条对象（CompanyHkResponse）：

| 名称 | 类型 | 默认显示 | 描述 |
|------|------|---------|------|
| security_name | string | Y | 证券简称 |
| trade_code | string | Y | 交易代码 |
| company_name | string | Y | 公司名称 |
| company_name_en | string | Y | 公司英文名称 |
| reg_date | int32 | Y | 成立日期 |
| business_status | string | Y | 经营状态 |
| reg_money | decimal | Y | 注册资本（元） |
| currencyid | int32 | Y | 币种 ID |
| currency | string | Y | 币种 |
| legal | string | Y | 法人代表 |
| reg_address | string | Y | 注册地址 |
| office_address | string | Y | 办公地址 |
| company_prof | string | Y | 公司简介 |
| business_main | string | Y | 主营业务 |
| staff_num | int32 | Y | 员工人数 |
| website | string | Y | 公司网址 |
| phone | string | Y | 公司电话 |
| fax | string | Y | 公司传真 |
| enterprise_type | string | Y | 企业类型 |

## 调用方法（MCP）

> MCP 工具名 `ft_get_company_hk`。MCP Streamable HTTP 要求**先 initialize 拿 `Mcp-Session-Id`，发送 `notifications/initialized`，再 `tools/call`**，后续请求同时带该 Session ID 和协商后的 `MCP-Protocol-Version`。返回统一 `metadata/data` 结构化输出；`content[0].text` 为同值 JSON 文本，不额外返回 Markdown。

**curl**：

```bash
set -euo pipefail

MCP_BASE_URL="<MCP_BASE_URL>"

check_mcp_response() {
  local response=$1
  printf '%s\n' "$response"
  # MCP 业务与协议错误仍可能使用 HTTP 200，必须检查 JSON-RPC 响应。
  if printf '%s\n' "$response" | grep -Eq '"isError"[[:space:]]*:[[:space:]]*true|"error"[[:space:]]*:[[:space:]]*\{'; then
    return 1
  fi
}

SID=$(curl -fsS -m 60 -D - -o /dev/null -X POST "$MCP_BASE_URL" \
  -H "Accept: application/json, text/event-stream" \
  -H "Content-Type: application/json" \
  -d '{"jsonrpc":"2.0","id":1,"method":"initialize","params":{"protocolVersion":"2025-11-25","capabilities":{},"clientInfo":{"name":"ftshare-doc-example","version":"1.0.0"}}}' \
  | awk 'tolower($1)=="mcp-session-id:" {print $2}' \
  | tr -d '\r')

if [ -z "$SID" ]; then
  printf '%s\n' 'initialize 未返回 Mcp-Session-Id' >&2
  exit 1
fi

curl -fsS -m 60 -o /dev/null -X POST "$MCP_BASE_URL" \
  -H "Accept: application/json, text/event-stream" \
  -H "Content-Type: application/json" \
  -H "Mcp-Session-Id: $SID" \
  -H "MCP-Protocol-Version: 2025-11-25" \
  -d '{"jsonrpc":"2.0","method":"notifications/initialized"}'

CALL_RESPONSE=$(curl -fsS -m 60 -X POST "$MCP_BASE_URL" \
  -H "Accept: application/json, text/event-stream" \
  -H "Content-Type: application/json" \
  -H "Mcp-Session-Id: $SID" \
  -H "MCP-Protocol-Version: 2025-11-25" \
  -d '{"jsonrpc":"2.0","id":2,"method":"tools/call","params":{"name":"ft_get_company_hk","arguments":{"trade_code":"00700.HK"}}}')

check_mcp_response "$CALL_RESPONSE"
```

**Python（`mcp` SDK，自动握手管理 session）**：

```python
import asyncio

from mcp import ClientSession
from mcp.client.streamable_http import streamable_http_client


MCP_BASE_URL = "<MCP_BASE_URL>"


async def main():
    async with streamable_http_client(MCP_BASE_URL) as (read_stream, write_stream):
        async with ClientSession(read_stream, write_stream) as session:
            await session.initialize()
            result = await session.call_tool(
                'ft_get_company_hk',
                {'trade_code': '00700.HK'},
            )
            if result.is_error:
                raise RuntimeError(result.content[0].text)
            print(result.structured_content)
            print(result.content[0].text)


asyncio.run(main())
```

## 数据样例

| trade_code | security_name | company_name | legal | reg_money | ... |
|------------|---------------|--------------|-------|-----------|-----|
| ... | ... | ... | ... | ... | ... |

> 示例调用已验证通过，真实样例可从 `structuredContent.data` 查看。
