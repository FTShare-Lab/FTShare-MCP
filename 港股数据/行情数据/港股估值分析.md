# 港股估值分析（MCP 工具 `ft_get_hk_valuatnanalyd`）

> **MCP 工具**：`ft_get_hk_valuatnanalyd`（category: `港股数据/行情数据`）。返回统一 MCP 输出：`structuredContent.metadata` + `structuredContent.data`；`content[0].text` 是同值的序列化 JSON，不额外返回 Markdown。输入参数 / 输出参数 / 数据样例见下文。
> 文中 `Response`、`items`、`records`、`code`、`message` 等名称仅为字段说明；MCP 对外固定为上述 `metadata/data`。

- 描述：查询港股日度估值序列，包含收盘价、成交量、成交额、波动率、港股股数、市值、换手率、市盈率、市销率、市净率、市现率和股息率；可按交易代码查询指定个股的全部交易日，未指定交易代码时查询全市场。
- 数据范围：按交易日查询
- 单次限量：分页，默认 `page=1` / `page_size=20`，`page_size` 上限 500
- 提示：
  - `trade_code` 可选：留空分页查全市场；指定则查该股全部交易日估值序列。
  - 本接口分页返回按日估值时间序列。
  - `update_time` 为 int64 整数时间戳（格式如 `20251202214424`）。

## 输入参数

| 名称 | 类型 | 必选 | 描述 |
|------|------|------|------|
| trade_code | string | N | 港股代码（支持 `700` / `00700.HK`）；留空查全市场 |
| page | int | N | 页码（从 1 开始），默认 1 |
| page_size | int | N | 每页数量，默认 20，上限 500 |

## 输出参数

> MCP 固定输出信封为 `structuredContent.metadata` + `structuredContent.data`；`content[0].text` 是与其同值的序列化 JSON，不是 Markdown。
>
> `items` / `records` / `code` / `message` 等传输字段不会直接出现在 MCP 结果中；分页与截断信息统一归入 `metadata`。

| MCP 字段 | 类型 | 必填 | 描述 |
|----------|------|------|------|
| metadata | object | Y | 契约版本、数据来源、工具名、业务口径、总量、分页、返回条数、截断状态及 warnings |
| data | array | Y | 归一化后的业务数据项 |

### data 业务字段

| 名称 | 类型 | 默认显示 | 描述 |
|------|------|---------|------|
| amount | decimal | Y | 成交额（元） |
| ashare_num | decimal | Y | A 股股数 |
| bshare_num | decimal | Y | B 股股数 |
| dividrt_lyr_rpt | decimal | Y | 静态股息率（报告期，%） |
| dividrt_ttm | decimal | Y | 滚动股息率（%） |
| forwd_pe | decimal | Y | 动态市盈率 |
| hshare_circ_mv | decimal | Y | 港股流通市值（元） |
| hshare_limit_num | decimal | Y | 港股限售股股数 |
| hshare_mv | decimal | Y | 港股市值（元） |
| hshare_num | decimal | Y | 港股股数 |
| id | int64 | Y | 记录 ID |
| minprice_chg | decimal | Y | 最小变动价格 |
| nonhshare_num | decimal | Y | 非港股股数 |
| op_mode | int32 | Y | 操作模式 |
| pb | decimal | Y | 市净率 |
| pcf | decimal | Y | 市现率（经营现金流量净额） |
| pcf_ttm | decimal | Y | 滚动市现率（经营现金流量净额） |
| pe_lyr | decimal | Y | 静态市盈率 |
| pe_ttm | decimal | Y | 滚动市盈率 |
| price_close | decimal | Y | 收盘价 |
| ps | decimal | Y | 市销率 |
| ps_ttm | decimal | Y | 滚动市销率 |
| security_id | string | Y | 证券 ID |
| security_name | string | Y | 证券简称 |
| trade_code | string | Y | 交易代码 |
| trade_date | int32 | Y | 计算日期 YYYYMMDD |
| turnover_rt | decimal | Y | 换手率（%） |
| update_time | int64 | Y | 更新时间（格式如 20251202214424） |
| volatility_y | decimal | Y | 波动率（一年，%） |
| volume | decimal | Y | 成交股数 |

## 调用方法（MCP）

> MCP Streamable HTTP 要求**先 initialize 拿 `Mcp-Session-Id`，发送 `notifications/initialized`，再 `tools/call`**，后续请求同时带该 Session ID 和协商后的 `MCP-Protocol-Version`。返回统一 `metadata/data` 结构化输出；`content[0].text` 为同值 JSON 文本，不额外返回 Markdown。

**curl**：

```bash
set -euo pipefail

MCP_BASE_URL="<MCP_BASE_URL>"

check_mcp_response() {
  local response=$1
  printf '%s\n' "$response"
  if printf '%s\n' "$response" | grep -Eq '"isError"[[:space:]]*:[[:space:]]*true|"error"[[:space:]]*:[[:space:]]*\{'; then
    return 1
  fi
}

SID=$(curl -fsS -m 60 -D - -o /dev/null -X POST "$MCP_BASE_URL" \
  -H "Accept: application/json, text/event-stream" \
  -H "Content-Type: application/json" \
  -d '{"jsonrpc":"2.0","id":1,"method":"initialize","params":{"protocolVersion":"2025-11-25","capabilities":{},"clientInfo":{"name":"ftshare-doc-example","version":"1.0.0"}}}' \
  | awk 'tolower($1)=="mcp-session-id:" {print $2}' \
  | tr -d '\r')

if [ -z "$SID" ]; then
  printf '%s\n' 'initialize 未返回 Mcp-Session-Id' >&2
  exit 1
fi

curl -fsS -m 60 -o /dev/null -X POST "$MCP_BASE_URL" \
  -H "Accept: application/json, text/event-stream" \
  -H "Content-Type: application/json" \
  -H "Mcp-Session-Id: $SID" \
  -H "MCP-Protocol-Version: 2025-11-25" \
  -d '{"jsonrpc":"2.0","method":"notifications/initialized"}'

CALL_RESPONSE=$(curl -fsS -m 60 -X POST "$MCP_BASE_URL" \
  -H "Accept: application/json, text/event-stream" \
  -H "Content-Type: application/json" \
  -H "Mcp-Session-Id: $SID" \
  -H "MCP-Protocol-Version: 2025-11-25" \
  -d '{"jsonrpc":"2.0","id":2,"method":"tools/call","params":{"name":"ft_get_hk_valuatnanalyd","arguments":{"page":1,"page_size":2}}}')

check_mcp_response "$CALL_RESPONSE"
```

**Python（`mcp` SDK，自动握手管理 session）**：

```python
import asyncio

from mcp import ClientSession
from mcp.client.streamable_http import streamable_http_client


MCP_BASE_URL = "<MCP_BASE_URL>"


async def main():
    async with streamable_http_client(MCP_BASE_URL) as (read_stream, write_stream):
        async with ClientSession(read_stream, write_stream) as session:
            await session.initialize()
            result = await session.call_tool(
                'ft_get_hk_valuatnanalyd',
                {'page': 1, 'page_size': 2},
            )
            if result.is_error:
                raise RuntimeError(result.content[0].text)
            print(result.structured_content)
            print(result.content[0].text)


asyncio.run(main())
```

## 数据样例

| trade_code | security_name | trade_date | price_close | volatility_y | hshare_mv | hshare_circ_mv |
|------|------|------|------|------|------|------|
| 08577.HK | 耀星科技集团 | 20260809 | 1.46 | 0 | 82132698.58 | 82132698.58 |
| 02990.HK | 鼎立资本(旧) | 20260809 | 0.465 | 55.9736 | 93650379.69 | 93650379.69 |
| ... | ... | ... | ... | ... | ... | ... |
