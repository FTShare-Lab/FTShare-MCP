# 港股利润表（银行业）（MCP 工具 `ft_hk_income_bank`）

> **MCP 工具**：`ft_hk_income_bank`（category: `港股数据/财务数据`）。返回统一 MCP 输出：`structuredContent.metadata` + `structuredContent.data`；`content[0].text` 是同值的序列化 JSON，不额外返回 Markdown。输入参数 / 输出参数 / 数据样例见下文。
> 文中 `Response`、`items`、`records`、`code`、`message` 等名称仅为字段说明；MCP 对外固定为上述 `metadata/data`。

- 描述：查询港股银行业利润表及其特有利润科目；至少提供交易代码、报告期范围或年份与报告类型组合之一，交易代码可单独查询该股全部报告期，年份和报告类型必须同时提供且仅覆盖最近 3 年，各筛选条件可组合使用。
- 数据范围：按报告期查询
- 单次限量：分页，默认 `page=1` / `page_size=20`，`page_size` 上限 500；`year` 限制最近 3 年
- 提示：
  - 必须至少传一个过滤条件（`trade_code`、`start_date`、`end_date`、或 `year`+`report_type`）。
  - `year` + `report_type` 需同时传入，`year` 仅支持 2024-2026。
  - `trade_code` 支持 1-5 位纯数字或带 .HK 后缀（如 `700` 或 `00700.HK`），空字符串按未传处理。
  - 银行表部分字段名较短（如 `operat_prof`/`affilcom_prof`），与一般企业表拼写不同。

## 输入参数

| 名称 | 类型 | 必选 | 描述 |
|------|------|------|------|
| trade_code | string | N | 港股代码，支持 1—5 位纯数字或带 .HK 后缀；空字符串按未传处理 |
| year | integer | N | 报告期年份，仅支持最近 3 年（2024—2026），需与 report_type 同时传入 |
| report_type | string | N | 报告类型：annual（年报）/ semi（半年报），需配 `year` |
| start_date | integer | N | 起始截止日期 YYYYMMDD，筛选 `end_date >= 此值` |
| end_date | integer | N | 结束截止日期 YYYYMMDD，筛选 `end_date <= 此值` |
| page | integer | N | 页码，默认 1 |
| page_size | integer | N | 每页数量，默认 20，上限 500 |

## 输出参数

> MCP 固定输出信封为 `structuredContent.metadata` + `structuredContent.data`；`content[0].text` 是与其同值的序列化 JSON，不是 Markdown。
>
> `items` / `records` / `code` / `message` 等传输字段不会直接出现在 MCP 结果中；分页与截断信息统一归入 `metadata`。

| MCP 字段 | 类型 | 必填 | 描述 |
|----------|------|------|------|
| metadata | object | Y | 契约版本、数据来源、工具名、业务口径、总量、分页、返回条数、截断状态及 warnings |
| data | array | Y | 归一化后的业务数据项 |

### data 业务字段

| 名称 | 类型 | 默认显示 | 描述 |
|------|------|---------|------|
| affilcom_prof | string | N | 联营公司利润 |
| currency | string | Y | 币种 |
| dividend_income | string | N | 股息收入 |
| dividend_pershare | string | N | 每股股息 |
| earning_aftax | string | N | 税后利润 |
| earning_beftax | string | N | 税前利润 |
| end_date | string | Y | 报告期 YYYY-MM-DD |
| eps | string | N | 稀释每股收益 |
| eps_basic | string | N | 基本每股收益 |
| fiscal_y | int32 | Y | 财年 |
| int_expense | string | N | 利息支出 |
| int_income | string | N | 利息收入 |
| jointventr_prof | string | N | 合资企业利润 |
| mino_n_profit | string | N | 少数股东利润 |
| net_int_income | string | N | 净利息收入 |
| netexchg_amt | string | N | 净汇兑收益 |
| netifins_frvalue | string | N | 净保险金融工具公允价值变动 |
| netopnhdg_income | string | N | 净开放式对冲收入 |
| netrent_income | string | N | 净租金收入 |
| netserv_income | string | N | 净服务收入（手续费及佣金净收入） |
| nettrans_income | string | N | 净交易收益 |
| opeprof_afdedimp | string | N | 扣减减值损失后营业利润 |
| opeprof_befded_imp | string | N | 扣减减值损失前营业利润 |
| operat_prof | string | N | 营业利润（注意银行表字段名较短） |
| operat_revenue | string | N | 营业收入 |
| operatpayt_bd | string | N | 运营支付 |
| operndadmin_exp | string | N | 运营及管理费用 |
| prof_sharehlders | string | N | 归属股东利润 |
| publish_date | string | Y | 发布日期 YYYY-MM-DD |
| report_type | string | Y | 报告类型 |
| rpt_period | string | Y | 报告期 |
| secndinvest_income | string | N | 次级投资收入 |
| security_name | string | Y | 证券简称 |
| serv_charge | string | N | 服务费用 |
| serv_income | string | N | 服务收入 |
| tax | string | N | 所得税 |
| trade_code | string | Y | 港股代码 |

## 调用方法（MCP）

> MCP Streamable HTTP 要求**先 initialize 拿 `Mcp-Session-Id`，发送 `notifications/initialized`，再 `tools/call`**，后续请求同时带该 Session ID 和协商后的 `MCP-Protocol-Version`。返回统一 `metadata/data` 结构化输出；`content[0].text` 为同值 JSON 文本，不额外返回 Markdown。

**curl**：

```bash
set -euo pipefail

MCP_BASE_URL="<MCP_BASE_URL>"

check_mcp_response() {
  local response=$1
  printf '%s\n' "$response"
  if printf '%s\n' "$response" | grep -Eq '"isError"[[:space:]]*:[[:space:]]*true|"error"[[:space:]]*:[[:space:]]*\{'; then
    return 1
  fi
}

SID=$(curl -fsS -m 60 -D - -o /dev/null -X POST "$MCP_BASE_URL" \
  -H "Accept: application/json, text/event-stream" \
  -H "Content-Type: application/json" \
  -d '{"jsonrpc":"2.0","id":1,"method":"initialize","params":{"protocolVersion":"2025-11-25","capabilities":{},"clientInfo":{"name":"ftshare-doc-example","version":"1.0.0"}}}' \
  | awk 'tolower($1)=="mcp-session-id:" {print $2}' \
  | tr -d '\r')

if [ -z "$SID" ]; then
  printf '%s\n' 'initialize 未返回 Mcp-Session-Id' >&2
  exit 1
fi

curl -fsS -m 60 -o /dev/null -X POST "$MCP_BASE_URL" \
  -H "Accept: application/json, text/event-stream" \
  -H "Content-Type: application/json" \
  -H "Mcp-Session-Id: $SID" \
  -H "MCP-Protocol-Version: 2025-11-25" \
  -d '{"jsonrpc":"2.0","method":"notifications/initialized"}'

CALL_RESPONSE=$(curl -fsS -m 60 -X POST "$MCP_BASE_URL" \
  -H "Accept: application/json, text/event-stream" \
  -H "Content-Type: application/json" \
  -H "Mcp-Session-Id: $SID" \
  -H "MCP-Protocol-Version: 2025-11-25" \
  -d '{"jsonrpc":"2.0","id":2,"method":"tools/call","params":{"name":"ft_hk_income_bank","arguments":{"trade_code":"03988.HK","page":1,"page_size":2}}}')

check_mcp_response "$CALL_RESPONSE"
```

**Python（`mcp` SDK，自动握手管理 session）**：

```python
import asyncio

from mcp import ClientSession
from mcp.client.streamable_http import streamable_http_client


MCP_BASE_URL = "<MCP_BASE_URL>"


async def main():
    async with streamable_http_client(MCP_BASE_URL) as (read_stream, write_stream):
        async with ClientSession(read_stream, write_stream) as session:
            await session.initialize()
            result = await session.call_tool(
                'ft_hk_income_bank',
                {"trade_code": "03988.HK", "page": 1, "page_size": 2},
            )
            if result.is_error:
                raise RuntimeError(result.content[0].text)
            print(result.structured_content)
            print(result.content[0].text)


asyncio.run(main())
```

## 数据样例

| trade_code | security_name | end_date | currency | net_int_income | int_income | earning_aftax | eps_basic |
| ------ | ------ | ------ | ------ | ------ | ------ | ------ | ------ |
| 03988.HK | 中国银行 | 2026-03-31 | 人民币 | 116143000000.0000 | 247655000000.0000 | 60959000000.0000 | 0.1800 |
| 03988.HK | 中国银行 | 2025-12-31 | 人民币 | 440705000000.0000 | 1000907000000.0000 | 257936000000.0000 | 0.7400 |
| ... | ... | ... | ... | ... | ... | ... | ... |
