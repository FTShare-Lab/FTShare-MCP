# 港股现金流量表（MCP 工具 `ft_hk_cashflow`）

> **MCP 工具**：`ft_hk_cashflow`（category: `港股数据/财务数据`）。返回统一 MCP 输出：`structuredContent.metadata` + `structuredContent.data`；`content[0].text` 是同值的序列化 JSON，不额外返回 Markdown。输入参数 / 输出参数 / 数据样例见下文。
> 文中 `Response`、`items`、`records`、`code`、`message` 等名称仅为字段说明；MCP 对外固定为上述 `metadata/data`。

- 描述：查询港股现金流量表，包含经营、投资、融资活动及现金流汇总科目；至少提供股票代码、报告期范围或年份与报告类型组合之一，股票代码可单独查询该股全部报告期，年份和报告类型必须同时提供且仅覆盖最近 3 年，各筛选条件可组合使用。
- 数据范围：按报告期查询
- 单次限量：分页，默认 `page=1` / `page_size=20`，`page_size` 上限 500；`year` 限制最近 3 年
- 提示：
  - 必须至少传一个过滤条件（`stock_code`、`start_date`、`end_date`、或 `year`+`report_type`）。
  - 每个报告期一行，所有现金流科目横向展开；下表仅列元数据 + 各类代表字段，完整字段以输出参数表为准。
  - `report_type` 取值：`annual`（年报）/ `semi`（半年报）。

## 输入参数

| 名称 | 类型 | 必选 | 描述 |
|------|------|------|------|
| stock_code | string | N | 港股代码（如 `00700.HK`） |
| year | integer | N | 报告期年份（如 2024），按 `end_date` 日历年过滤，需配 `report_type` |
| report_type | string | N | 报告类型：annual（年报）/ semi（半年报），需配 `year` |
| start_date | integer | N | 报告期范围下界 YYYYMMDD（含），过滤 `end_date >= 此值` |
| end_date | integer | N | 报告期范围上界 YYYYMMDD（含），过滤 `end_date <= 此值` |
| page | integer | N | 页码，默认 1 |
| page_size | integer | N | 每页数量，默认 20，上限 500 |

## 输出参数

> MCP 固定输出信封为 `structuredContent.metadata` + `structuredContent.data`；`content[0].text` 是与其同值的序列化 JSON，不是 Markdown。
>
> `items` / `records` / `code` / `message` 等传输字段不会直接出现在 MCP 结果中；分页与截断信息统一归入 `metadata`。

| MCP 字段 | 类型 | 必填 | 描述 |
|----------|------|------|------|
| metadata | object | Y | 契约版本、数据来源、工具名、业务口径、总量、分页、返回条数、截断状态及 warnings |
| data | array | Y | 归一化后的业务数据项 |

### data 业务字段

| 名称 | 类型 | 默认显示 | 描述 |
|------|------|---------|------|
| absorbinv_income | string | N |  |
| account_std | string | Y | 会计准则 |
| accpayable_chg | string | N |  |
| accreceiv_chg | string | N | 应收账款变动（经营活动-营运资金变动） |
| accrepayable_chg | string | N |  |
| adjit_opebusi | string | N |  |
| adjitems_fin | string | N |  |
| adjitmang_adj | string | N |  |
| adjitwcap_chg | string | N |  |
| advpayment_chg | string | N |  |
| advreceipts_chg | string | N |  |
| affcmp_profndlos | string | N |  |
| bank_dept | string | N |  |
| bankdeposit_chg | string | N |  |
| bbacksfassets_chg | string | N |  |
| begin_date | string | Y | 期初日期 YYYY-MM-DD |
| beginper_cash | string | N |  |
| bfaafvalonpl_chg | string | N |  |
| bflafvalonpl_chg | string | N |  |
| borfromcb_chg | string | N |  |
| cash_cashequiv | string | N | 现金及现金等价物期末余额（汇总） |
| cash_endper | string | N |  |
| cashndbank_bal | string | N |  |
| cashndoth_bffin | string | N |  |
| cashrece_ope | string | N |  |
| cinfv_derfininst | string | N |  |
| cinfv_invpropert | string | N |  |
| cinfv_othast | string | N |  |
| cninctax_paid | string | N |  |
| company_type | string | Y | 公司类型 |
| currency | string | Y | 币种 |
| cusdeposits_chg | string | N |  |
| deposit_chg | string | N |  |
| depr_amortzon | string | N |  |
| depreciation | string | N | 折旧（经营活动-利润调整） |
| derfininst_chg | string | N |  |
| dev_avafsalinv | string | N |  |
| dev_goodwill | string | N |  |
| dev_inventories | string | N |  |
| dev_others | string | N |  |
| dev_proplequip | string | N |  |
| dev_traderece | string | N |  |
| dis_invtcash | string | N |  |
| dividincome_ad | string | N |  |
| dividpaid_bo | string | N |  |
| dividpaid_fb | string | N | 分配股利支付的现金（融资活动） |
| dividrec_bo | string | N |  |
| dividrec_ib | string | N |  |
| earning_bftax | string | N | 税前利润（经营活动-利润调整） |
| effect_rate | string | N |  |
| end_date | string | Y | 报告期截止日 YYYY-MM-DD（即 period） |
| exchg_proloss | string | N |  |
| expit_opebusi | string | N |  |
| expitmang_adj | string | N |  |
| expitopea_chg | string | N |  |
| expitopel_chg | string | N |  |
| faafvalonpl_chg | string | N |  |
| fin_expitems | string | N |  |
| financial_cost | string | N |  |
| fiscal_y | int32 | Y | 财年 |
| fixedast_deprectn | string | N |  |
| flafvalonpl_chg | string | N |  |
| hkproftax_paid | string | N |  |
| impairmt_provin | string | N |  |
| insucontliab_chg | string | N |  |
| insureceiv_chg | string | N |  |
| intangast_amort | string | N |  |
| interpaid_bo | string | N |  |
| interpaid_cb | string | N |  |
| interpaid_fb | string | N |  |
| interrec_bo | string | N |  |
| interrec_cb | string | N |  |
| interrec_ib | string | N |  |
| intertexp_ad | string | N |  |
| intertincome_ad | string | N | 已收利息调整（经营活动） |
| inventy_chg | string | N | 存货变动（经营活动-营运资金变动） |
| invest_profndlos | string | N |  |
| investprop_da | string | N |  |
| invt_adjitems | string | N |  |
| invt_expitems | string | N |  |
| invt_paycash | string | N |  |
| issexpapay_redsec | string | N |  |
| issue_bonds | string | N |  |
| issue_shares | string | N |  |
| loanreceiv_chg | string | N |  |
| loansaadvan_chg | string | N |  |
| net_cash | string | N | 现金及现金等价物净增加额（汇总） |
| net_invbscflow | string | N | 投资活动现金流量净额（投资活动） |
| net_opecflow | string | N | 经营活动现金流量净额（经营活动-现金及税） |
| netcash_bffin | string | N |  |
| netcashf_fin | string | N | 融资活动现金流量净额（融资活动） |
| new_loan | string | N | 新增借款（融资活动） |
| opepro_bfchg_inopecap | string | N |  |
| oth_depdividersal | string | N |  |
| oth_taxes | string | N |  |
| othit_affect_nc | string | N |  |
| othit_fin | string | N |  |
| othit_period | string | N |  |
| pledgeddep_chg | string | N |  |
| prodisp_affceq | string | N |  |
| prodisp_afsaleinv | string | N |  |
| prodisp_ast | string | N |  |
| prodisp_othast | string | N |  |
| prodisp_propleq | string | N |  |
| prouddevel_chg | string | N |  |
| publish_date | string | Y | 发布日期 YYYY-MM-DD |
| pur_affcomp | string | N |  |
| pur_capasnt | string | N |  |
| pur_intast | string | N | 购建无形资产（投资活动） |
| refund | string | N |  |
| report_type | string | Y | 报告类型 |
| restrcash_chg | string | N |  |
| revaltn_surplus | string | N |  |
| rpt_period | string | Y | 报告期 |
| speitwcap_chg | string | N |  |
| stock_code | string | Y | 港股代码 |
| stock_name | string | Y | 证券简称 |
| unrl_exchgincome | string | N |  |
| usufructast_da | string | N |  |
| vend_affcomp | string | N |  |
| vend_capasnt | string | N |  |
| vend_intast | string | N |  |

## 调用方法（MCP）

> MCP Streamable HTTP 要求**先 initialize 拿 `Mcp-Session-Id`，发送 `notifications/initialized`，再 `tools/call`**，后续请求同时带该 Session ID 和协商后的 `MCP-Protocol-Version`。返回统一 `metadata/data` 结构化输出；`content[0].text` 为同值 JSON 文本，不额外返回 Markdown。

**curl**：

```bash
set -euo pipefail

MCP_BASE_URL="<MCP_BASE_URL>"

check_mcp_response() {
  local response=$1
  printf '%s\n' "$response"
  if printf '%s\n' "$response" | grep -Eq '"isError"[[:space:]]*:[[:space:]]*true|"error"[[:space:]]*:[[:space:]]*\{'; then
    return 1
  fi
}

SID=$(curl -fsS -m 60 -D - -o /dev/null -X POST "$MCP_BASE_URL" \
  -H "Accept: application/json, text/event-stream" \
  -H "Content-Type: application/json" \
  -d '{"jsonrpc":"2.0","id":1,"method":"initialize","params":{"protocolVersion":"2025-11-25","capabilities":{},"clientInfo":{"name":"ftshare-doc-example","version":"1.0.0"}}}' \
  | awk 'tolower($1)=="mcp-session-id:" {print $2}' \
  | tr -d '\r')

if [ -z "$SID" ]; then
  printf '%s\n' 'initialize 未返回 Mcp-Session-Id' >&2
  exit 1
fi

curl -fsS -m 60 -o /dev/null -X POST "$MCP_BASE_URL" \
  -H "Accept: application/json, text/event-stream" \
  -H "Content-Type: application/json" \
  -H "Mcp-Session-Id: $SID" \
  -H "MCP-Protocol-Version: 2025-11-25" \
  -d '{"jsonrpc":"2.0","method":"notifications/initialized"}'

CALL_RESPONSE=$(curl -fsS -m 60 -X POST "$MCP_BASE_URL" \
  -H "Accept: application/json, text/event-stream" \
  -H "Content-Type: application/json" \
  -H "Mcp-Session-Id: $SID" \
  -H "MCP-Protocol-Version: 2025-11-25" \
  -d '{"jsonrpc":"2.0","id":2,"method":"tools/call","params":{"name":"ft_hk_cashflow","arguments":{"stock_code":"00700.HK","page":1,"page_size":2}}}')

check_mcp_response "$CALL_RESPONSE"
```

**Python（`mcp` SDK，自动握手管理 session）**：

```python
import asyncio

from mcp import ClientSession
from mcp.client.streamable_http import streamable_http_client


MCP_BASE_URL = "<MCP_BASE_URL>"


async def main():
    async with streamable_http_client(MCP_BASE_URL) as (read_stream, write_stream):
        async with ClientSession(read_stream, write_stream) as session:
            await session.initialize()
            result = await session.call_tool(
                'ft_hk_cashflow',
                {"stock_code": "00700.HK", "page": 1, "page_size": 2},
            )
            if result.is_error:
                raise RuntimeError(result.content[0].text)
            print(result.structured_content)
            print(result.content[0].text)


asyncio.run(main())
```

## 数据样例

| stock_code | stock_name | end_date | report_type | currency | earning_bftax | intertincome_ad |
| ------ | ------ | ------ | ------ | ------ | ------ | ------ |
| 00700.HK | 腾讯控股 | 2026-03-31 | 合并未调整 | 人民币 | 0.0000 | 0.0000 |
| 00700.HK | 腾讯控股 | 2025-12-31 | 合并未调整 | 人民币 | 229801000000.0000 | -16909000000.0000 |
| ... | ... | ... | ... | ... | ... | ... |
