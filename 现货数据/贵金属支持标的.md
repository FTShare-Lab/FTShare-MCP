# 贵金属支持标的（MCP 工具 `ft_get_bullion_support_symbol`）

> **MCP 工具**：`ft_get_bullion_support_symbol`（category: `现货数据`）。返回 **markdown 表格文本**（非结构化 JSON）。输入参数 / 输出参数 / 数据样例对照 [ftshare-doc](../../ftshare-doc/api-doc) 原接口。

- 描述：查询贵金属支持的标的（标的代码、名称、单位、币种）。无入参，返回数组 `Vec<BullionSupportSymbol>`，由 `get_bullion_support_symbols` 返回内置清单。
- 数据范围：最新快照（支持的贵金属标的清单，无时间序列）
- 单次限量：全量清单返回，无分页
- 提示：
  - 无入参，请求体为空对象。
  - 典型标的如 `XAUUSD`（现货黄金，美元/盎司）、`AU9999`（黄金，元/克）。

## 输入参数

无入参（请求体为空对象 `BullionSupportSymbolRequest {}`）。

## 输出参数

BullionSupportSymbol：

| 名称 | 类型 | 默认显示 | 描述 |
|------|------|---------|------|
| symbol | string | Y | 标的代码，如 XAUUSD、AU9999 |
| symbol_name | string | Y | 标的名称，如 现货黄金(伦敦)、黄金(上海) |
| unit | string | Y | 单位，如 美元/盎司、元/克 |
| currency | string | Y | 币种，如 人民币、美元 |

## 调用方法（MCP）

> MCP 工具名 `ft_get_bullion_support_symbol`。MCP Streamable HTTP 要求**先 initialize 拿 `Mcp-Session-Id`，再 `tools/call`**，后续请求带该 header。返回 **markdown 表格文本**。

**curl**：

```bash
SID=$(curl -sS -m 10 -D - -o /dev/null -X POST <MCP_BASE_URL> \
  -H "Accept: application/json, text/event-stream" -H "Content-Type: application/json" \
  -d '{"jsonrpc":"2.0","id":1,"method":"initialize","params":{"protocolVersion":"2025-11-25","capabilities":{},"clientInfo":{"name":"t","version":"1"}}}' \
  | grep -i 'mcp-session-id:' | awk '{print $2}' | tr -d '')

curl -sS -m 10 -X POST <MCP_BASE_URL> \
  -H "Accept: application/json, text/event-stream" -H "Content-Type: application/json" \
  -H "Mcp-Session-Id: $SID" \
  -d '{"jsonrpc":"2.0","id":2,"method":"tools/call","params":{"name":"ft_get_bullion_support_symbol","arguments":{}}}'
```

**Python（`mcp` SDK，自动握手管理 session）**：

```python
import asyncio
from mcp import ClientSession
from mcp.client.streamable_http import streamablehttp_client

async def main():
    async with streamablehttp_client('<MCP_BASE_URL>') as (r, w, _):
        async with ClientSession(r, w) as s:
            await s.initialize()
            res = await s.call_tool('ft_get_bullion_support_symbol', {})
            print(res.content[0].text)   # markdown 表格文本

asyncio.run(main())
```

## 数据样例

支持的贵金属标的清单：

| symbol | symbol_name | unit | currency |
|--------|-------------|------|----------|
| XAUUSD | 现货黄金(伦敦) | 美元/盎司 | 美元 |
| AU9999 | 黄金(上海) | 元/克 | 人民币 |
| ... | ... | ... | ... |
