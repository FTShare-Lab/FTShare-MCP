# 美股资产负债表（MCP 工具 `ft_us_balance`）

> **MCP 工具**：`ft_us_balance`（category: `美股数据/财务数据`）。返回统一 MCP 输出：`structuredContent.metadata` + `structuredContent.data`；`content[0].text` 是同值的序列化 JSON，不额外返回 Markdown。输入参数 / 输出参数 / 数据样例见下文。
> 文中 `Response`、`items`、`records`、`code`、`message` 等名称仅为字段说明；MCP 对外固定为上述 `metadata/data`。

- 描述：查询美股上市公司资产负债表，包含货币资金、应收账款、存货、固定资产、商誉、短期借款、长期债务、总资产、总负债、股本和留存收益等科目；数据按公司财年与报告期归集，股票代码必填，可按财年、报告类型或报告期范围筛选，美股财年不一定等同自然年。
- 数据范围：2023-01 至今
- 单次限量：每页默认 50 条，最大 500 条；分页按**报告期**分页（page_size 限制报告期数，非科目行数）
- 提示：
  - `stock_code` 必填，纯代码不带交易所后缀（如 `NVDA`）。
  - 长表结构：`ind_name` 是科目名（源数据英文字段名，如 tot_assets / tot_liab / tot_eq）、`ind_value` 是科目值，一个报告期展开为多行。
  - 分页基于报告期数，`items` 行数 ≈ 报告期数 × (80~100)，不能用 `total_items` 估算长表总行数。
  - 美股财年非自然年，`period` 匹配 `stmt_year`（财年字段），非日历年。
  - `report_type` 取值：`Q1` / `Q2` / `Q3` / `Q4` / `H1`（半年报）。

## 输入参数

| 名称 | 类型 | 必选 | 描述 |
|------|------|------|------|
| stock_code | string | Y | 美股代码，纯代码不带后缀，如 NVDA |
| period | int | N | 财年（如 2024），匹配 stmt_year，非自然年 |
| report_type | string | N | 报告期类型：Q1 / Q2 / Q3 / Q4 / H1 |
| start_date | int | N | 报告期下界 YYYYMMDD（含），过滤 end_date |
| end_date | int | N | 报告期上界 YYYYMMDD（含），过滤 end_date |
| page | int | N | 页码，默认 1 |
| page_size | int | N | 每页报告期数，默认 50，最大 500 |

## 输出参数

> MCP 固定输出信封为 `structuredContent.metadata` + `structuredContent.data`；`content[0].text` 是与其同值的序列化 JSON，不是 Markdown。
>
> `items` / `records` / `code` / `message` 等传输字段不会直接出现在 MCP 结果中；分页与截断信息统一归入 `metadata`。

| MCP 字段 | 类型 | 必填 | 描述 |
|----------|------|------|------|
| metadata | object | Y | 契约版本、数据来源、工具名、业务口径、总量、分页、返回条数、截断状态及 warnings |
| data | array | Y | 归一化后的业务数据项 |

### data 业务字段

| 名称 | 类型 | 默认显示 | 描述 |
|------|------|---------|------|
| end_date | string | Y | 报告期截止日 YYYY-MM-DD |
| ind_type | string | Y | 报告期类型：Q1 / Q2 / Q3 / Q4 / H1 |
| indicators | array | Y |  |
| name | string | Y | 股票名称（中文） |
| report_type | string | Y | 报告类型：一季报 / 中报 / 三季报 / 年报 |
| stock_code | string | Y | 美股代码，如 NVDA |

## 调用方法（MCP）

> MCP Streamable HTTP 要求**先 initialize 拿 `Mcp-Session-Id`，发送 `notifications/initialized`，再 `tools/call`**，后续请求同时带该 Session ID 和协商后的 `MCP-Protocol-Version`。返回统一 `metadata/data` 结构化输出；`content[0].text` 为同值 JSON 文本，不额外返回 Markdown。

**curl**：

```bash
set -euo pipefail

MCP_BASE_URL="<MCP_BASE_URL>"

check_mcp_response() {
  local response=$1
  printf '%s\n' "$response"
  if printf '%s\n' "$response" | grep -Eq '"isError"[[:space:]]*:[[:space:]]*true|"error"[[:space:]]*:[[:space:]]*\{'; then
    return 1
  fi
}

SID=$(curl -fsS -m 60 -D - -o /dev/null -X POST "$MCP_BASE_URL" \
  -H "Accept: application/json, text/event-stream" \
  -H "Content-Type: application/json" \
  -d '{"jsonrpc":"2.0","id":1,"method":"initialize","params":{"protocolVersion":"2025-11-25","capabilities":{},"clientInfo":{"name":"ftshare-doc-example","version":"1.0.0"}}}' \
  | awk 'tolower($1)=="mcp-session-id:" {print $2}' \
  | tr -d '\r')

if [ -z "$SID" ]; then
  printf '%s\n' 'initialize 未返回 Mcp-Session-Id' >&2
  exit 1
fi

curl -fsS -m 60 -o /dev/null -X POST "$MCP_BASE_URL" \
  -H "Accept: application/json, text/event-stream" \
  -H "Content-Type: application/json" \
  -H "Mcp-Session-Id: $SID" \
  -H "MCP-Protocol-Version: 2025-11-25" \
  -d '{"jsonrpc":"2.0","method":"notifications/initialized"}'

CALL_RESPONSE=$(curl -fsS -m 60 -X POST "$MCP_BASE_URL" \
  -H "Accept: application/json, text/event-stream" \
  -H "Content-Type: application/json" \
  -H "Mcp-Session-Id: $SID" \
  -H "MCP-Protocol-Version: 2025-11-25" \
  -d '{"jsonrpc":"2.0","id":2,"method":"tools/call","params":{"name":"ft_us_balance","arguments":{"stock_code":"NVDA","page":1,"page_size":2}}}')

check_mcp_response "$CALL_RESPONSE"
```

**Python（`mcp` SDK，自动握手管理 session）**：

```python
import asyncio

from mcp import ClientSession
from mcp.client.streamable_http import streamable_http_client


MCP_BASE_URL = "<MCP_BASE_URL>"


async def main():
    async with streamable_http_client(MCP_BASE_URL) as (read_stream, write_stream):
        async with ClientSession(read_stream, write_stream) as session:
            await session.initialize()
            result = await session.call_tool(
                'ft_us_balance',
                {'stock_code': 'NVDA', 'page': 1, 'page_size': 2},
            )
            if result.is_error:
                raise RuntimeError(result.content[0].text)
            print(result.structured_content)
            print(result.content[0].text)


asyncio.run(main())
```

## 数据样例

| stock_code | end_date | ind_type | ind_name | ind_value | report_type |
|------|------|------|------|------|------|
| NVDA | 2026-04-26 | Q1 | null | null | 一季报 |
| NVDA | 2026-01-25 | Q4 | null | null | 年报 |
| ... | ... | ... | ... | ... | ... |
