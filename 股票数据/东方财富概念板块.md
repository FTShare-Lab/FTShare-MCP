# 东方财富概念板块（MCP 工具 `ft_eastmoney_concept_boards`）

> **MCP 工具**：`ft_eastmoney_concept_boards`（category: `股票数据/打板专题数据`）。返回 **markdown 表格文本**（非结构化 JSON）。输入参数 / 输出参数 / 数据样例对照 [ftshare-doc](../../ftshare-doc/api-doc) 原接口。

- 描述：获取东方财富**概念板块**列表（板块代码以 `BK` 开头，如 BK1024）。返回数组（非分页），当前在线约 486 个概念板块。数据直接读取文件中心 CSV。
- 数据范围：无时间维度，实时/最新快照
- 单次限量：无入参、无分页，一次返回全部板块
- 提示：
  - 此接口仅返回**概念**板块；成份股、最新/历史 K 线另见对应接口。
  - 板块代码（BKxxxx）可用作成份股与 K 线接口的入参。

## 输入参数

无入参。

## 输出参数

| 名称 | 类型 | 默认显示 | 描述 |
|------|------|---------|------|
| code | string | Y | 板块代码，如 BK1024 |
| name | string | Y | 板块名称（中文） |

## 调用方法（MCP）

> MCP 工具名 `ft_eastmoney_concept_boards`。MCP Streamable HTTP 要求**先 initialize 拿 `Mcp-Session-Id`，再 `tools/call`**，后续请求带该 header。返回 **markdown 表格文本**。

**curl**：

```bash
SID=$(curl -sS -m 10 -D - -o /dev/null -X POST <MCP_BASE_URL> \
  -H "Accept: application/json, text/event-stream" -H "Content-Type: application/json" \
  -d '{"jsonrpc":"2.0","id":1,"method":"initialize","params":{"protocolVersion":"2025-11-25","capabilities":{},"clientInfo":{"name":"t","version":"1"}}}' \
  | grep -i 'mcp-session-id:' | awk '{print $2}' | tr -d '')

curl -sS -m 10 -X POST <MCP_BASE_URL> \
  -H "Accept: application/json, text/event-stream" -H "Content-Type: application/json" \
  -H "Mcp-Session-Id: $SID" \
  -d '{"jsonrpc":"2.0","id":2,"method":"tools/call","params":{"name":"ft_eastmoney_concept_boards","arguments":{}}}'
```

**Python（`mcp` SDK，自动握手管理 session）**：

```python
import asyncio
from mcp import ClientSession
from mcp.client.streamable_http import streamablehttp_client

async def main():
    async with streamablehttp_client('<MCP_BASE_URL>') as (r, w, _):
        async with ClientSession(r, w) as s:
            await s.initialize()
            res = await s.call_tool('ft_eastmoney_concept_boards', {})
            print(res.content[0].text)   # markdown 表格文本

asyncio.run(main())
```

## 数据样例

| code | name |
|------|------|
| BK1645 | 昨日打二板以上表现 |
| BK1051 | 昨日连板_含一字 |
| BK1132 | MLOps概念 |
| BK0816 | 昨日连板 |
| BK1169 | Kimi概念 |
| ... | ... |
| BK1121 | 第四代半导体 |
| BK1128 | CPO概念 |
