# 十大流通股东（MCP 工具 `ft_stock_float_holders`）

> **MCP 工具**：`ft_stock_float_holders`（category: `股票数据/参考数据`）。返回统一 MCP 输出：`structuredContent.metadata` + `structuredContent.data`；`content[0].text` 是同值的序列化 JSON，不额外返回 Markdown。输入参数 / 输出参数 / 数据样例见下文。
> 文中 `Response`、`items`、`records`、`code`、`message` 等名称仅为字段说明；MCP 对外固定为上述 `metadata/data`。

- 描述：查询 A 股十大流通股东。指定 `stock_code` 时查询该股票全部历史记录；传入 `is_last=true` 时查询全市场最新一期数据，两种模式必须选择一种。
- 数据范围：按报告期，以实际返回为准
- 单次限量：默认 `page=1`、`page_size=50`；按 `stock_code` 查询时不分页，一次返回该标的全量历史
- 提示：
  - 未指定 `stock_code` 时，需配合 `is_last=true` 才有数据返回。
  - `fen_holders` 为该报告期前十大流通股东明细数组。
  - 数据更新随上市公司定期报告披露。

## 输入参数

| 名称 | 类型 | 必选 | 描述 |
|------|------|------|------|
| stock_code | string | N | 标的代码，指定时返回该标的全部历史数据 |
| is_last | bool | N | 是否取所有标的最新一期，true 时按 page/page_size 分页 |
| page | int | N | 页码，默认 1 |
| page_size | int | N | 每页条数，默认 50 |

## 输出参数

> MCP 固定输出信封为 `structuredContent.metadata` + `structuredContent.data`；`content[0].text` 是与其同值的序列化 JSON，不是 Markdown。
>
> `items` / `records` / `code` / `message` 等传输字段不会直接出现在 MCP 结果中；分页与截断信息统一归入 `metadata`。

| MCP 字段 | 类型 | 必填 | 描述 |
|----------|------|------|------|
| metadata | object | Y | 契约版本、数据来源、工具名、业务口径、总量、分页、返回条数、截断状态及 warnings |
| data | array | Y | 归一化后的业务数据项 |

### data 业务字段

| 名称 | 类型 | 默认显示 | 描述 |
|------|------|---------|------|
| fen_holders | array[HolderDetail] | Y | 十大流通股东明细 |
| publish_date | string | Y | 发布日期 YYYY-MM-DD |
| share_holding | decimal | Y | 持股合计 |
| stock_code | string | Y | 股票代码 |
| stock_name | string | Y | 股票全称 |

fen_holders 元素：

| 名称 | 类型 | 默认显示 | 描述 |
|------|------|---------|------|
| rank | int | Y | 股东名次 |
| shareholder_name | string | Y | 股东名称 |
| shareholder_type | string | Y | 股东性质 |
| share_type | string | Y | 股份类型 |
| shareholding | string | Y | 持股数（股） |
| share_ratio | string | Y | 占股本持股比例（%） |
| limit_num | string | N | 流通限售数量 |
| unlimit_num | string | N | 流通无限售数量 |
| change_shares | string | Y | 增减（股） |
| change_type | string | N | 变动类型 |
| change_percentage | string | N | 变动比例（%） |

## 调用方法（MCP）

> MCP Streamable HTTP 要求**先 initialize 拿 `Mcp-Session-Id`，发送 `notifications/initialized`，再 `tools/call`**，后续请求同时带该 Session ID 和协商后的 `MCP-Protocol-Version`。返回统一 `metadata/data` 结构化输出；`content[0].text` 为同值 JSON 文本，不额外返回 Markdown。

**curl**：

```bash
set -euo pipefail

MCP_BASE_URL="<MCP_BASE_URL>"

check_mcp_response() {
  local response=$1
  printf '%s\n' "$response"
  if printf '%s\n' "$response" | grep -Eq '"isError"[[:space:]]*:[[:space:]]*true|"error"[[:space:]]*:[[:space:]]*\{'; then
    return 1
  fi
}

SID=$(curl -fsS -m 60 -D - -o /dev/null -X POST "$MCP_BASE_URL" \
  -H "Accept: application/json, text/event-stream" \
  -H "Content-Type: application/json" \
  -d '{"jsonrpc":"2.0","id":1,"method":"initialize","params":{"protocolVersion":"2025-11-25","capabilities":{},"clientInfo":{"name":"ftshare-doc-example","version":"1.0.0"}}}' \
  | awk 'tolower($1)=="mcp-session-id:" {print $2}' \
  | tr -d '\r')

if [ -z "$SID" ]; then
  printf '%s\n' 'initialize 未返回 Mcp-Session-Id' >&2
  exit 1
fi

curl -fsS -m 60 -o /dev/null -X POST "$MCP_BASE_URL" \
  -H "Accept: application/json, text/event-stream" \
  -H "Content-Type: application/json" \
  -H "Mcp-Session-Id: $SID" \
  -H "MCP-Protocol-Version: 2025-11-25" \
  -d '{"jsonrpc":"2.0","method":"notifications/initialized"}'

CALL_RESPONSE=$(curl -fsS -m 60 -X POST "$MCP_BASE_URL" \
  -H "Accept: application/json, text/event-stream" \
  -H "Content-Type: application/json" \
  -H "Mcp-Session-Id: $SID" \
  -H "MCP-Protocol-Version: 2025-11-25" \
  -d '{"jsonrpc":"2.0","id":2,"method":"tools/call","params":{"name":"ft_stock_float_holders","arguments":{"is_last":true,"page":1,"page_size":2}}}')

check_mcp_response "$CALL_RESPONSE"
```

**Python（`mcp` SDK，自动握手管理 session）**：

```python
import asyncio

from mcp import ClientSession
from mcp.client.streamable_http import streamable_http_client


MCP_BASE_URL = "<MCP_BASE_URL>"


async def main():
    async with streamable_http_client(MCP_BASE_URL) as (read_stream, write_stream):
        async with ClientSession(read_stream, write_stream) as session:
            await session.initialize()
            result = await session.call_tool(
                'ft_stock_float_holders',
                {'is_last': True, 'page': 1, 'page_size': 2},
            )
            if result.is_error:
                raise RuntimeError(result.content[0].text)
            print(result.structured_content)
            print(result.content[0].text)


asyncio.run(main())
```

## 数据样例

| stock_code | stock_name | publish_date | share_holding |
|------|------|------|------|
| 000001.SZ | 平安银行 | 2026-03-31 | 64.649901 |
| 000002.SZ | 万科A | 2026-03-31 | 50.964923 |
| ... | ... | ... | ... |

000001.SZ 2026-03-31 的 `fen_holders` 前 3 大流通股东：

| rank | shareholder_name | shareholder_type | share_type | shareholding | share_ratio | change_shares | change_type |
|------|------|------|------|------|------|------|------|
| 1 | 中国平安保险(集团)股份有限公司-集团本级-自有资金 | 一般企业 | A股 | 9618540236 | 49.564984 | 0 | 不变 |
| 2 | 中国平安人寿保险股份有限公司-自有资金 | 一般企业 | A股 | 1186100488 | 6.112055 | 0 | 不变 |
| 3 | 香港中央结算有限公司 | 一般企业 | A股 | 570772048 | 2.941227 | -57519101 | 减持 |
| ... | ... | ... | ... | ... | ... | ... | ... |
