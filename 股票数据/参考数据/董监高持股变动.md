# 董监高持股变动（MCP 工具 `ft_stock_ggmx_handler`）

> **MCP 工具**：`ft_stock_ggmx_handler`（category: `股票数据/参考数据`）。返回统一 MCP 输出：`structuredContent.metadata` + `structuredContent.data`；`content[0].text` 是同值的序列化 JSON，不额外返回 Markdown。输入参数 / 输出参数 / 数据样例见下文。
> 文中 `Response`、`items`、`records`、`code`、`message` 等名称仅为字段说明；MCP 对外固定为上述 `metadata/data`。

- 描述：查询东方财富董监高持股变动明细，包括变动人、职务、变动股数与金额、变动均价、变动后持股、变动比例、当日收盘价和涨跌幅。支持按股票代码、增减持方向和变动日期范围筛选。数据来源：东方财富。
- 数据范围：按变动日期
- 单次限量：分页返回，默认 `page=1`、`page_size` 默认 50
- 提示：
  - 所有入参均为可选，参数支持 camelCase 与 snake_case 双别名。
  - 日期格式为 `YYYY-MM-DD`。
  - 数据来源东方财富。

## 输入参数

| 名称 | 类型 | 必选 | 描述 |
|------|------|------|------|
| stock_code | string | N | 股票代码（如 600001），别名 stockCode |
| change_direction | string | N | 变动方向：增持 / 减持，别名 changeDirection |
| start_date | string | N | 变动日期起始 YYYY-MM-DD，别名 startDate |
| end_date | string | N | 变动日期截止 YYYY-MM-DD，别名 endDate |
| page | int | N | 页码 |
| page_size | int | N | 每页条数，别名 pageSize |

## 输出参数

> MCP 固定输出信封为 `structuredContent.metadata` + `structuredContent.data`；`content[0].text` 是与其同值的序列化 JSON，不是 Markdown。
>
> `items` / `records` / `code` / `message` 等传输字段不会直接出现在 MCP 结果中；分页与截断信息统一归入 `metadata`。

| MCP 字段 | 类型 | 必填 | 描述 |
|----------|------|------|------|
| metadata | object | Y | 契约版本、数据来源、工具名、业务口径、总量、分页、返回条数、截断状态及 warnings |
| data | array | Y | 归一化后的业务数据项 |

### data 业务字段

| 名称 | 类型 | 默认显示 | 描述 |
|------|------|---------|------|
| avgPrice | string | Y | 变动均价 |
| changeAmount | string | Y | 变动金额 |
| changeDate | string | Y | 变动日期 |
| changeDirection | string | Y | 变动方向 |
| changeQuantity | string | Y | 变动数量 |
| changeRatio | string | Y | 变动比例 |
| changeReason | string | Y | 变动原因 |
| changeShares | string | Y | 变动股数 |
| changer | string | N | 变动人 |
| closePrice | string | Y | 收盘价 |
| crawlBatchTs | string | Y | 抓取批次时间戳 |
| crawlDate | string | Y | 抓取日期 |
| dataTime | string | Y | 数据时间 |
| executiveName | string | Y | 高管姓名 |
| noticeDate | string | Y | 公告日期 |
| position | string | N | 职务 |
| priceChange | string | Y | 涨跌幅 |
| quoteChange | string | Y | 行情涨跌幅 |
| quotePrice | string | Y | 行情价 |
| registerDate | string | Y | 注册/登记日期 |
| relation | string | N | 关系 |
| sharesAfter | string | Y | 变动后持股 |
| source | string | Y | 数据来源 |
| stockCode | string | Y | 股票代码 |
| stockName | string | Y | 股票名称 |
| totalShare | string | Y | 总股本 |

## 调用方法（MCP）

> MCP Streamable HTTP 要求**先 initialize 拿 `Mcp-Session-Id`，发送 `notifications/initialized`，再 `tools/call`**，后续请求同时带该 Session ID 和协商后的 `MCP-Protocol-Version`。返回统一 `metadata/data` 结构化输出；`content[0].text` 为同值 JSON 文本，不额外返回 Markdown。

**curl**：

```bash
set -euo pipefail

MCP_BASE_URL="<MCP_BASE_URL>"

check_mcp_response() {
  local response=$1
  printf '%s\n' "$response"
  if printf '%s\n' "$response" | grep -Eq '"isError"[[:space:]]*:[[:space:]]*true|"error"[[:space:]]*:[[:space:]]*\{'; then
    return 1
  fi
}

SID=$(curl -fsS -m 60 -D - -o /dev/null -X POST "$MCP_BASE_URL" \
  -H "Accept: application/json, text/event-stream" \
  -H "Content-Type: application/json" \
  -d '{"jsonrpc":"2.0","id":1,"method":"initialize","params":{"protocolVersion":"2025-11-25","capabilities":{},"clientInfo":{"name":"ftshare-doc-example","version":"1.0.0"}}}' \
  | awk 'tolower($1)=="mcp-session-id:" {print $2}' \
  | tr -d '\r')

if [ -z "$SID" ]; then
  printf '%s\n' 'initialize 未返回 Mcp-Session-Id' >&2
  exit 1
fi

curl -fsS -m 60 -o /dev/null -X POST "$MCP_BASE_URL" \
  -H "Accept: application/json, text/event-stream" \
  -H "Content-Type: application/json" \
  -H "Mcp-Session-Id: $SID" \
  -H "MCP-Protocol-Version: 2025-11-25" \
  -d '{"jsonrpc":"2.0","method":"notifications/initialized"}'

CALL_RESPONSE=$(curl -fsS -m 60 -X POST "$MCP_BASE_URL" \
  -H "Accept: application/json, text/event-stream" \
  -H "Content-Type: application/json" \
  -H "Mcp-Session-Id: $SID" \
  -H "MCP-Protocol-Version: 2025-11-25" \
  -d '{"jsonrpc":"2.0","id":2,"method":"tools/call","params":{"name":"ft_stock_ggmx_handler","arguments":{"page":1,"page_size":2,"start_date":"2026-06-24","end_date":"2026-06-24"}}}')

check_mcp_response "$CALL_RESPONSE"
```

**Python（`mcp` SDK，自动握手管理 session）**：

```python
import asyncio

from mcp import ClientSession
from mcp.client.streamable_http import streamable_http_client


MCP_BASE_URL = "<MCP_BASE_URL>"


async def main():
    async with streamable_http_client(MCP_BASE_URL) as (read_stream, write_stream):
        async with ClientSession(read_stream, write_stream) as session:
            await session.initialize()
            result = await session.call_tool(
                'ft_stock_ggmx_handler',
                {'page': 1, 'page_size': 2, 'start_date': '2026-06-24', 'end_date': '2026-06-24'},
            )
            if result.is_error:
                raise RuntimeError(result.content[0].text)
            print(result.structured_content)
            print(result.content[0].text)


asyncio.run(main())
```

## 数据样例

| stockCode | stockName | changeDate | noticeDate |
|------|------|------|------|
| 000078 | ST海王 | 2026-06-24 | 2026-06-24 |
| 000800 | 一汽解放 | 2026-06-24 | 2026-06-24 |
| ... | ... | ... | ... |
