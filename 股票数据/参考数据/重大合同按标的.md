# 重大合同按标的（MCP 工具 `ft_major_contract_by_symbol`）

> **MCP 工具**：`ft_major_contract_by_symbol`（category: `股票数据/参考数据`）。返回统一 MCP 输出：`structuredContent.metadata` + `structuredContent.data`；`content[0].text` 是同值的序列化 JSON，不额外返回 Markdown。输入参数 / 输出参数 / 数据样例见下文。
> 文中 `Response`、`items`、`records`、`code`、`message` 等名称仅为字段说明；MCP 对外固定为上述 `metadata/data`。

- 描述：按证券代码查询 A 股上市公司披露的重大合同，包括合同双方、合同类型与名称、合同金额、占上年营收比例、签约日和披露日。数据来源：东方财富。
- 数据范围：按报告期查询
- 单次限量：分页默认 `page=1`、`page_size=50`、最大 200
- 提示：
  - `symbol` 必填，为证券代码。
  - 按标的过滤后分页返回重大合同记录。
  - 多数字段为 `Option<String>`，缺失返回 null。

## 输入参数

| 名称 | 类型 | 必选 | 描述 |
|------|------|------|------|
| symbol | string | Y | 证券代码（标的） |
| page | int | N | 页码，默认 1 |
| page_size | int | N | 每页条数，默认 50、最大 200 |

## 输出参数

> MCP 固定输出信封为 `structuredContent.metadata` + `structuredContent.data`；`content[0].text` 是与其同值的序列化 JSON，不是 Markdown。
>
> `items` / `records` / `code` / `message` 等传输字段不会直接出现在 MCP 结果中；分页与截断信息统一归入 `metadata`。

| MCP 字段 | 类型 | 必填 | 描述 |
|----------|------|------|------|
| metadata | object | Y | 契约版本、数据来源、工具名、业务口径、总量、分页、返回条数、截断状态及 warnings |
| data | array | Y | 归一化后的业务数据项 |

### data 业务字段

| 名称 | 类型 | 默认显示 | 描述 |
|------|------|---------|------|
| amounts | string | Y | 合同金额 |
| contract_name | string | Y | 合同名称 |
| contract_type_name | string | Y | 合同类型（如「销售合同」） |
| counter_party | string | Y | 合同对方 |
| counter_party_rel_name | string | Y | 合同对方关系 |
| dim_rdate | string | Y | 披露日期 |
| operate_reve | string | Y | 营业收入 |
| security_code | string | Y | 证券代码 |
| security_short_name | string | Y | 证券简称 |
| seq | int | Y | 序号 |
| sign_date | string | Y | 签约日期 |
| signatory | string | Y | 签约方 |
| signatory_rel_name | string | Y | 签约方关系 |
| snd_yysr | string | Y | 上年同期营收 |
| zsnd_yysr_bl | string | Y | 占上年同期营收比例（%） |

## 调用方法（MCP）

> MCP Streamable HTTP 要求**先 initialize 拿 `Mcp-Session-Id`，发送 `notifications/initialized`，再 `tools/call`**，后续请求同时带该 Session ID 和协商后的 `MCP-Protocol-Version`。返回统一 `metadata/data` 结构化输出；`content[0].text` 为同值 JSON 文本，不额外返回 Markdown。

**curl**：

```bash
set -euo pipefail

MCP_BASE_URL="<MCP_BASE_URL>"

check_mcp_response() {
  local response=$1
  printf '%s\n' "$response"
  if printf '%s\n' "$response" | grep -Eq '"isError"[[:space:]]*:[[:space:]]*true|"error"[[:space:]]*:[[:space:]]*\{'; then
    return 1
  fi
}

SID=$(curl -fsS -m 60 -D - -o /dev/null -X POST "$MCP_BASE_URL" \
  -H "Accept: application/json, text/event-stream" \
  -H "Content-Type: application/json" \
  -d '{"jsonrpc":"2.0","id":1,"method":"initialize","params":{"protocolVersion":"2025-11-25","capabilities":{},"clientInfo":{"name":"ftshare-doc-example","version":"1.0.0"}}}' \
  | awk 'tolower($1)=="mcp-session-id:" {print $2}' \
  | tr -d '\r')

if [ -z "$SID" ]; then
  printf '%s\n' 'initialize 未返回 Mcp-Session-Id' >&2
  exit 1
fi

curl -fsS -m 60 -o /dev/null -X POST "$MCP_BASE_URL" \
  -H "Accept: application/json, text/event-stream" \
  -H "Content-Type: application/json" \
  -H "Mcp-Session-Id: $SID" \
  -H "MCP-Protocol-Version: 2025-11-25" \
  -d '{"jsonrpc":"2.0","method":"notifications/initialized"}'

CALL_RESPONSE=$(curl -fsS -m 60 -X POST "$MCP_BASE_URL" \
  -H "Accept: application/json, text/event-stream" \
  -H "Content-Type: application/json" \
  -H "Mcp-Session-Id: $SID" \
  -H "MCP-Protocol-Version: 2025-11-25" \
  -d '{"jsonrpc":"2.0","id":2,"method":"tools/call","params":{"name":"ft_major_contract_by_symbol","arguments":{"symbol":"600039","page":1,"page_size":3}}}')

check_mcp_response "$CALL_RESPONSE"
```

**Python（`mcp` SDK，自动握手管理 session）**：

```python
import asyncio

from mcp import ClientSession
from mcp.client.streamable_http import streamable_http_client


MCP_BASE_URL = "<MCP_BASE_URL>"


async def main():
    async with streamable_http_client(MCP_BASE_URL) as (read_stream, write_stream):
        async with ClientSession(read_stream, write_stream) as session:
            await session.initialize()
            result = await session.call_tool(
                'ft_major_contract_by_symbol',
                {'symbol': '600039', 'page': 1, 'page_size': 3},
            )
            if result.is_error:
                raise RuntimeError(result.content[0].text)
            print(result.structured_content)
            print(result.content[0].text)


asyncio.run(main())
```

## 数据样例

| security_code | security_short_name | contract_name | amounts | sign_date | dim_rdate |
|------|------|------|------|------|------|
| 600039 | 四川路桥 | G85G76重庆(川渝界)至成都高速公路扩容工程项目路基土建工程施工总承包合同 | 11596000000.0000 | null | 2025-07-03 |
| 600039 | 四川路桥 | G5京昆高速汉中至广元段(四川境)扩容工程施工总承包C2合同段施工总承包合同 | 15740000000.0000 | null | 2023-11-18 |
| 600039 | 四川路桥 | G7611线西昌至香格里拉(四川境)高速公路项目ZCB-3合同段施工总承包合同 | 32408000000.0000 | null | 2023-06-02 |
| ... | ... | ... | ... | ... | ... |
