# 限售解禁（MCP 工具 `ft_stock_unlock_handler`）

> **MCP 工具**：`ft_stock_unlock_handler`（category: `股票数据/参考数据`）。返回统一 MCP 输出：`structuredContent.metadata` + `structuredContent.data`；`content[0].text` 是同值的序列化 JSON，不额外返回 Markdown。输入参数 / 输出参数 / 数据样例见下文。
> 文中 `Response`、`items`、`records`、`code`、`message` 等名称仅为字段说明；MCP 对外固定为上述 `metadata/data`。

- 描述：按证券代码查询限售解禁批次及股东明细，包括解禁日、股东户数、解禁股数、当前流通与未流通股数、解禁市值、解禁比例、新股价、限售股份类型，以及解禁前后 20 日涨跌幅。数据来源：东方财富。
- 数据范围：按报告期查询
- 单次限量：SQL 分页，默认 `page=1`、`page_size=50`
- 提示：
  - `stock_code` 必填，为证券代码。
  - 分页以「批次」为粒度，每个批次 records 内嵌 `holders` 股东明细。
  - 字段序列化为 camelCase（如 `stockCode`、`unlockDate`、`holderCount`）。

## 输入参数

| 名称 | 类型 | 必选 | 描述 |
|------|------|------|------|
| stock_code | string | Y | 证券代码 |
| page | int | N | 页码，默认 1 |
| page_size | int | N | 每页条数，默认 50 |

## 输出参数

> MCP 固定输出信封为 `structuredContent.metadata` + `structuredContent.data`；`content[0].text` 是与其同值的序列化 JSON，不是 Markdown。
>
> `items` / `records` / `code` / `message` 等传输字段不会直接出现在 MCP 结果中；分页与截断信息统一归入 `metadata`。

| MCP 字段 | 类型 | 必填 | 描述 |
|----------|------|------|------|
| metadata | object | Y | 契约版本、数据来源、工具名、业务口径、总量、分页、返回条数、截断状态及 warnings |
| data | array | Y | 归一化后的业务数据项 |

### data 业务字段

| 名称 | 类型 | 默认显示 | 描述 |
|------|------|---------|------|
| a20Adjchrate | string | Y | 解禁后 20 日涨跌幅 |
| ableFreeShares | string | Y | 解禁股数 |
| b20Adjchrate | string | Y | 解禁前 20 日涨跌幅 |
| crawlDate | string | Y | 爬取日期 |
| currentFreeShares | string | Y | 当前流通股数 |
| freeRatio | string | Y | 解禁比例（占流通股本） |
| freeSharesType | string | Y | 限售股份类型 |
| holderCount | string | Y | 股东户数 |
| holders | array | Y | 股东明细列表 |
| liftMarketCap | string | Y | 解禁市值 |
| newPrice | string | Y | 新股价 |
| nonFreeShares | string | Y | 未流通股数 |
| source | string | Y | 数据来源（如 eastmoney） |
| stockCode | string | Y | 证券代码 |
| stockName | string | Y | 证券简称 |
| totalRatio | string | Y | 解禁比例（占总股本） |
| unlockDate | string | Y | 解禁日期 |

holders 元素：

| 名称 | 类型 | 默认显示 | 描述 |
|------|------|---------|------|
| holderName | string | Y | 股东名称 |
| addListingShares | string | Y | 新增上市股数 |
| actualListedShares | string | Y | 实际上市股数 |
| addListingCap | string | Y | 新增上市市值 |
| lockMonth | string | Y | 锁定月数 |
| residualLimitedShares | string | Y | 剩余限售股数 |
| freeSharesType | string | Y | 限售股份类型 |
| planFeature | string | Y | 计划进度（如「已实施」） |

## 调用方法（MCP）

> MCP Streamable HTTP 要求**先 initialize 拿 `Mcp-Session-Id`，发送 `notifications/initialized`，再 `tools/call`**，后续请求同时带该 Session ID 和协商后的 `MCP-Protocol-Version`。返回统一 `metadata/data` 结构化输出；`content[0].text` 为同值 JSON 文本，不额外返回 Markdown。

**curl**：

```bash
set -euo pipefail

MCP_BASE_URL="<MCP_BASE_URL>"

check_mcp_response() {
  local response=$1
  printf '%s\n' "$response"
  if printf '%s\n' "$response" | grep -Eq '"isError"[[:space:]]*:[[:space:]]*true|"error"[[:space:]]*:[[:space:]]*\{'; then
    return 1
  fi
}

SID=$(curl -fsS -m 60 -D - -o /dev/null -X POST "$MCP_BASE_URL" \
  -H "Accept: application/json, text/event-stream" \
  -H "Content-Type: application/json" \
  -d '{"jsonrpc":"2.0","id":1,"method":"initialize","params":{"protocolVersion":"2025-11-25","capabilities":{},"clientInfo":{"name":"ftshare-doc-example","version":"1.0.0"}}}' \
  | awk 'tolower($1)=="mcp-session-id:" {print $2}' \
  | tr -d '\r')

if [ -z "$SID" ]; then
  printf '%s\n' 'initialize 未返回 Mcp-Session-Id' >&2
  exit 1
fi

curl -fsS -m 60 -o /dev/null -X POST "$MCP_BASE_URL" \
  -H "Accept: application/json, text/event-stream" \
  -H "Content-Type: application/json" \
  -H "Mcp-Session-Id: $SID" \
  -H "MCP-Protocol-Version: 2025-11-25" \
  -d '{"jsonrpc":"2.0","method":"notifications/initialized"}'

CALL_RESPONSE=$(curl -fsS -m 60 -X POST "$MCP_BASE_URL" \
  -H "Accept: application/json, text/event-stream" \
  -H "Content-Type: application/json" \
  -H "Mcp-Session-Id: $SID" \
  -H "MCP-Protocol-Version: 2025-11-25" \
  -d '{"jsonrpc":"2.0","id":2,"method":"tools/call","params":{"name":"ft_stock_unlock_handler","arguments":{"stock_code":"000001","page":1,"page_size":3}}}')

check_mcp_response "$CALL_RESPONSE"
```

**Python（`mcp` SDK，自动握手管理 session）**：

```python
import asyncio

from mcp import ClientSession
from mcp.client.streamable_http import streamable_http_client


MCP_BASE_URL = "<MCP_BASE_URL>"


async def main():
    async with streamable_http_client(MCP_BASE_URL) as (read_stream, write_stream):
        async with ClientSession(read_stream, write_stream) as session:
            await session.initialize()
            result = await session.call_tool(
                'ft_stock_unlock_handler',
                {'stock_code': '000001', 'page': 1, 'page_size': 3},
            )
            if result.is_error:
                raise RuntimeError(result.content[0].text)
            print(result.structured_content)
            print(result.content[0].text)


asyncio.run(main())
```

## 数据样例

| stockCode | stockName | unlockDate | holderCount |
|------|------|------|------|
| 000001 | 平安银行 | 2018-05-21 | 1 |
| 000001 | 平安银行 | 2017-01-09 | 1 |
| 000001 | 平安银行 | 2016-05-23 | 6 |
| ... | ... | ... | ... |

000001 平安银行 2018-05-21 批次的 `holders` 明细：

| holderName | addListingShares | actualListedShares | addListingCap | lockMonth | residualLimitedShares | freeSharesType | planFeature |
|------|------|------|------|------|------|------|------|
| 中国平安保险(集团)股份有限公司 | 252247983.0 | 252247983.0 | 2762115413.85 | 36 | 0.0 | 定向增发机构配售股份 | 实施 |
| ... | ... | ... | ... | ... | ... | ... | ... |
