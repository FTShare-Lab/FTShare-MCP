# 公司列表（MCP 工具 `ft_get_company_list`）

> **MCP 工具**：`ft_get_company_list`（category: `股票数据/基础数据`）。返回统一 MCP 输出：`structuredContent.metadata` + `structuredContent.data`；`content[0].text` 是同值的序列化 JSON，不额外返回 Markdown。输入参数 / 输出参数 / 数据样例见下文。
> 文中 `Response`、`items`、`records`、`code`、`message` 等名称仅为字段说明；MCP 对外固定为上述 `metadata/data`。

- 描述：查询上市公司基本信息，支持按 `stock_name` 或 `stock_code` 精确筛选，不传筛选条件时查询全部公司。信息包括代码、名称、涨跌幅、收盘价、PE、PB、总市值、流通市值、总股本、流通股本、上市日期与类型、所属板块、主营业务、主要产品、经营范围和公司简介等。
- 数据范围：最新公司信息快照（含来源与抓取/创建/更新时间，无时间维度）
- 单次限量：分页默认 `page=1`、`page_size=50`、最大 200
- 提示：
  - `stock_name` / `stock_code` 为精确匹配，二者均可选，同时为空时返回全部。
  - `page` 默认 1，`page_size` 默认 50、最大 200。
  - 查询出错时返回 `code=400`、`data=null`。

## 输入参数

| 名称 | 类型 | 必选 | 描述 |
|------|------|------|------|
| stock_name | string | N | 股票名称，精确匹配 |
| stock_code | string | N | 股票代码，精确匹配 |
| page | uint32 | N | 页码，默认 1 |
| page_size | uint32 | N | 每页条数，默认 50，最大 200 |

## 输出参数

> MCP 固定输出信封为 `structuredContent.metadata` + `structuredContent.data`；`content[0].text` 是与其同值的序列化 JSON，不是 Markdown。
>
> `items` / `records` / `code` / `message` 等传输字段不会直接出现在 MCP 结果中；分页与截断信息统一归入 `metadata`。

| MCP 字段 | 类型 | 必填 | 描述 |
|----------|------|------|------|
| metadata | object | Y | 契约版本、数据来源、工具名、业务口径、总量、分页、返回条数、截断状态及 warnings |
| data | array | Y | 归一化后的业务数据项 |

### data 业务字段

| 名称 | 类型 | 默认显示 | 描述 |
|------|------|---------|------|
| bk | string | N | 所属板块 |
| business_products | string | N | 主要产品 |
| business_scope | string | N | 经营范围 |
| change_percent | string | N | 涨跌幅 |
| circulation_capital | string | N | 流通股本 |
| circulation_value | string | N | 流通市值 |
| close | string | N | 收盘价 |
| company_code | string | Y | 公司代码 |
| company_profile | string | N | 公司简介 |
| crawl_date | string | Y | 抓取日期 |
| created_at | string | Y | 创建时间 |
| id | uint64 | Y | 记录 ID |
| listing_date | string | N | 上市日期 |
| listing_type | string | N | 上市类型 |
| main_business | string | N | 主营业务 |
| pb | string | N | 市净率 |
| pe | string | N | 市盈率 |
| source | string | Y | 数据来源 |
| stock_code | string | Y | 股票代码 |
| stock_name | string | Y | 股票名称 |
| total_capital | string | N | 总股本 |
| total_market_value | string | N | 总市值 |
| updated_at | string | Y | 更新时间 |

## 调用方法（MCP）

> MCP Streamable HTTP 要求**先 initialize 拿 `Mcp-Session-Id`，发送 `notifications/initialized`，再 `tools/call`**，后续请求同时带该 Session ID 和协商后的 `MCP-Protocol-Version`。返回统一 `metadata/data` 结构化输出；`content[0].text` 为同值 JSON 文本，不额外返回 Markdown。

**curl**：

```bash
set -euo pipefail

MCP_BASE_URL="<MCP_BASE_URL>"

check_mcp_response() {
  local response=$1
  printf '%s\n' "$response"
  if printf '%s\n' "$response" | grep -Eq '"isError"[[:space:]]*:[[:space:]]*true|"error"[[:space:]]*:[[:space:]]*\{'; then
    return 1
  fi
}

SID=$(curl -fsS -m 60 -D - -o /dev/null -X POST "$MCP_BASE_URL" \
  -H "Accept: application/json, text/event-stream" \
  -H "Content-Type: application/json" \
  -d '{"jsonrpc":"2.0","id":1,"method":"initialize","params":{"protocolVersion":"2025-11-25","capabilities":{},"clientInfo":{"name":"ftshare-doc-example","version":"1.0.0"}}}' \
  | awk 'tolower($1)=="mcp-session-id:" {print $2}' \
  | tr -d '\r')

if [ -z "$SID" ]; then
  printf '%s\n' 'initialize 未返回 Mcp-Session-Id' >&2
  exit 1
fi

curl -fsS -m 60 -o /dev/null -X POST "$MCP_BASE_URL" \
  -H "Accept: application/json, text/event-stream" \
  -H "Content-Type: application/json" \
  -H "Mcp-Session-Id: $SID" \
  -H "MCP-Protocol-Version: 2025-11-25" \
  -d '{"jsonrpc":"2.0","method":"notifications/initialized"}'

CALL_RESPONSE=$(curl -fsS -m 60 -X POST "$MCP_BASE_URL" \
  -H "Accept: application/json, text/event-stream" \
  -H "Content-Type: application/json" \
  -H "Mcp-Session-Id: $SID" \
  -H "MCP-Protocol-Version: 2025-11-25" \
  -d '{"jsonrpc":"2.0","id":2,"method":"tools/call","params":{"name":"ft_get_company_list","arguments":{"stock_code":"000001","page":1,"page_size":2}}}')

check_mcp_response "$CALL_RESPONSE"
```

**Python（`mcp` SDK，自动握手管理 session）**：

```python
import asyncio

from mcp import ClientSession
from mcp.client.streamable_http import streamable_http_client


MCP_BASE_URL = "<MCP_BASE_URL>"


async def main():
    async with streamable_http_client(MCP_BASE_URL) as (read_stream, write_stream):
        async with ClientSession(read_stream, write_stream) as session:
            await session.initialize()
            result = await session.call_tool(
                'ft_get_company_list',
                {'stock_code': '000001', 'page': 1, 'page_size': 2},
            )
            if result.is_error:
                raise RuntimeError(result.content[0].text)
            print(result.structured_content)
            print(result.content[0].text)


asyncio.run(main())
```

## 数据样例

| company_code | stock_code | stock_name |
|------|------|------|
| 10004085 | 000001 | 平安银行 |
| ... | ... | ... |
