# 机构股本持股（MCP 工具 `ft_get_stock_institution_share_holdings`）

> **MCP 工具**：`ft_get_stock_institution_share_holdings`（category: `股票数据/基础数据`）。返回统一 MCP 输出：`structuredContent.metadata` + `structuredContent.data`；`content[0].text` 是同值的序列化 JSON，不额外返回 Markdown。输入参数 / 输出参数 / 数据样例见下文。
> 文中 `Response`、`items`、`records`、`code`、`message` 等名称仅为字段说明；MCP 对外固定为上述 `metadata/data`。

- 描述：查询机构指定报告期机构持股汇总（机构角度）。按机构 ID + 年份 + 报告期 + 持仓类型查询，返回该机构在该报告期的持仓明细列表（持股数量、市值、净值/流通/总股本比例）、前 10 大持仓占比等。`institution_id`、`report_type`、`year`、`invest_type` 均必填。提示：缺少必填参数时返回错误。
- 数据范围：按报告期，以服务端返回为准
- 单次限量：单个机构单个报告期通常返回 1 条聚合记录
- 提示：
  - `report_type` 取值：q1 / q2 / q3 / annual / announcement。
  - `invest_type` 取值：all / stock / fund / bond / other。
  - 缺少必填参数时返回错误。

## 输入参数

| 名称 | 类型 | 必选 | 描述 |
|------|------|------|------|
| institution_id | string | Y | 机构 ID |
| year | int | Y | 年份 |
| report_type | ReportType | Y | 报告类型：q1 / q2 / q3 / annual / announcement |
| invest_type | InvestType | Y | 持仓类型：all / stock / fund / bond / other |

## 输出参数

> MCP 固定输出信封为 `structuredContent.metadata` + `structuredContent.data`；`content[0].text` 是与其同值的序列化 JSON，不是 Markdown。
>
> `items` / `records` / `code` / `message` 等传输字段不会直接出现在 MCP 结果中；分页与截断信息统一归入 `metadata`。

| MCP 字段 | 类型 | 必填 | 描述 |
|----------|------|------|------|
| metadata | object | Y | 契约版本、数据来源、工具名、业务口径、总量、分页、返回条数、截断状态及 warnings |
| data | array | Y | 归一化后的业务数据项；元素字段见下方 |

### data 业务字段

GetStockInstitutionShareHoldingsResponse：

| 名称 | 类型 | 默认显示 | 描述 |
|------|------|---------|------|
| year | int | Y | 年份 |
| report_type | string | Y | 报告类型 |
| institution_type | string | Y | 机构类型 |
| invest_type | string | Y | 持仓类型 |
| institution_id | string | Y | 机构 ID |
| institution_code | string | Y | 机构代码 |
| institution_main_code | string | Y | 机构主编码 |
| institution_name | string | Y | 机构名称 |
| stock_hold_details | array[StockHoldDetail] | Y | 持仓明细列表 |
| top10_holding_ratio | decimal | Y | 前 10 大持仓占比 |

StockHoldDetail：

| 名称 | 类型 | 默认显示 | 描述 |
|------|------|---------|------|
| stock_code | string | Y | 股票代码 |
| stock_name | string | Y | 股票名称 |
| hold_shares | decimal | Y | 持股数量 |
| mkt_value | decimal | Y | 市值（万元） |
| nv_ratio | decimal | N | 净值比例 |
| circ_ratio | decimal | Y | 流通比例 |
| total_share_ratio | decimal | Y | 总股本比例 |

## 调用方法（MCP）

> MCP 工具名 `ft_get_stock_institution_share_holdings`。MCP Streamable HTTP 要求**先 initialize 拿 `Mcp-Session-Id`，发送 `notifications/initialized`，再 `tools/call`**，后续请求同时带该 Session ID 和协商后的 `MCP-Protocol-Version`。返回统一 `metadata/data` 结构化输出；`content[0].text` 为同值 JSON 文本，不额外返回 Markdown。

**curl**：

```bash
SID=$(curl -sS -m 10 -D - -o /dev/null -X POST <MCP_BASE_URL> \
  -H "Accept: application/json, text/event-stream" -H "Content-Type: application/json" \
  -d '{"jsonrpc":"2.0","id":1,"method":"initialize","params":{"protocolVersion":"2025-11-25","capabilities":{},"clientInfo":{"name":"t","version":"1"}}}' \
  | grep -i 'mcp-session-id:' | awk '{print $2}' | tr -d '\r')

curl -fsS -m 10 -o /dev/null -X POST <MCP_BASE_URL> \
  -H "Accept: application/json, text/event-stream" -H "Content-Type: application/json" \
  -H "Mcp-Session-Id: $SID" \
  -H "MCP-Protocol-Version: 2025-11-25" \
  -d '{"jsonrpc":"2.0","method":"notifications/initialized"}'

curl -sS -m 10 -X POST <MCP_BASE_URL> \
  -H "Accept: application/json, text/event-stream" -H "Content-Type: application/json" \
  -H "Mcp-Session-Id: $SID" \
  -H "MCP-Protocol-Version: 2025-11-25" \
  -d '{"jsonrpc":"2.0","id":2,"method":"tools/call","params":{"name":"ft_get_stock_institution_share_holdings","arguments":{"institution_id": "JPMORGAN", "year": 2024, "report_type": "annual", "invest_type": "all"}}}'
```

**Python（`mcp` SDK，自动握手管理 session）**：

```python
import asyncio
from mcp import ClientSession
from mcp.client.streamable_http import streamablehttp_client

async def main():
    async with streamablehttp_client('<MCP_BASE_URL>') as (r, w, _):
        async with ClientSession(r, w) as s:
            await s.initialize()
            res = await s.call_tool('ft_get_stock_institution_share_holdings', {"institution_id": "JPMORGAN", "year": 2024, "report_type": "annual", "invest_type": "all"})
            print(res.structuredContent)   # 推荐：统一 metadata/data 结构化输出
            print(res.content[0].text)   # 兼容：与 structuredContent 同值的 JSON 文本
asyncio.run(main())
```

## 数据样例

| institution_name | institution_code | report_type | invest_type | top10_holding_ratio | stock_hold_details |
|------------------|------------------|-------------|-------------|---------------------|--------------------|
| ... | ... | ... | ... | ... | ... |

> 示例调用已验证通过，真实样例可从 `structuredContent.data` 查看。
