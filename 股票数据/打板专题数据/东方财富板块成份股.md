# 东方财富板块成份股（MCP 工具 `ft_eastmoney_board_constituents`）

> **MCP 工具**：`ft_eastmoney_board_constituents`（category: `股票数据/打板专题数据`）。返回统一 MCP 输出：`structuredContent.metadata` + `structuredContent.data`；`content[0].text` 是同值的序列化 JSON，不额外返回 Markdown。输入参数 / 输出参数 / 数据样例见下文。
> 文中 `Response`、`items`、`records`、`code`、`message` 等名称仅为字段说明；MCP 对外固定为上述 `metadata/data`。

- 描述：按板块代码查询东方财富概念板块的成份股列表，返回板块代码、板块名称及成份股数组（每只含 6 位股票代码 + 中文名称）。典型板块成份股数量从几十到两百不等（如 BK1024 绿色电力约 168 只）。提示：`board_code` 必填，取东方财富概念板块代码（BK 开头）；`stock_code` 为 6 位代码，不含交易所后缀。
- 数据范围：无时间维度，实时/最新快照
- 单次限量：无分页，一次返回该板块全部成份股
- 提示：
  - `board_code` 必填，取东方财富概念板块代码（BK 开头），未知代码会返回 404。
  - `stock_code` 为 6 位代码，**不含交易所后缀**。

## 输入参数

| 名称 | 类型 | 必选 | 描述 |
|------|------|------|------|
| board_code | string | Y | 板块代码，如 BK1024 |

## 输出参数

> MCP 固定输出信封为 `structuredContent.metadata` + `structuredContent.data`；`content[0].text` 是与其同值的序列化 JSON，不是 Markdown。
>
> `items` / `records` / `code` / `message` 等传输字段不会直接出现在 MCP 结果中；分页与截断信息统一归入 `metadata`。

| MCP 字段 | 类型 | 必填 | 描述 |
|----------|------|------|------|
| metadata | object | Y | 契约版本、数据来源、工具名、业务口径、总量、分页、返回条数、截断状态及 warnings |
| data | array | Y | 归一化后的业务数据项；元素字段见下方 |

### data 业务字段

| 名称 | 类型 | 默认显示 | 描述 |
|------|------|---------|------|
| board_code | string | Y | 板块代码 |
| board_name | string | Y | 板块名称 |
| constituents | array | Y | 成份股列表 |

EastmoneyBoardConstituent：

| 名称 | 类型 | 默认显示 | 描述 |
|------|------|---------|------|
| stock_code | string | Y | 股票代码（6 位，不含交易所后缀） |
| stock_name | string | Y | 股票名称（中文） |

## 调用方法（MCP）

> MCP 工具名 `ft_eastmoney_board_constituents`。MCP Streamable HTTP 要求**先 initialize 拿 `Mcp-Session-Id`，发送 `notifications/initialized`，再 `tools/call`**，后续请求同时带该 Session ID 和协商后的 `MCP-Protocol-Version`。返回统一 `metadata/data` 结构化输出；`content[0].text` 为同值 JSON 文本，不额外返回 Markdown。

**curl**：

```bash
SID=$(curl -sS -m 10 -D - -o /dev/null -X POST <MCP_BASE_URL> \
  -H "Accept: application/json, text/event-stream" -H "Content-Type: application/json" \
  -d '{"jsonrpc":"2.0","id":1,"method":"initialize","params":{"protocolVersion":"2025-11-25","capabilities":{},"clientInfo":{"name":"t","version":"1"}}}' \
  | grep -i 'mcp-session-id:' | awk '{print $2}' | tr -d '\r')

curl -fsS -m 10 -o /dev/null -X POST <MCP_BASE_URL> \
  -H "Accept: application/json, text/event-stream" -H "Content-Type: application/json" \
  -H "Mcp-Session-Id: $SID" \
  -H "MCP-Protocol-Version: 2025-11-25" \
  -d '{"jsonrpc":"2.0","method":"notifications/initialized"}'

curl -sS -m 10 -X POST <MCP_BASE_URL> \
  -H "Accept: application/json, text/event-stream" -H "Content-Type: application/json" \
  -H "Mcp-Session-Id: $SID" \
  -H "MCP-Protocol-Version: 2025-11-25" \
  -d '{"jsonrpc":"2.0","id":2,"method":"tools/call","params":{"name":"ft_eastmoney_board_constituents","arguments":{"board_code": "BK0425"}}}'
```

**Python（`mcp` SDK，自动握手管理 session）**：

```python
import asyncio
from mcp import ClientSession
from mcp.client.streamable_http import streamablehttp_client

async def main():
    async with streamablehttp_client('<MCP_BASE_URL>') as (r, w, _):
        async with ClientSession(r, w) as s:
            await s.initialize()
            res = await s.call_tool('ft_eastmoney_board_constituents', {"board_code": "BK0425"})
            print(res.structuredContent)   # 推荐：统一 metadata/data 结构化输出
            print(res.content[0].text)   # 兼容：与 structuredContent 同值的 JSON 文本
asyncio.run(main())
```

## 数据样例

BK1024 绿色电力（节选）：

| board_code | board_name | stock_code | stock_name |
|------------|------------|------------|------------|
| BK1024 | 绿色电力 | 001896 | 豫能控股 |
| BK1024 | 绿色电力 | 001258 | 立新能源 |
| BK1024 | 绿色电力 | 603693 | 江苏新能 |
| BK1024 | 绿色电力 | 300304 | 云意电气 |
| ... | ... | ... | ... |
