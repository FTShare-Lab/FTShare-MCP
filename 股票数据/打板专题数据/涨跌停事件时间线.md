# 涨跌停事件时间线（MCP 工具 `ft_limit_event_timeline_3s`）

> **MCP 工具**：`ft_limit_event_timeline_3s`（category: `股票数据/打板专题数据`）。返回统一 MCP 输出：`structuredContent.metadata` + `structuredContent.data`；`content[0].text` 是同值的序列化 JSON，不额外返回 Markdown。输入参数 / 输出参数 / 数据样例见下文。
> 文中 `Response`、`items`、`records`、`code`、`message` 等名称仅为字段说明；MCP 对外固定为上述 `metadata/data`。

- 描述：查询股票涨跌停事件的 3 秒级时间线，包括涨停价、跌停价、进入涨停、炸板、进入跌停、翘板的时间点，以及首次或最后封板时间、炸板或开板次数和跌停封单资金等。不传 `trade_date` 或指定当日时查询实时快照，指定历史交易日时查询当日事件；传 `symbol` 查询单只股票，不传时查询全市场股票并排除可转债等非股票标的。
- 数据范围：当日为 3 秒级实时快照；历史交易日为该交易日的已落库事件时间线。
- 单次限量：无分页，全量返回最新快照（单只或全市场）
- 提示：
  - `symbol` 可选，传则查单只；不传返回全市场（已过滤非股票标的）。
  - `symbol` 找不到或非股票标的时返回错误。
  - `trade_date` 为可选参数，格式 `YYYYMMDD`；不传或传当日时查询实时数据；超出数据范围时返回 `{"msg":"系统错误"}`。
  - 价格字段：有对应涨/跌停进入记录时返回真实价，否则为 0。
  - 返回为裸 JSON 数组，无 code/message 信封。

## 输入参数

| 名称 | 类型 | 必选 | 描述 |
|------|------|------|------|
| symbol | string | N | 标的代码，如 000001.XSHE；不传返回全市场 |
| trade_date | string | N | 交易日期，格式 `YYYYMMDD`；不传或传当日时查询实时数据 |

## 输出参数

> MCP 固定输出信封为 `structuredContent.metadata` + `structuredContent.data`；`content[0].text` 是与其同值的序列化 JSON，不是 Markdown。
>
> `items` / `records` / `code` / `message` 等传输字段不会直接出现在 MCP 结果中；分页与截断信息统一归入 `metadata`。

| MCP 字段 | 类型 | 必填 | 描述 |
|----------|------|------|------|
| metadata | object | Y | 契约版本、数据来源、工具名、业务口径、总量、分页、返回条数、截断状态及 warnings |
| data | array | Y | 归一化后的业务数据项 |

### data 业务字段

| 名称 | 类型 | 默认显示 | 描述 |
|------|------|---------|------|
| first_limit_up_time | string | Y | 首次封涨停时间 HH:mm:ss，无封板记录时为 null |
| last_limit_down_time | string | Y | 最后封跌停时间 HH:mm:ss，无封板记录时为 null |
| limit_down_break | array | Y | 翘板时间数组，格式 HH:mm:ss |
| limit_down_break_count | int | Y | 开板次数（跌停封板后被打开次数，即翘板次数） |
| limit_down_enter | array | Y | 进入跌停的时间点数组，格式 HH:mm:ss |
| limit_down_price | decimal | Y | 跌停价，单位元；无跌停记录时为 0 |
| limit_down_seal_value | decimal | Y | 跌停封单资金，单位元；跌停时累加卖一跌停价挂单额，其余为 0 |
| limit_up_break | array | Y | 炸板时间数组，格式 HH:mm:ss |
| limit_up_break_count | int | Y | 炸板次数（涨停封板后被打开次数） |
| limit_up_enter | array | Y | 进入涨停的时间点数组，格式 HH:mm:ss |
| limit_up_price | decimal | Y | 涨停价，单位元；无涨停记录时为 0 |
| ready | bool | Y | 当日涨跌停价格是否已就绪 |
| status | string | Y | 当前状态：normal / limit_up / limit_down |
| symbol | string | Y | 标的代码 |
| trade_date | string | Y | 交易日 YYYYMMDD |

## 调用方法（MCP）

> MCP Streamable HTTP 要求**先 initialize 拿 `Mcp-Session-Id`，发送 `notifications/initialized`，再 `tools/call`**，后续请求同时带该 Session ID 和协商后的 `MCP-Protocol-Version`。返回统一 `metadata/data` 结构化输出；`content[0].text` 为同值 JSON 文本，不额外返回 Markdown。

**curl**：

```bash
set -euo pipefail

MCP_BASE_URL="<MCP_BASE_URL>"

check_mcp_response() {
  local response=$1
  printf '%s\n' "$response"
  if printf '%s\n' "$response" | grep -Eq '"isError"[[:space:]]*:[[:space:]]*true|"error"[[:space:]]*:[[:space:]]*\{'; then
    return 1
  fi
}

SID=$(curl -fsS -m 60 -D - -o /dev/null -X POST "$MCP_BASE_URL" \
  -H "Accept: application/json, text/event-stream" \
  -H "Content-Type: application/json" \
  -d '{"jsonrpc":"2.0","id":1,"method":"initialize","params":{"protocolVersion":"2025-11-25","capabilities":{},"clientInfo":{"name":"ftshare-doc-example","version":"1.0.0"}}}' \
  | awk 'tolower($1)=="mcp-session-id:" {print $2}' \
  | tr -d '\r')

if [ -z "$SID" ]; then
  printf '%s\n' 'initialize 未返回 Mcp-Session-Id' >&2
  exit 1
fi

curl -fsS -m 60 -o /dev/null -X POST "$MCP_BASE_URL" \
  -H "Accept: application/json, text/event-stream" \
  -H "Content-Type: application/json" \
  -H "Mcp-Session-Id: $SID" \
  -H "MCP-Protocol-Version: 2025-11-25" \
  -d '{"jsonrpc":"2.0","method":"notifications/initialized"}'

CALL_RESPONSE=$(curl -fsS -m 60 -X POST "$MCP_BASE_URL" \
  -H "Accept: application/json, text/event-stream" \
  -H "Content-Type: application/json" \
  -H "Mcp-Session-Id: $SID" \
  -H "MCP-Protocol-Version: 2025-11-25" \
  -d '{"jsonrpc":"2.0","id":2,"method":"tools/call","params":{"name":"ft_limit_event_timeline_3s","arguments":{}}}')

check_mcp_response "$CALL_RESPONSE"
```

**Python（`mcp` SDK，自动握手管理 session）**：

```python
import asyncio

from mcp import ClientSession
from mcp.client.streamable_http import streamable_http_client


MCP_BASE_URL = "<MCP_BASE_URL>"


async def main():
    async with streamable_http_client(MCP_BASE_URL) as (read_stream, write_stream):
        async with ClientSession(read_stream, write_stream) as session:
            await session.initialize()
            result = await session.call_tool(
                'ft_limit_event_timeline_3s',
                {},
            )
            if result.is_error:
                raise RuntimeError(result.content[0].text)
            print(result.structured_content)
            print(result.content[0].text)


asyncio.run(main())
```

## 数据样例

| 字段 | 值 |
|------|------|
| null | null |
| null | null |
| null | null |
| ... | ... |
