# 昨日涨停池（MCP 工具 `ft_limit_up_pool_yesterday`）

> **MCP 工具**：`ft_limit_up_pool_yesterday`（category: `股票数据/打板专题数据`）。返回 **markdown 表格文本**（非结构化 JSON）。输入参数 / 输出参数 / 数据样例对照 [ftshare-doc](../../ftshare-doc/api-doc) 原接口。

- 描述：查询前一交易日涨停股池。取 `prev_trading_date`（前一交易日），从 ClickHouse 涨跌停事件表（`TABLE_TIMELINE`，FINAL）查询 `limit_up_enter` 非空的记录，返回结构与「涨停池」一致。用于回看昨日涨停股表现。返回裸数组（非信封）。
- 数据范围：前一交易日（`prev_trading_date`）快照，无更早历史序列
- 单次限量：无分页，全量返回前一交易日涨停股池
- 提示：
  - 固定取前一交易日，不接受 `trade_date` 参数指定日期。
  - 返回结构与「涨停池」相同（LimitUpPoolItem）。
  - 返回为裸 JSON 数组，无 code/message 信封。

## 输入参数

| 名称 | 类型 | 必选 | 描述 |
|------|------|------|------|
| - | - | - | 无入参 |

## 输出参数

LimitUpPoolItem：

| 名称 | 类型 | 默认显示 | 描述 |
|------|------|---------|------|
| symbol | string | Y | 标的代码 |
| trade_date | string | Y | 交易日 YYYYMMDD（前一交易日） |
| ready | bool | Y | 当日涨跌停价格是否已就绪 |
| status | string | Y | 当前状态：normal / limit_up / limit_down |
| limit_up_price | decimal | Y | 涨停价，单位元 |
| limit_up_enter | array[string] | Y | 进入涨停的时间点数组，格式 HH:mm:ss |
| limit_up_break | array[string] | Y | 炸板时间数组，格式 HH:mm:ss |
| first_limit_up_time | string | Y | 首次封涨停时间 HH:mm:ss，无封板记录时为 null |
| limit_up_break_count | int | Y | 炸板次数 |

## 调用方法（MCP）

> MCP 工具名 `ft_limit_up_pool_yesterday`。MCP Streamable HTTP 要求**先 initialize 拿 `Mcp-Session-Id`，再 `tools/call`**，后续请求带该 header。返回 **markdown 表格文本**。

**curl**：

```bash
SID=$(curl -sS -m 10 -D - -o /dev/null -X POST <MCP_BASE_URL> \
  -H "Accept: application/json, text/event-stream" -H "Content-Type: application/json" \
  -d '{"jsonrpc":"2.0","id":1,"method":"initialize","params":{"protocolVersion":"2025-11-25","capabilities":{},"clientInfo":{"name":"t","version":"1"}}}' \
  | grep -i 'mcp-session-id:' | awk '{print $2}' | tr -d '')

curl -sS -m 10 -X POST <MCP_BASE_URL> \
  -H "Accept: application/json, text/event-stream" -H "Content-Type: application/json" \
  -H "Mcp-Session-Id: $SID" \
  -d '{"jsonrpc":"2.0","id":2,"method":"tools/call","params":{"name":"ft_limit_up_pool_yesterday","arguments":{}}}'
```

**Python（`mcp` SDK，自动握手管理 session）**：

```python
import asyncio
from mcp import ClientSession
from mcp.client.streamable_http import streamablehttp_client

async def main():
    async with streamablehttp_client('<MCP_BASE_URL>') as (r, w, _):
        async with ClientSession(r, w) as s:
            await s.initialize()
            res = await s.call_tool('ft_limit_up_pool_yesterday', {})
            print(res.content[0].text)   # markdown 表格文本

asyncio.run(main())
```

## 数据样例

前一交易日（2026-06-16）涨停股池，节选：

| symbol | trade_date | limit_up_price | first_limit_up_time |
|--------|------------|----------------|---------------------|
| 000012.XSHE | 20260616 | 4.5200 | 09:37:51 |
| ... | ... | ... | ... |
