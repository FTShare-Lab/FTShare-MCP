# 千股千评（MCP 工具 `ft_stock_comment_em`）

> **MCP 工具**：`ft_stock_comment_em`（category: `股票数据/特色数据`）。返回统一 MCP 输出：`structuredContent.metadata` + `structuredContent.data`；`content[0].text` 是同值的序列化 JSON，不额外返回 Markdown。输入参数 / 输出参数 / 数据样例见下文。
> 文中 `Response`、`items`、`records`、`code`、`message` 等名称仅为字段说明；MCP 对外固定为上述 `metadata/data`。

- 描述：查询东方财富「千股千评」全市场 A 股综合快照，包括证券代码、简称、交易日、收盘价、涨跌幅、换手率、动态市盈率、主力成本、机构参与度、综合评分、排名和市场关注度等。数据来源为东方财富。
- 数据范围：最新一期快照（无时间维度参数，仅返回当前内存中的全市场快照）
- 单次限量：分页返回，`page` 默认 1，`page_size` 默认 50、最大 200
- 提示：
  - 不支持按标的过滤，返回的是全市场快照；如需单票请用 `stock-comment/org-participate`、`/score`、`/focus`、`/desire` 等子接口。
  - 字段值多为字符串形式的数值（如 `"12.5000"`、`"-2.8600"`），消费方需自行解析。
  - `change_rate` 单位为百分比，`turnover_rate` 单位为百分比。

## 输入参数

| 名称 | 类型 | 必选 | 描述 |
|------|------|------|------|
| page | int | N | 页码，默认 1 |
| page_size | int | N | 每页条数，默认 50、最大 200 |

## 输出参数

> MCP 固定输出信封为 `structuredContent.metadata` + `structuredContent.data`；`content[0].text` 是与其同值的序列化 JSON，不是 Markdown。
>
> `items` / `records` / `code` / `message` 等传输字段不会直接出现在 MCP 结果中；分页与截断信息统一归入 `metadata`。

| MCP 字段 | 类型 | 必填 | 描述 |
|----------|------|------|------|
| metadata | object | Y | 契约版本、数据来源、工具名、业务口径、总量、分页、返回条数、截断状态及 warnings |
| data | array | Y | 归一化后的业务数据项 |

### data 业务字段

| 名称 | 类型 | 默认显示 | 描述 |
|------|------|---------|------|
| change_rate | string | Y | 涨跌幅（百分比） |
| close_price | string | Y | 收盘价 |
| focus | string | Y | 市场关注度 |
| org_participate | string | Y | 机构参与度 |
| pe_dynamic | string | Y | 动态市盈率 |
| prime_cost | string | Y | 主力成本 |
| rank | string | Y | 排名 |
| security_code | string | Y | 证券代码（如 000001） |
| security_name_abbr | string | Y | 证券简称 |
| seq | int | Y | 序号 |
| total_score | string | Y | 综合评分 |
| trade_date | string | Y | 交易日（如 2026-05-26） |
| turnover_rate | string | Y | 换手率（百分比） |

## 调用方法（MCP）

> MCP Streamable HTTP 要求**先 initialize 拿 `Mcp-Session-Id`，发送 `notifications/initialized`，再 `tools/call`**，后续请求同时带该 Session ID 和协商后的 `MCP-Protocol-Version`。返回统一 `metadata/data` 结构化输出；`content[0].text` 为同值 JSON 文本，不额外返回 Markdown。

**curl**：

```bash
set -euo pipefail

MCP_BASE_URL="<MCP_BASE_URL>"

check_mcp_response() {
  local response=$1
  printf '%s\n' "$response"
  if printf '%s\n' "$response" | grep -Eq '"isError"[[:space:]]*:[[:space:]]*true|"error"[[:space:]]*:[[:space:]]*\{'; then
    return 1
  fi
}

SID=$(curl -fsS -m 60 -D - -o /dev/null -X POST "$MCP_BASE_URL" \
  -H "Accept: application/json, text/event-stream" \
  -H "Content-Type: application/json" \
  -d '{"jsonrpc":"2.0","id":1,"method":"initialize","params":{"protocolVersion":"2025-11-25","capabilities":{},"clientInfo":{"name":"ftshare-doc-example","version":"1.0.0"}}}' \
  | awk 'tolower($1)=="mcp-session-id:" {print $2}' \
  | tr -d '\r')

if [ -z "$SID" ]; then
  printf '%s\n' 'initialize 未返回 Mcp-Session-Id' >&2
  exit 1
fi

curl -fsS -m 60 -o /dev/null -X POST "$MCP_BASE_URL" \
  -H "Accept: application/json, text/event-stream" \
  -H "Content-Type: application/json" \
  -H "Mcp-Session-Id: $SID" \
  -H "MCP-Protocol-Version: 2025-11-25" \
  -d '{"jsonrpc":"2.0","method":"notifications/initialized"}'

CALL_RESPONSE=$(curl -fsS -m 60 -X POST "$MCP_BASE_URL" \
  -H "Accept: application/json, text/event-stream" \
  -H "Content-Type: application/json" \
  -H "Mcp-Session-Id: $SID" \
  -H "MCP-Protocol-Version: 2025-11-25" \
  -d '{"jsonrpc":"2.0","id":2,"method":"tools/call","params":{"name":"ft_stock_comment_em","arguments":{"page":1,"page_size":2}}}')

check_mcp_response "$CALL_RESPONSE"
```

**Python（`mcp` SDK，自动握手管理 session）**：

```python
import asyncio

from mcp import ClientSession
from mcp.client.streamable_http import streamable_http_client


MCP_BASE_URL = "<MCP_BASE_URL>"


async def main():
    async with streamable_http_client(MCP_BASE_URL) as (read_stream, write_stream):
        async with ClientSession(read_stream, write_stream) as session:
            await session.initialize()
            result = await session.call_tool(
                'ft_stock_comment_em',
                {'page': 1, 'page_size': 2},
            )
            if result.is_error:
                raise RuntimeError(result.content[0].text)
            print(result.structured_content)
            print(result.content[0].text)


asyncio.run(main())
```

## 数据样例

| seq | security_code | security_name_abbr | trade_date | close_price | change_rate | turnover_rate | pe_dynamic | prime_cost | org_participate | total_score | rank | focus |
|------|------|------|------|------|------|------|------|------|------|------|------|------|
| 0 | 000001 | 平安银行 | 2026-08-07 | 11.1900 | -0.70980000 | 0.45500000 | 3.73807451 | 11.171001420071 | 0.41707480 | 68.90614803 | 1571 | 89.20000000 |
| 1 | 000002 | 万  科Ａ | 2026-08-07 | 3.2200 | -1.22700000 | 1.64770000 | -1.61357007 | 3.194760274501 | 0.38640920 | 59.62433207 | 4123 | 75.60000000 |
| ... | ... | ... | ... | ... | ... | ... | ... | ... | ... | ... | ... | ... |
