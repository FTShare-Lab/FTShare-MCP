# 千股千评（MCP 工具 `ft_stock_comment_em`）

> **MCP 工具**：`ft_stock_comment_em`（category: `股票数据/特色数据`）。返回统一 MCP 输出：`structuredContent.metadata` + `structuredContent.data`；`content[0].text` 是同值的序列化 JSON，不额外返回 Markdown。输入参数 / 输出参数 / 数据样例见下文。
> 文中 `Response`、`items`、`records`、`code`、`message` 等名称仅为字段说明；MCP 对外固定为上述 `metadata/data`。

- 描述：查询东方财富「千股千评」主表数据。返回全市场 A 股的千股千评综合快照，包含证券代码、简称、交易日、收盘价、涨跌幅、换手率、动态市盈率、主力成本、机构参与度、综合评分、排名、市场关注度等字段。提示：不支持按标的过滤，返回的是全市场快照；`change_rate` 单位为百分比，`turnover_rate` 单位为百分比。
- 数据范围：最新一期快照（无时间维度参数，仅返回当前内存中的全市场快照）
- 单次限量：分页返回，`page` 默认 1，`page_size` 默认 50、最大 200
- 提示：
  - 不支持按标的过滤，返回的是全市场快照；如需单票请用 `stock-comment/org-participate`、`/score`、`/focus`、`/desire` 等子接口。
  - 字段值多为字符串形式的数值（如 `"12.5000"`、`"-2.8600"`），消费方需自行解析。
  - `change_rate` 单位为百分比，`turnover_rate` 单位为百分比。

## 输入参数

| 名称 | 类型 | 必选 | 描述 |
|------|------|------|------|
| page | int | N | 页码，默认 1 |
| page_size | int | N | 每页条数，默认 50、最大 200 |

## 输出参数

> MCP 固定输出信封为 `structuredContent.metadata` + `structuredContent.data`；`content[0].text` 是与其同值的序列化 JSON，不是 Markdown。
>
> `items` / `records` / `code` / `message` 等传输字段不会直接出现在 MCP 结果中；分页与截断信息统一归入 `metadata`。

| MCP 字段 | 类型 | 必填 | 描述 |
|----------|------|------|------|
| metadata | object | Y | 契约版本、数据来源、工具名、业务口径、总量、分页、返回条数、截断状态及 warnings |
| data | array | Y | 归一化后的业务数据项；元素字段见下方 |

### data 业务字段

StockCommentItem：

| 名称 | 类型 | 默认显示 | 描述 |
|------|------|---------|------|
| seq | int | Y | 序号 |
| security_code | string | Y | 证券代码（如 000001） |
| security_name_abbr | string | Y | 证券简称 |
| trade_date | string | Y | 交易日（如 2026-05-26） |
| close_price | string | Y | 收盘价 |
| change_rate | string | Y | 涨跌幅（百分比） |
| turnover_rate | string | Y | 换手率（百分比） |
| pe_dynamic | string | Y | 动态市盈率 |
| prime_cost | string | Y | 主力成本 |
| org_participate | string | Y | 机构参与度 |
| total_score | string | Y | 综合评分 |
| rank | string | Y | 排名 |
| focus | string | Y | 市场关注度 |

## 调用方法（MCP）

> MCP 工具名 `ft_stock_comment_em`。MCP Streamable HTTP 要求**先 initialize 拿 `Mcp-Session-Id`，发送 `notifications/initialized`，再 `tools/call`**，后续请求同时带该 Session ID 和协商后的 `MCP-Protocol-Version`。返回统一 `metadata/data` 结构化输出；`content[0].text` 为同值 JSON 文本，不额外返回 Markdown。

**curl**：

```bash
SID=$(curl -sS -m 10 -D - -o /dev/null -X POST <MCP_BASE_URL> \
  -H "Accept: application/json, text/event-stream" -H "Content-Type: application/json" \
  -d '{"jsonrpc":"2.0","id":1,"method":"initialize","params":{"protocolVersion":"2025-11-25","capabilities":{},"clientInfo":{"name":"t","version":"1"}}}' \
  | grep -i 'mcp-session-id:' | awk '{print $2}' | tr -d '\r')

curl -fsS -m 10 -o /dev/null -X POST <MCP_BASE_URL> \
  -H "Accept: application/json, text/event-stream" -H "Content-Type: application/json" \
  -H "Mcp-Session-Id: $SID" \
  -H "MCP-Protocol-Version: 2025-11-25" \
  -d '{"jsonrpc":"2.0","method":"notifications/initialized"}'

curl -sS -m 10 -X POST <MCP_BASE_URL> \
  -H "Accept: application/json, text/event-stream" -H "Content-Type: application/json" \
  -H "Mcp-Session-Id: $SID" \
  -H "MCP-Protocol-Version: 2025-11-25" \
  -d '{"jsonrpc":"2.0","id":2,"method":"tools/call","params":{"name":"ft_stock_comment_em","arguments":{}}}'
```

**Python（`mcp` SDK，自动握手管理 session）**：

```python
import asyncio
from mcp import ClientSession
from mcp.client.streamable_http import streamablehttp_client

async def main():
    async with streamablehttp_client('<MCP_BASE_URL>') as (r, w, _):
        async with ClientSession(r, w) as s:
            await s.initialize()
            res = await s.call_tool('ft_stock_comment_em', {})
            print(res.structuredContent)   # 推荐：统一 metadata/data 结构化输出
            print(res.content[0].text)   # 兼容：与 structuredContent 同值的 JSON 文本
asyncio.run(main())
```

## 数据样例

全市场快照共 77770 条，节选（symbol=000001.SZ 平安银行 2026-06-16）：

```json
{
  "code": 0,
  "message": "OK",
  "data": {
    "pageNum": 1,
    "pageSize": 50,
    "total": 77770,
    "pages": 1556,
    "records": [
      {
        "seq": 0,
        "security_code": "000001",
        "security_name_abbr": "平安银行",
        "trade_date": "2026-06-16",
        "close_price": "...",
        "main_cost": "...",
        "trend": "..."
      }
    ]
  }
}
```

| seq | security_code | security_name_abbr | trade_date | close_price | change_rate | turnover_rate | pe_dynamic | prime_cost | org_participate | total_score | rank | focus |
|-----|---------------|--------------------|------------|-------------|-------------|---------------|------------|------------|-----------------|-------------|------|-------|
| 0 | 000001 | 平安银行 | 2026-06-16 | ... | ... | ... | ... | ... | ... | ... | ... | ... |
| ... | ... | ... | ... | ... | ... | ... | ... | ... | ... | ... | ... | ... |
