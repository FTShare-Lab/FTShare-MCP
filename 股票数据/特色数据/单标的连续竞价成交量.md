# 单标的连续竞价成交量（MCP 工具 `ft_stock_intraday_auction_volume_symbol`）

> **MCP 工具**：`ft_stock_intraday_auction_volume_symbol`（category: `股票数据/特色数据`）。返回统一 MCP 输出：`structuredContent.metadata` + `structuredContent.data`；`content[0].text` 是同值的序列化 JSON，不额外返回 Markdown。输入参数 / 输出参数 / 数据样例见下文。
> 文中 `Response`、`items`、`records`、`code`、`message` 等名称仅为字段说明；MCP 对外固定为上述 `metadata/data`。

- 描述：查询单只标的的连续竞价成交占比，按分钟返回成交量和成交额占比分时序列。提示：`symbol` 必填，支持 `.SH/.SZ/.BJ` 及长交易所后缀；`trade_date` 格式 YYYYMMDD，不传或传入当天时返回实时数据。
- 数据范围：以服务端返回为准（按交易日分钟级更新）
- 单次限量：分页返回，默认 `page=1`、`page_size=50`，`page_size` 最大 200
- 提示：
  - `symbol` 格式 `code.suffix`（如 `600000.SH`），suffix 支持 XSHG/SH、XSHE/SZ、BJSE/BJ。
  - `trade_date` 格式 YYYYMMDD；不传返回当日实时数据，传入当日会降级为实时。
  - `volume_ratio` = 该分钟成交量 / 全天总成交量；`turnover_ratio` = 该分钟成交额 / 全天总成交额。
  - 缺 `symbol` 返回空 records。

## 输入参数

| 名称 | 类型 | 必选 | 描述 |
|------|------|------|------|
| symbol | string | Y | 标的代码，格式 code.suffix（XSHG/SH、XSHE/SZ、BJSE/BJ） |
| trade_date | string | N | 交易日期 YYYYMMDD；不传返回当日实时数据 |
| page | int | N | 页码，从 1 开始，默认 1 |
| page_size | int | N | 每页数量，默认 50，最大 200 |

## 输出参数

> MCP 固定输出信封为 `structuredContent.metadata` + `structuredContent.data`；`content[0].text` 是与其同值的序列化 JSON，不是 Markdown。
>
> `items` / `records` / `code` / `message` 等传输字段不会直接出现在 MCP 结果中；分页与截断信息统一归入 `metadata`。

| MCP 字段 | 类型 | 必填 | 描述 |
|----------|------|------|------|
| metadata | object | Y | 契约版本、数据来源、工具名、业务口径、总量、分页、返回条数、截断状态及 warnings |
| data | array | Y | 归一化后的业务数据项；元素字段见下方 |

### data 业务字段

AuctionVolumeItem：

| 名称 | 类型 | 默认显示 | 描述 |
|------|------|---------|------|
| ts_millis | int | Y | 该分钟收盘时间戳，单位毫秒 |
| volume | int | Y | 该分钟成交量 |
| volume_ratio | f64 | Y | 成交量占比 = 该分钟成交量 / 全天总成交量 |
| turnover | string | Y | 该分钟成交额，单位元 |
| turnover_ratio | f64 | Y | 成交额占比 = 该分钟成交额 / 全天总成交额 |

## 调用方法（MCP）

> MCP 工具名 `ft_stock_intraday_auction_volume_symbol`。MCP Streamable HTTP 要求**先 initialize 拿 `Mcp-Session-Id`，发送 `notifications/initialized`，再 `tools/call`**，后续请求同时带该 Session ID 和协商后的 `MCP-Protocol-Version`。返回统一 `metadata/data` 结构化输出；`content[0].text` 为同值 JSON 文本，不额外返回 Markdown。

**curl**：

```bash
set -euo pipefail

MCP_BASE_URL="<MCP_BASE_URL>"

check_mcp_response() {
  local response=$1
  printf '%s\n' "$response"
  # MCP 业务与协议错误仍可能使用 HTTP 200，必须检查 JSON-RPC 响应。
  if printf '%s\n' "$response" | grep -Eq '"isError"[[:space:]]*:[[:space:]]*true|"error"[[:space:]]*:[[:space:]]*\{'; then
    return 1
  fi
}

SID=$(curl -fsS -m 60 -D - -o /dev/null -X POST "$MCP_BASE_URL" \
  -H "Accept: application/json, text/event-stream" \
  -H "Content-Type: application/json" \
  -d '{"jsonrpc":"2.0","id":1,"method":"initialize","params":{"protocolVersion":"2025-11-25","capabilities":{},"clientInfo":{"name":"ftshare-doc-example","version":"1.0.0"}}}' \
  | awk 'tolower($1)=="mcp-session-id:" {print $2}' \
  | tr -d '\r')

if [ -z "$SID" ]; then
  printf '%s\n' 'initialize 未返回 Mcp-Session-Id' >&2
  exit 1
fi

curl -fsS -m 60 -o /dev/null -X POST "$MCP_BASE_URL" \
  -H "Accept: application/json, text/event-stream" \
  -H "Content-Type: application/json" \
  -H "Mcp-Session-Id: $SID" \
  -H "MCP-Protocol-Version: 2025-11-25" \
  -d '{"jsonrpc":"2.0","method":"notifications/initialized"}'

CALL_RESPONSE=$(curl -fsS -m 60 -X POST "$MCP_BASE_URL" \
  -H "Accept: application/json, text/event-stream" \
  -H "Content-Type: application/json" \
  -H "Mcp-Session-Id: $SID" \
  -H "MCP-Protocol-Version: 2025-11-25" \
  -d '{"jsonrpc":"2.0","id":2,"method":"tools/call","params":{"name":"ft_stock_intraday_auction_volume_symbol","arguments":{"symbol":"600584.SH"}}}')

check_mcp_response "$CALL_RESPONSE"
```

**Python（`mcp` SDK，自动握手管理 session）**：

```python
import asyncio

from mcp import ClientSession
from mcp.client.streamable_http import streamable_http_client


MCP_BASE_URL = "<MCP_BASE_URL>"


async def main():
    async with streamable_http_client(MCP_BASE_URL) as (read_stream, write_stream):
        async with ClientSession(read_stream, write_stream) as session:
            await session.initialize()
            result = await session.call_tool(
                'ft_stock_intraday_auction_volume_symbol',
                {'symbol': '600584.SH'},
            )
            if result.is_error:
                raise RuntimeError(result.content[0].text)
            print(result.structured_content)
            print(result.content[0].text)


asyncio.run(main())
```

## 数据样例

`symbol=000001.SZ` 分钟级个股快照（records 每条一分钟），核心列：

| ts_millis | volume | turnover | volume_ratio | turnover_ratio |
|-----------|--------|----------|--------------|----------------|
| 1781659800000 | 605800 | 6645626 | 0.006272338151130279 | 0.006349941631255491 |
| 1781659860000 | 2077000 | ... | ... | ... |
| ... | ... | ... | ... | ... |
