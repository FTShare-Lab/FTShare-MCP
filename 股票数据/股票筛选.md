# 股票筛选（MCP 工具 `ft_stock_filter`）

> **MCP 工具**：`ft_stock_filter`（category: `股票数据/行情数据`）。返回统一 MCP 输出：`structuredContent.metadata` + `structuredContent.data`；`content[0].text` 是同值的序列化 JSON，不额外返回 Markdown。输入参数 / 输出参数 / 数据样例见下文。
> 文中 `Response`、`items`、`records`、`code`、`message` 等名称仅为字段说明；MCP 对外固定为上述 `metadata/data`。

- 描述：按板块、交易所和上市日期筛选 A 股实时行情，返回价格、涨跌幅、量额、换手率、市值及多周期涨跌幅等指标。提示：支持 `board`、`listing_date_since`、`page` 和 `page_size`。
- 数据范围：实时计算（基于实时行情快照，无固定时间维度）
- 单次限量：分页返回，默认 `page=1`，page_size 由后端定
- 提示：
  - `board` 取值：`star`（科创板）/ `chi_next`（创业板）/ `bjse`（北交所）/ `xshg`（沪市）/ `xshe`（深市）/ `main`（主板）。
  - 支持新股（按 `listing_date_since` 筛选）。
  - 大量字段为可选（实时行情未覆盖时为空）。

## 输入参数

| 名称 | 类型 | 必选 | 描述 |
|------|------|------|------|
| board | string | N | 板块/交易所筛选：`star` / `chi_next` / `bjse` / `xshg` / `xshe` / `main` |
| listing_date_since | string | N | 上市日期起点 YYYYMMDD，筛选此后上市的股票 |
| page | uint32 | N | 页码 |
| page_size | uint32 | N | 每页条数 |

## 输出参数

> MCP 固定输出信封为 `structuredContent.metadata` + `structuredContent.data`；`content[0].text` 是与其同值的序列化 JSON，不是 Markdown。
>
> `items` / `records` / `code` / `message` 等传输字段不会直接出现在 MCP 结果中；分页与截断信息统一归入 `metadata`。

| MCP 字段 | 类型 | 必填 | 描述 |
|----------|------|------|------|
| metadata | object | Y | 契约版本、数据来源、工具名、业务口径、总量、分页、返回条数、截断状态及 warnings |
| data | array | Y | 归一化后的业务数据项；元素字段见下方 |

### data 业务字段

StockFilterItem：

| 名称 | 类型 | 默认显示 | 描述 |
|------|------|---------|------|
| symbol | string | Y | 标的代码 |
| name | string | Y | 股票名称 |
| board | string | N | 板块/交易所 |
| listing_date | string | N | 上市日期 |
| close | string | N | 收盘价 |
| change | string | N | 价格变动 |
| change_rate | float64 | N | 涨跌幅 |
| open | string | N | 开盘价 |
| high | string | N | 最高价 |
| low | string | N | 最低价 |
| prev_close | string | N | 昨收价 |
| volume | int64 | N | 成交量 |
| turnover | string | N | 成交额 |
| amplitude | float64 | N | 振幅 |
| avg | float64 | N | 均价 |
| buy_volume | int64 | N | 买盘成交量 |
| sell_volume | int64 | N | 卖盘成交量 |
| turnover_rate | float64 | N | 换手率 |
| float_a_market_cap | string | N | A 股流通市值 |
| float_a_shares | int64 | N | A 股流通股本 |
| pe_ttm | float64 | N | TTM 市盈率 |
| cum_adjust_factor | float64 | N | 累计复权因子 |
| change_rate_day5 | float64 | N | 5 日涨跌幅 |
| change_rate_day10 | float64 | N | 10 日涨跌幅 |
| change_rate_day20 | float64 | N | 20 日涨跌幅 |
| change_rate_day60 | float64 | N | 60 日涨跌幅 |
| change_rate_day120 | float64 | N | 120 日涨跌幅 |
| change_rate_ytd | float64 | N | 年初至今涨跌幅 |
| limit_up | string | N | 涨停价 |
| limit_down | string | N | 跌停价 |

## 调用方法（MCP）

> MCP 工具名 `ft_stock_filter`。MCP Streamable HTTP 要求**先 initialize 拿 `Mcp-Session-Id`，发送 `notifications/initialized`，再 `tools/call`**，后续请求同时带该 Session ID 和协商后的 `MCP-Protocol-Version`。返回统一 `metadata/data` 结构化输出；`content[0].text` 为同值 JSON 文本，不额外返回 Markdown。

**curl**：

```bash
SID=$(curl -sS -m 10 -D - -o /dev/null -X POST <MCP_BASE_URL> \
  -H "Accept: application/json, text/event-stream" -H "Content-Type: application/json" \
  -d '{"jsonrpc":"2.0","id":1,"method":"initialize","params":{"protocolVersion":"2025-11-25","capabilities":{},"clientInfo":{"name":"t","version":"1"}}}' \
  | grep -i 'mcp-session-id:' | awk '{print $2}' | tr -d '\r')

curl -fsS -m 10 -o /dev/null -X POST <MCP_BASE_URL> \
  -H "Accept: application/json, text/event-stream" -H "Content-Type: application/json" \
  -H "Mcp-Session-Id: $SID" \
  -H "MCP-Protocol-Version: 2025-11-25" \
  -d '{"jsonrpc":"2.0","method":"notifications/initialized"}'

curl -sS -m 10 -X POST <MCP_BASE_URL> \
  -H "Accept: application/json, text/event-stream" -H "Content-Type: application/json" \
  -H "Mcp-Session-Id: $SID" \
  -H "MCP-Protocol-Version: 2025-11-25" \
  -d '{"jsonrpc":"2.0","id":2,"method":"tools/call","params":{"name":"ft_stock_filter","arguments":{}}}'
```

**Python（`mcp` SDK，自动握手管理 session）**：

```python
import asyncio
from mcp import ClientSession
from mcp.client.streamable_http import streamablehttp_client

async def main():
    async with streamablehttp_client('<MCP_BASE_URL>') as (r, w, _):
        async with ClientSession(r, w) as s:
            await s.initialize()
            res = await s.call_tool('ft_stock_filter', {})
            print(res.structuredContent)   # 推荐：统一 metadata/data 结构化输出
            print(res.content[0].text)   # 兼容：与 structuredContent 同值的 JSON 文本
asyncio.run(main())
```

## 数据样例

`board=sz` 板块筛选实时行情，节选：

| symbol | board | change | change_rate | amplitude |
|--------|-------|--------|-------------|-----------|
| 000001.SZ | sz | -0.3300 | -0.027049180327868856 | 0.03442622950819672 |
| ... | ... | ... | ... | ... |
