# 董监高持股变动（MCP 工具 `ft_stock_ggmx_handler`）

> **MCP 工具**：`ft_stock_ggmx_handler`（category: `股票数据/参考数据`）。返回 **markdown 表格文本**（非结构化 JSON）。输入参数 / 输出参数 / 数据样例对照 [ftshare-doc](../../ftshare-doc/api-doc) 原接口。

- 描述：东方财富董监高持股变动明细。支持按股票代码、变动方向（增持/减持）、变动日期范围分页查询。返回变动人、职务、变动股数/金额、变动均价、变动后持股、变动比例、当日收盘价/涨跌幅等明细字段。响应为 `PaginatedApiResponse` 信封（code/message/data），data 为分页对象。字段序列化为 camelCase。
- 数据范围：按变动日期，以服务端返回为准
- 单次限量：分页返回，默认 `page=1`、`page_size` 由后端定
- 提示：
  - 所有入参均为可选，参数支持 camelCase 与 snake_case 双别名。
  - 日期格式为 `YYYY-MM-DD`。
  - 数据来源东方财富。

## 输入参数

| 名称 | 类型 | 必选 | 描述 |
|------|------|------|------|
| stock_code | string | N | 股票代码（如 600001），别名 stockCode |
| change_direction | string | N | 变动方向：增持 / 减持，别名 changeDirection |
| start_date | string | N | 变动日期起始 YYYY-MM-DD，别名 startDate |
| end_date | string | N | 变动日期截止 YYYY-MM-DD，别名 endDate |
| page | int | N | 页码 |
| page_size | int | N | 每页条数，别名 pageSize |

## 输出参数

| 名称 | 类型 | 默认显示 | 描述 |
|------|------|---------|------|
| code | int | Y | 状态码：0 成功 |
| message | string | Y | 状态信息 |
| data | object | Y | 分页对象（见下） |

data ：

| 名称 | 类型 | 默认显示 | 描述 |
|------|------|---------|------|
| pageNum | int | Y | 当前页码 |
| pageSize | int | Y | 每页条数 |
| total | int | Y | 总记录数 |
| pages | int | Y | 总页数 |
| records | array[StockGgmxItem] | Y | 持股变动明细列表 |

StockGgmxItem：

| 名称 | 类型 | 默认显示 | 描述 |
|------|------|---------|------|
| stock_code | string | Y | 股票代码 |
| stock_name | string | Y | 股票名称 |
| change_date | string | Y | 变动日期 |
| notice_date | string | N | 公告日期 |
| register_date | string | N | 注册/登记日期 |
| changer | string | N | 变动人 |
| executive_name | string | N | 高管姓名 |
| relation | string | N | 关系 |
| position | string | N | 职务 |
| change_direction | string | N | 变动方向 |
| change_shares | string | N | 变动股数 |
| change_quantity | string | N | 变动数量 |
| avg_price | string | N | 变动均价 |
| change_amount | string | N | 变动金额 |
| change_reason | string | N | 变动原因 |
| shares_after | string | N | 变动后持股 |
| change_ratio | string | N | 变动比例 |
| close_price | string | N | 收盘价 |
| price_change | string | N | 涨跌幅 |
| total_share | string | N | 总股本 |
| data_time | string | N | 数据时间 |
| quote_price | string | N | 行情价 |
| quote_change | string | N | 行情涨跌幅 |
| crawl_date | string | Y | 抓取日期 |
| source | string | Y | 数据来源 |
| crawl_batch_ts | string | N | 抓取批次时间戳 |

## 调用方法（MCP）

> MCP 工具名 `ft_stock_ggmx_handler`。MCP Streamable HTTP 要求**先 initialize 拿 `Mcp-Session-Id`，再 `tools/call`**，后续请求带该 header。返回 **markdown 表格文本**。

**curl**：

```bash
SID=$(curl -sS -m 10 -D - -o /dev/null -X POST <MCP_BASE_URL> \
  -H "Accept: application/json, text/event-stream" -H "Content-Type: application/json" \
  -d '{"jsonrpc":"2.0","id":1,"method":"initialize","params":{"protocolVersion":"2025-11-25","capabilities":{},"clientInfo":{"name":"t","version":"1"}}}' \
  | grep -i 'mcp-session-id:' | awk '{print $2}' | tr -d '')

curl -sS -m 10 -X POST <MCP_BASE_URL> \
  -H "Accept: application/json, text/event-stream" -H "Content-Type: application/json" \
  -H "Mcp-Session-Id: $SID" \
  -d '{"jsonrpc":"2.0","id":2,"method":"tools/call","params":{"name":"ft_stock_ggmx_handler","arguments":{}}}'
```

**Python（`mcp` SDK，自动握手管理 session）**：

```python
import asyncio
from mcp import ClientSession
from mcp.client.streamable_http import streamablehttp_client

async def main():
    async with streamablehttp_client('<MCP_BASE_URL>') as (r, w, _):
        async with ClientSession(r, w) as s:
            await s.initialize()
            res = await s.call_tool('ft_stock_ggmx_handler', {})
            print(res.content[0].text)   # markdown 表格文本

asyncio.run(main())
```

## 数据样例

`page=1&page_size=5`，total=187675，节选：

| stockCode | stockName | changeDate | noticeDate |
|-----------|-----------|------------|------------|
| 002385 | 大北农 | 2026-06-16 | 2026-06-16 |
| ... | ... | ... | ... |
