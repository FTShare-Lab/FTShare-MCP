# 融资融券明细分页（MCP 工具 `ft_margin_trading_details_paginated`）

> **MCP 工具**：`ft_margin_trading_details_paginated`（category: `股票数据/两融及转融通`）。返回 **markdown 表格文本**（非结构化 JSON）。输入参数 / 输出参数 / 数据样例对照 [ftshare-doc](../../ftshare-doc/api-doc) 原接口。

- 描述：融资融券明细分页查询（v0 内部接口）。与 `margin_trading_details` 行为一致：传入 `date` 时读取文件中心当日 CSV，不传时回退内存 `MARGIN_TRADE` 快照；区别在于显式返回 `PaginatedResponse<MarginTradingDetail>`（含 `total_pages`/`total_items`），而非截断列表。底层 handler 为 `margin_trading_details_paginated`。
- 数据范围：以服务端返回为准（按交易日更新）
- 单次限量：分页返回，默认 `page=1`、`page_size=50`
- 提示：
  - 该接口未在 ft-data-server 对外暴露独立 v1 路径，对外调用请使用 `融资融券明细` 的 v1 路径（其本身已分页）。
  - 字段语义、单位同「融资融券明细」。

## 输入参数

| 名称 | 类型 | 必选 | 描述 |
|------|------|------|------|
| date | string | N | 查询日期 YYYYMMDD；不传则使用当前内存快照 |
| page | int | N | 页码，从 1 开始 |
| page_size | int | N | 每页数量 |

## 输出参数

| 名称 | 类型 | 默认显示 | 描述 |
|------|------|---------|------|
| items | array[MarginTradingDetail] | Y | 融资融券明细列表 |
| total_pages | int | Y | 总页数 |
| total_items | int | Y | 总记录数（未分页前） |

MarginTradingDetail：

| 名称 | 类型 | 默认显示 | 描述 |
|------|------|---------|------|
| date | string | Y | 交易日 |
| symbol | string | Y | 标的代码（SymbolKey） |
| margin_trading_balance | int | Y | 融资余额，单位元 |
| margin_trading_buying_amount | int | Y | 融资买入额，单位元 |
| margin_trading_repayment_amount | int | Y | 融资偿还额，单位元 |
| securities_lending_balance_volume | int | Y | 融券余量，单位股 |
| securities_lending_repayment_volume | string | Y | 融券偿还量，单位股（源数据可能含小数） |
| securities_lending_selling_volume | int | Y | 融券卖出量，单位股 |
| total_balance | int | Y | 融资融券余额，单位元 |

## 调用方法（MCP）

> MCP 工具名 `ft_margin_trading_details_paginated`。MCP Streamable HTTP 要求**先 initialize 拿 `Mcp-Session-Id`，再 `tools/call`**，后续请求带该 header。返回 **markdown 表格文本**。

**curl**：

```bash
SID=$(curl -sS -m 10 -D - -o /dev/null -X POST <MCP_BASE_URL> \
  -H "Accept: application/json, text/event-stream" -H "Content-Type: application/json" \
  -d '{"jsonrpc":"2.0","id":1,"method":"initialize","params":{"protocolVersion":"2025-11-25","capabilities":{},"clientInfo":{"name":"t","version":"1"}}}' \
  | grep -i 'mcp-session-id:' | awk '{print $2}' | tr -d '')

curl -sS -m 10 -X POST <MCP_BASE_URL> \
  -H "Accept: application/json, text/event-stream" -H "Content-Type: application/json" \
  -H "Mcp-Session-Id: $SID" \
  -d '{"jsonrpc":"2.0","id":2,"method":"tools/call","params":{"name":"ft_margin_trading_details_paginated","arguments":{}}}'
```

**Python（`mcp` SDK，自动握手管理 session）**：

```python
import asyncio
from mcp import ClientSession
from mcp.client.streamable_http import streamablehttp_client

async def main():
    async with streamablehttp_client('<MCP_BASE_URL>') as (r, w, _):
        async with ClientSession(r, w) as s:
            await s.initialize()
            res = await s.call_tool('ft_margin_trading_details_paginated', {})
            print(res.content[0].text)   # markdown 表格文本

asyncio.run(main())
```

## 数据样例

| date | symbol | margin_trading_balance | total_balance | ... |
|------|--------|------------------------|---------------|-----|
| ... | ... | ... | ... | ... |

> ft-data-server v1 未单独暴露该分页变体（对外统一走主路径 margin-trading-details，已含分页），样例待补
