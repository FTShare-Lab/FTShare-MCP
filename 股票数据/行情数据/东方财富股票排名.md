# 东方财富股票排名（MCP 工具 `ft_eastmoney_rank`）

> **MCP 工具**：`ft_eastmoney_rank`（category: `股票数据/行情数据`）。返回统一 MCP 输出：`structuredContent.metadata` + `structuredContent.data`；`content[0].text` 是同值的序列化 JSON，不额外返回 Markdown。输入参数 / 输出参数 / 数据样例见下文。
> 文中 `Response`、`items`、`records`、`code`、`message` 等名称仅为字段说明；MCP 对外固定为上述 `metadata/data`。

- 描述：查询东方财富股票排行榜最新一期快照。支持按榜单组（人气榜 / 飙升榜）与市场（A股 / 港股 / 美股）查询；返回榜单展示名、核心指标名（人气分 / 排名变化）、返回记录数与排行条目（当前排名、排名变化、统一代码、简称、人气分、最新价、涨跌额、涨跌幅、东财原始代码）。提示：`trade_date` 不传默认取最新一期；格式 `YYYY-MM-DD`。
- 数据范围：东方财富排名最新一期快照（无时间序列）
- 单次限量：东财榜单不分页，单次返回当前榜单全部条目（由后端定）
- 提示：
  - 三层命名遵循 CLAUDE.md 来源标识约定：v0 `/stock-rank/eastmoney`、client `eastmoney_stock_rank`、v1 `/eastmoney-rank`、对外类型 `EastmoneyRank*`。
  - `trade_date` 不传默认取最新一期；格式 `YYYY-MM-DD`。
  - 飙升榜 `rank_no` 不一定从 1 开始，核心指标为 `rank_change`。
  - 响应为 `ApiResponse<EastmoneyRankResponseData>` 信封结构。

## 输入参数

| 名称 | 类型 | 必选 | 描述 |
|------|------|------|------|
| rank_group | string | N | 榜单组：`hot`（人气榜）/ `up`（飙升榜） |
| market | string | N | 市场：`A`（A股）/ `HK`（港股）/ `US`（美股） |
| trade_date | string | N | 交易日期，格式 `YYYY-MM-DD`，不传默认取最新 |

## 输出参数

> MCP 固定输出信封为 `structuredContent.metadata` + `structuredContent.data`；`content[0].text` 是与其同值的序列化 JSON，不是 Markdown。
>
> `items` / `records` / `code` / `message` 等传输字段不会直接出现在 MCP 结果中；分页与截断信息统一归入 `metadata`。

| MCP 字段 | 类型 | 必填 | 描述 |
|----------|------|------|------|
| metadata | object | Y | 契约版本、数据来源、工具名、业务口径、总量、分页、返回条数、截断状态及 warnings |
| data | array | Y | 归一化后的业务数据项；元素字段见下方 |

### data 业务字段

EastmoneyRankItem：

| 名称 | 类型 | 默认显示 | 描述 |
|------|------|---------|------|
| rank_no | int | Y | 当前排名（飙升榜不一定从 1 开始） |
| rank_change | int | Y | 排名变化，飙升榜核心指标 |
| normalized_symbol | string | Y | 统一股票代码 |
| stock_name | string | Y | 股票简称 |
| hot_score | string | Y | 人气分 |
| latest_price | string | Y | 最新价 |
| change_amount | string | Y | 涨跌额 |
| change_pct | string | Y | 涨跌幅，百分比，如 `10.02` |
| raw_symbol | string | Y | 东财原始代码，如 `HK_01347` |

## 调用方法（MCP）

> MCP 工具名 `ft_eastmoney_rank`。MCP Streamable HTTP 要求**先 initialize 拿 `Mcp-Session-Id`，发送 `notifications/initialized`，再 `tools/call`**，后续请求同时带该 Session ID 和协商后的 `MCP-Protocol-Version`。返回统一 `metadata/data` 结构化输出；`content[0].text` 为同值 JSON 文本，不额外返回 Markdown。

**curl**：

```bash
SID=$(curl -sS -m 10 -D - -o /dev/null -X POST <MCP_BASE_URL> \
  -H "Accept: application/json, text/event-stream" -H "Content-Type: application/json" \
  -d '{"jsonrpc":"2.0","id":1,"method":"initialize","params":{"protocolVersion":"2025-11-25","capabilities":{},"clientInfo":{"name":"t","version":"1"}}}' \
  | grep -i 'mcp-session-id:' | awk '{print $2}' | tr -d '\r')

curl -fsS -m 10 -o /dev/null -X POST <MCP_BASE_URL> \
  -H "Accept: application/json, text/event-stream" -H "Content-Type: application/json" \
  -H "Mcp-Session-Id: $SID" \
  -H "MCP-Protocol-Version: 2025-11-25" \
  -d '{"jsonrpc":"2.0","method":"notifications/initialized"}'

curl -sS -m 10 -X POST <MCP_BASE_URL> \
  -H "Accept: application/json, text/event-stream" -H "Content-Type: application/json" \
  -H "Mcp-Session-Id: $SID" \
  -H "MCP-Protocol-Version: 2025-11-25" \
  -d '{"jsonrpc":"2.0","id":2,"method":"tools/call","params":{"name":"ft_eastmoney_rank","arguments":{}}}'
```

**Python（`mcp` SDK，自动握手管理 session）**：

```python
import asyncio
from mcp import ClientSession
from mcp.client.streamable_http import streamablehttp_client

async def main():
    async with streamablehttp_client('<MCP_BASE_URL>') as (r, w, _):
        async with ClientSession(r, w) as s:
            await s.initialize()
            res = await s.call_tool('ft_eastmoney_rank', {})
            print(res.structuredContent)   # 推荐：统一 metadata/data 结构化输出
            print(res.content[0].text)   # 兼容：与 structuredContent 同值的 JSON 文本
asyncio.run(main())
```

## 数据样例

`trade_date=2026-06-17`、`display_name=东方财富人气榜-A股`、`metric_name=人气分`、`total=600`，节选（rank 1：京东方Ａ）：

| rank_no | stock_name | hot_score | latest_price | change_pct | rank_change |
|---------|------------|-----------|--------------|------------|-------------|
| 1 | 京东方Ａ | 40212057.527711 | 6.710000 | 10.000000 | 11 |
| ... | ... | ... | ... | ... | ... |
