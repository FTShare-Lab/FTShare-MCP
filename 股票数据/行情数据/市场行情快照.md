# 市场行情快照（MCP 工具 `ft_daec_market_snapshot`）

> **MCP 工具**：`ft_daec_market_snapshot`（category: `股票数据/行情数据`）。返回统一 MCP 输出：`structuredContent.metadata` + `structuredContent.data`；`content[0].text` 是同值的序列化 JSON，不额外返回 Markdown。输入参数 / 输出参数 / 数据样例见下文。
> 文中 `Response`、`items`、`records`、`code`、`message` 等名称仅为字段说明；MCP 对外固定为上述 `metadata/data`。

- 描述：查询全市场或单个交易所的行情快照，包括市场状态、涨跌幅、成交量额，以及按涨跌幅区间统计的九档涨跌分布。
- 数据范围：最新实时快照
- 单次限量：单次返回一个快照对象，无分页
- 提示：
  - `scope` 可选，默认 `ChinaStock`（全市场）；可指定 `Xshg`（沪市）/ `Xshe`（深市）/ `Bjse`（北交所）。
  - 市场状态依据北京时间交易时段判断，非交易时段 `status` 为 `closed`。

## 输入参数

| 名称 | 类型 | 必选 | 描述 |
|------|------|------|------|
| scope | string | N | 市场范围：`ChinaStock`（默认）/ `Xshg` / `Xshe` / `Bjse` |

## 输出参数

> MCP 固定输出信封为 `structuredContent.metadata` + `structuredContent.data`；`content[0].text` 是与其同值的序列化 JSON，不是 Markdown。
>
> `items` / `records` / `code` / `message` 等传输字段不会直接出现在 MCP 结果中；分页与截断信息统一归入 `metadata`。

| MCP 字段 | 类型 | 必填 | 描述 |
|----------|------|------|------|
| metadata | object | Y | 契约版本、数据来源、工具名、业务口径、总量、分页、返回条数、截断状态及 warnings |
| data | array | Y | 归一化后的业务数据项 |

### data 业务字段

| 名称 | 类型 | 默认显示 | 描述 |
|------|------|---------|------|
| change_rate | float64 | Y | 涨跌幅 |
| distribution | object | Y | 涨跌分布（9 档，见下） |
| prev_turnover | string | N | 前成交额，单位元 |
| status | string | Y | 市场状态（如 `closed` / 交易中） |
| turnover | string | Y | 成交额，单位元 |
| volume | int64 | Y | 成交量，单位股 |

distribution 对象（各档股票数量）：

| 名称 | 类型 | 默认显示 | 描述 |
|------|------|---------|------|
| up_limited | int32 | Y | 涨停 |
| up_5pct_to_limited | int32 | Y | 涨 5% ~ 涨停 |
| up_1pct_to_5pct | int32 | Y | 涨 1% ~ 5% |
| up_0pct_to_1pct | int32 | Y | 涨 0 ~ 1% |
| flat | int32 | Y | 平盘 |
| down_0pct_to_1pct | int32 | Y | 跌 0 ~ 1% |
| down_1pct_to_5pct | int32 | Y | 跌 1% ~ 5% |
| down_5pct_to_limited | int32 | Y | 跌 5% ~ 跌停 |
| down_limited | int32 | Y | 跌停 |

## 调用方法（MCP）

> MCP Streamable HTTP 要求**先 initialize 拿 `Mcp-Session-Id`，发送 `notifications/initialized`，再 `tools/call`**，后续请求同时带该 Session ID 和协商后的 `MCP-Protocol-Version`。返回统一 `metadata/data` 结构化输出；`content[0].text` 为同值 JSON 文本，不额外返回 Markdown。

**curl**：

```bash
set -euo pipefail

MCP_BASE_URL="<MCP_BASE_URL>"

check_mcp_response() {
  local response=$1
  printf '%s\n' "$response"
  if printf '%s\n' "$response" | grep -Eq '"isError"[[:space:]]*:[[:space:]]*true|"error"[[:space:]]*:[[:space:]]*\{'; then
    return 1
  fi
}

SID=$(curl -fsS -m 60 -D - -o /dev/null -X POST "$MCP_BASE_URL" \
  -H "Accept: application/json, text/event-stream" \
  -H "Content-Type: application/json" \
  -d '{"jsonrpc":"2.0","id":1,"method":"initialize","params":{"protocolVersion":"2025-11-25","capabilities":{},"clientInfo":{"name":"ftshare-doc-example","version":"1.0.0"}}}' \
  | awk 'tolower($1)=="mcp-session-id:" {print $2}' \
  | tr -d '\r')

if [ -z "$SID" ]; then
  printf '%s\n' 'initialize 未返回 Mcp-Session-Id' >&2
  exit 1
fi

curl -fsS -m 60 -o /dev/null -X POST "$MCP_BASE_URL" \
  -H "Accept: application/json, text/event-stream" \
  -H "Content-Type: application/json" \
  -H "Mcp-Session-Id: $SID" \
  -H "MCP-Protocol-Version: 2025-11-25" \
  -d '{"jsonrpc":"2.0","method":"notifications/initialized"}'

CALL_RESPONSE=$(curl -fsS -m 60 -X POST "$MCP_BASE_URL" \
  -H "Accept: application/json, text/event-stream" \
  -H "Content-Type: application/json" \
  -H "Mcp-Session-Id: $SID" \
  -H "MCP-Protocol-Version: 2025-11-25" \
  -d '{"jsonrpc":"2.0","id":2,"method":"tools/call","params":{"name":"ft_daec_market_snapshot","arguments":{"scope":"Xshg"}}}')

check_mcp_response "$CALL_RESPONSE"
```

**Python（`mcp` SDK，自动握手管理 session）**：

```python
import asyncio

from mcp import ClientSession
from mcp.client.streamable_http import streamable_http_client


MCP_BASE_URL = "<MCP_BASE_URL>"


async def main():
    async with streamable_http_client(MCP_BASE_URL) as (read_stream, write_stream):
        async with ClientSession(read_stream, write_stream) as session:
            await session.initialize()
            result = await session.call_tool(
                'ft_daec_market_snapshot',
                {'scope': 'Xshg'},
            )
            if result.is_error:
                raise RuntimeError(result.content[0].text)
            print(result.structured_content)
            print(result.content[0].text)


asyncio.run(main())
```

## 数据样例

| status | change_rate | volume | turnover |
|------|------|------|------|
| closed | 0.0 | 54187103929 | 1166803236269.93 |
| ... | ... | ... | ... |

distribution 完整 9 档：

| up_limited | up_5pct_to_limited | up_1pct_to_5pct | up_0pct_to_1pct | flat |
|------|------|------|------|------|
| 49 | 112 | 1063 | 464 | 41 |

| down_0pct_to_1pct | down_1pct_to_5pct | down_5pct_to_limited | down_limited |
|------|------|------|------|
| 241 | 313 | 26 | 2 |

## 注意事项

- `status` 依据北京时间交易时段判断，非交易时段为 `closed`。
- `distribution` 各档为快照时刻的股票数量统计，9 档合计覆盖全市场标的。
