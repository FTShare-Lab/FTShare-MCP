# 商誉预测（MCP 工具 `ft_goodwill_predict`）

> **MCP 工具**：`ft_goodwill_predict`（category: `股票数据/财务数据`）。返回统一 MCP 输出：`structuredContent.metadata` + `structuredContent.data`；`content[0].text` 是同值的序列化 JSON，不额外返回 Markdown。输入参数 / 输出参数 / 数据样例见下文。
> 文中 `Response`、`items`、`records`、`code`、`message` 等名称仅为字段说明；MCP 对外固定为上述 `metadata/data`。

- 描述：按报告期查询 A 股个股商誉减值预期/业绩预告明细，返回业绩变动说明、预告期间、最新商誉、上期商誉、预告净利润下限/上限、业绩变动幅度下限/上限、上年同期净利润、公告日期、所属市场。提示：`date` 必填，报告期通常为季末/年末；多数字段可为空，缺失返回 null。
- 数据范围：按报告期查询（`date` 必填，如 `20250331`）
- 单次限量：内存分页，默认 `page=1`、`page_size=50`、最大 200
- 提示：
  - `date` 必填，报告期通常为季末/年末。
  - 对应「商誉减值预期/业绩预告」口径，非实际减值（实际减值见 `goodwill/stock-impairment`）。
  - 多数字段为 `Option<String>`，缺失返回 null。

## 输入参数

| 名称 | 类型 | 必选 | 描述 |
|------|------|------|------|
| date | string | Y | 报告期，如 20250331 |
| page | int | N | 页码，默认 1 |
| page_size | int | N | 每页条数，默认 50、最大 200 |

## 输出参数

> MCP 固定输出信封为 `structuredContent.metadata` + `structuredContent.data`；`content[0].text` 是与其同值的序列化 JSON，不是 Markdown。
>
> `items` / `records` / `code` / `message` 等传输字段不会直接出现在 MCP 结果中；分页与截断信息统一归入 `metadata`。

| MCP 字段 | 类型 | 必填 | 描述 |
|----------|------|------|------|
| metadata | object | Y | 契约版本、数据来源、工具名、业务口径、总量、分页、返回条数、截断状态及 warnings |
| data | array | Y | 归一化后的业务数据项；元素字段见下方 |

### data 业务字段

GoodwillPredictItem：

| 名称 | 类型 | 默认显示 | 描述 |
|------|------|---------|------|
| seq | int | Y | 序号 |
| security_code | string | Y | 证券代码 |
| security_name | string | Y | 证券简称 |
| perform_change_explain | string | Y | 业绩变动说明（如「业绩预增」） |
| predict_period | string | Y | 预告期间（如 20251231） |
| newest_goodwill | string | Y | 最新商誉 |
| goodwill_previous | string | Y | 上期商誉 |
| predict_netprofit_lower | string | Y | 预告净利润下限 |
| predict_netprofit_upper | string | Y | 预告净利润上限 |
| perform_change_lower | string | Y | 业绩变动幅度下限 |
| perform_change_upper | string | Y | 业绩变动幅度上限 |
| pe_samereport_netprofit | string | Y | 上年同期净利润 |
| notice_date | string | Y | 公告日期 |
| trade_market | string | Y | 所属市场（如「主板」） |

## 调用方法（MCP）

> MCP 工具名 `ft_goodwill_predict`。MCP Streamable HTTP 要求**先 initialize 拿 `Mcp-Session-Id`，发送 `notifications/initialized`，再 `tools/call`**，后续请求同时带该 Session ID 和协商后的 `MCP-Protocol-Version`。返回统一 `metadata/data` 结构化输出；`content[0].text` 为同值 JSON 文本，不额外返回 Markdown。

**curl**：

```bash
SID=$(curl -sS -m 10 -D - -o /dev/null -X POST <MCP_BASE_URL> \
  -H "Accept: application/json, text/event-stream" -H "Content-Type: application/json" \
  -d '{"jsonrpc":"2.0","id":1,"method":"initialize","params":{"protocolVersion":"2025-11-25","capabilities":{},"clientInfo":{"name":"t","version":"1"}}}' \
  | grep -i 'mcp-session-id:' | awk '{print $2}' | tr -d '\r')

curl -fsS -m 10 -o /dev/null -X POST <MCP_BASE_URL> \
  -H "Accept: application/json, text/event-stream" -H "Content-Type: application/json" \
  -H "Mcp-Session-Id: $SID" \
  -H "MCP-Protocol-Version: 2025-11-25" \
  -d '{"jsonrpc":"2.0","method":"notifications/initialized"}'

curl -sS -m 10 -X POST <MCP_BASE_URL> \
  -H "Accept: application/json, text/event-stream" -H "Content-Type: application/json" \
  -H "Mcp-Session-Id: $SID" \
  -H "MCP-Protocol-Version: 2025-11-25" \
  -d '{"jsonrpc":"2.0","id":2,"method":"tools/call","params":{"name":"ft_goodwill_predict","arguments":{"date": "20251231"}}}'
```

**Python（`mcp` SDK，自动握手管理 session）**：

```python
import asyncio
from mcp import ClientSession
from mcp.client.streamable_http import streamablehttp_client

async def main():
    async with streamablehttp_client('<MCP_BASE_URL>') as (r, w, _):
        async with ClientSession(r, w) as s:
            await s.initialize()
            res = await s.call_tool('ft_goodwill_predict', {"date": "20251231"})
            print(res.structuredContent)   # 推荐：统一 metadata/data 结构化输出
            print(res.content[0].text)   # 兼容：与 structuredContent 同值的 JSON 文本
asyncio.run(main())
```

## 数据样例

date=20251231，个股商誉预测共 820 条，节选：

| security_code | security_name | perform_change_explain |
|---------------|---------------|------------------------|
| 601211 | 国泰海通 | 2025年一... |
| ... | ... | ... |
