# 风险警示股行情（MCP 工具 `ft_risk_warning_stock_quotes`）

> **MCP 工具**：`ft_risk_warning_stock_quotes`（category: `股票数据/参考数据`）。返回统一 MCP 输出：`structuredContent.metadata` + `structuredContent.data`；`content[0].text` 是同值的序列化 JSON，不额外返回 Markdown。输入参数 / 输出参数 / 数据样例见下文。
> 文中 `Response`、`items`、`records`、`code`、`message` 等名称仅为字段说明；MCP 对外固定为上述 `metadata/data`。

- 描述：查询指定交易日的风险警示股票名单及其实时行情。含最新价、开/高/低/前收、涨跌额/涨跌幅、成交量/成交额、均价、委比、多周期涨跌幅、涨停/跌停价及是否封板、市值、流通股本等。`date` 必填（YYYYMMDD）。提示：`date` 必填，格式 YYYYMMDD。
- 数据范围：按交易日，`date` 指定哪一期风险警示名单；行情为实时快照
- 单次限量：无分页，全量返回该日风险警示股+行情
- 提示：
  - `date` 必填，格式 YYYYMMDD。
  - 每项 = 风险警示基础字段（stock_code/stock_name/risk_type）+ StockInfo 实时行情字段（扁平展开，非嵌套）。
  - 响应为 code/message/data 信封，data 为对象（非分页）。

## 输入参数

| 名称 | 类型 | 必选 | 描述 |
|------|------|------|------|
| date | string | Y | 交易日，格式 YYYYMMDD |

## 输出参数

> MCP 固定输出信封为 `structuredContent.metadata` + `structuredContent.data`；`content[0].text` 是与其同值的序列化 JSON，不是 Markdown。
>
> `items` / `records` / `code` / `message` 等传输字段不会直接出现在 MCP 结果中；分页与截断信息统一归入 `metadata`。

| MCP 字段 | 类型 | 必填 | 描述 |
|----------|------|------|------|
| metadata | object | Y | 契约版本、数据来源、工具名、业务口径、总量、分页、返回条数、截断状态及 warnings |
| data | array | Y | 归一化后的业务数据项；元素字段见下方 |

### data 业务字段

RiskWarningStockQuoteItem：

| 名称 | 类型 | 默认显示 | 描述 |
|------|------|---------|------|
| stock_code | string | Y | 股票代码 |
| stock_name | string | Y | 股票名称 |
| risk_type | string | Y | 风险类型标签，如 ST / *ST |
| open | decimal | Y | 开盘价 |
| high | decimal | Y | 最高价 |
| low | decimal | Y | 最低价 |
| close | decimal | Y | 收盘/最新价 |
| prev_close | decimal | Y | 前收盘价 |
| change | decimal | Y | 涨跌额 |
| change_rate | float | Y | 涨跌幅 |
| amplitude | float | Y | 振幅 |
| volume | int | Y | 成交量 |
| turnover | decimal | Y | 成交额 |
| avg | float | Y | 均价 |
| bid_ask_ratio | float | Y | 委比 |
| bids | array | Y | 买盘档位（`{price,volume}`） |
| asks | array | Y | 卖盘档位（`{price,volume}`） |
| buy_volume | int | Y | 买盘成交量（外盘） |
| sell_volume | int | Y | 卖盘成交量（内盘） |
| limit_up | decimal | Y | 涨停价 |
| limit_down | decimal | Y | 跌停价 |
| up_limited | bool | Y | 是否涨停 |
| down_limited | bool | Y | 是否跌停 |
| change_rate_day5 | float | Y | 5 日涨跌幅 |
| change_rate_day10 | float | Y | 10 日涨跌幅 |
| change_rate_day20 | float | Y | 20 日涨跌幅 |
| change_rate_day60 | float | Y | 60 日涨跌幅 |
| change_rate_day120 | float | Y | 120 日涨跌幅 |
| change_rate_ytd | float | Y | 年初至今涨跌幅 |
| cum_adjust_factor | decimal | Y | 累计复权因子 |
| market_cap | decimal | Y | 总市值 |
| tradable_a_market_cap | decimal | Y | 流通 A 股市值 |
| tradable_a_shares | int | Y | 流通 A 股股本 |
| non_tradable_a_shares | int | Y | 非流通 A 股股本 |
| shares | int | Y | 总股本 |
| turnover_rate | float | Y | 换手率 |
| trades | int | Y | 成交笔数 |
| ts_nanos | int | Y | 行情时间戳（纳秒） |
| buy_orders | int | N | 买委托单数（可为空） |
| sell_orders | int | N | 卖委托单数（可为空） |
| canceled_buy_orders | int | N | 撤单买笔数（可为空） |
| canceled_sell_orders | int | N | 撤单卖笔数（可为空） |
| filled_buy_orders | int | N | 成交买单数（可为空） |
| filled_sell_orders | int | N | 成交卖单数（可为空） |

## 调用方法（MCP）

> MCP 工具名 `ft_risk_warning_stock_quotes`。MCP Streamable HTTP 要求**先 initialize 拿 `Mcp-Session-Id`，发送 `notifications/initialized`，再 `tools/call`**，后续请求同时带该 Session ID 和协商后的 `MCP-Protocol-Version`。返回统一 `metadata/data` 结构化输出；`content[0].text` 为同值 JSON 文本，不额外返回 Markdown。

**curl**：

```bash
SID=$(curl -sS -m 10 -D - -o /dev/null -X POST <MCP_BASE_URL> \
  -H "Accept: application/json, text/event-stream" -H "Content-Type: application/json" \
  -d '{"jsonrpc":"2.0","id":1,"method":"initialize","params":{"protocolVersion":"2025-11-25","capabilities":{},"clientInfo":{"name":"t","version":"1"}}}' \
  | grep -i 'mcp-session-id:' | awk '{print $2}' | tr -d '\r')

curl -fsS -m 10 -o /dev/null -X POST <MCP_BASE_URL> \
  -H "Accept: application/json, text/event-stream" -H "Content-Type: application/json" \
  -H "Mcp-Session-Id: $SID" \
  -H "MCP-Protocol-Version: 2025-11-25" \
  -d '{"jsonrpc":"2.0","method":"notifications/initialized"}'

curl -sS -m 10 -X POST <MCP_BASE_URL> \
  -H "Accept: application/json, text/event-stream" -H "Content-Type: application/json" \
  -H "Mcp-Session-Id: $SID" \
  -H "MCP-Protocol-Version: 2025-11-25" \
  -d '{"jsonrpc":"2.0","id":2,"method":"tools/call","params":{"name":"ft_risk_warning_stock_quotes","arguments":{"date": "20260624"}}}'
```

**Python（`mcp` SDK，自动握手管理 session）**：

```python
import asyncio
from mcp import ClientSession
from mcp.client.streamable_http import streamablehttp_client

async def main():
    async with streamablehttp_client('<MCP_BASE_URL>') as (r, w, _):
        async with ClientSession(r, w) as s:
            await s.initialize()
            res = await s.call_tool('ft_risk_warning_stock_quotes', {"date": "20260624"})
            print(res.structuredContent)   # 推荐：统一 metadata/data 结构化输出
            print(res.content[0].text)   # 兼容：与 structuredContent 同值的 JSON 文本
asyncio.run(main())
```

## 数据样例

| stock_code | stock_name | risk_type | change_rate | amplitude |
|------------|------------|-----------|-------------|-----------|
| 000004 | *ST国华 | *ST | 0.0 | 0.0 |
| ... | ... | ... | ... | ... |

> 真实数据样例（date=20260617，公网实测 total=223，此处摘录其中一条）
